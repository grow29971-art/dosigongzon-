# 개발일지 2026-07-18 — 부채청산 실측·디자인 벤치마킹·온보딩 재설계·크론 결행 원인

> 어제(7/17) "내일 하자"로 남긴 것들 이어서. 코어는 여전히 문서·실측 중심이나, 사장님 지시로
> **온보딩 재설계는 실제 구현·배포**함. 크론 결행 원인도 코드 추적으로 확정.

## §1. box SQL 대장 "확인필요 5건" → 전부 [적용] 확정 (커밋 ad11777)
service_role REST 프로브로 객체 존재/동작 확인. 어제 보안 SQL 오진과 같은 패턴(문서가 실제보다 비관적).
- care_cleanliness(`cats.cleaned_at` 200) · weekly_points(checkin_days·user_points·point_ledger·orders.points_used 200) · security_invoker_views(뷰 2개 존재) · cat_photos(anon 업로드 403) · admin_rate_limit_exempt(운영자 동일 초 10건+ 대량발송 = 면제 활성 행동증거).
- 요약: 적용 153→158, 미적용 2→1, 불명 10→6.

## §2. TNR 지불의향 1문항 (커밋 694d573)
인터뷰 후속용. "지지하세요?"(예의상 답) 대신 구체 금액(5천원)+"마음은 있어도 안 하게 될 것 같다" 퇴로로 진짜 답 유도. 돈 걷는 링크 없음(순수 신호 탐침, 모금·결제 규제 무관). → `box/인터뷰_리마인드_문구_20260717.md §5`.

## §3. 디자인 벤치마킹 (커밋 f16289b, `box/디자인_벤치마킹_20260718.md`)
디자인 3에이전트가 Toss·당근·Duolingo·Strava·오늘의집을 웹리서치. **수렴점: 코어 행동 하나(밥주기)를 온보딩·홈·넛지 전부 최상단에.**
- 온보딩: 가입을 첫 행동 뒤로(inverted). 지도·밥주기: 병목은 동선 아닌 cue 부재(마커 바텀시트·주간 streak·2티어 푸시). 홈/네비: 홈 최상단="오늘 밥 줄 고양이 1탭", 바텀 6→4탭.
- 대부분 신규빌드 아닌 재배치(MyCatsQuickCare·hungryCatName·FirstCheerCard 이미 존재).

## §4. 온보딩 재설계 — 실제 구현·배포 (커밋 76561a4)
벤치마킹 1순위(활성화) 적용. `app/onboarding/page.tsx` 단일 파일(롤백 용이).
- **3단계**: intro(감성 3장→1장) → **pick(실제 고양이 카드에서 "챙길 아이 고르기"=가입 전 첫 행동)** → start(시작점, 고른 아이로 가입 카드 개인화).
- 고른 아이 = localStorage(`dosigongzon_pending_care`) 커밋. 가입 전 DB 케어는 RLS상 불가라 "고르기(주인공 의식)"로 정직하게. 고양이 없으면 "첫 등록자 되기" 대체. anon `listCats`(public만) 재사용.
- 검증: tsc 통과·build 성공·프로덕션 200(city-amber-omega, dosigongzon.com). ⚠ 헤드리스라 캐러셀 인터랙션 육안 미확인 → 사장님 폰 확인 권장. 기존 유저는 onboarded 플래그로 영향 없음(신규만).
- **미완(follow-up)**: 가입 후 홈에서 `dosigongzon_pending_care` 이어받아 "방금 고른 ○○ 첫 밥 주기" 핸드오프 — 별도 커밋 예정.

## §5. 크론 admin-daily-digest 결행 원인 확정
**증상**: admin-daily-digest(`10 0 * * *`) 발화 이력 7/14✅(00:37)·7/15✅(00:29)·**7/16❌**·7/17✅(00:44)·**7/18❌**. 발화해도 정시(00:10) 아닌 창 내 늦은발화, 40% 결행.

**원인 (코드 추적 + 웹확인)**:
1. **하트비트가 미들웨어(`proxy.ts` logCronRun)에 있음** → cron_runs 행 부재 = 라우트 실패 아니라 **Vercel이 URL을 아예 호출 안 함.** 앱 코드(디지스트 로직) 결백.
2. **Vercel Hobby 크론 = 하루 1회 한정 + 정시 보장 없음 + 배달 자체가 베스트에포트(누락 가능).** (vercel.com/docs 확인) 예약시각~다음 1시간 사이 랜덤 발화, 일부 누락.
3. **악화요인**: 00시대에 news-crawl(00:00)·digest(00:10)·payment-reconcile(00:20) 3개 클러스터 → 가운데 digest가 반복 희생.
4. **7/13 스케줄 이동(23:00→00:10)은 오진 처방** — 붐비는 00시대 창으로 밀어넣은 셈이라 결행 지속.

**해결 — 구현·검증 완료 (커밋 98ba42d, 배포됨)**:
- **`daily-dispatch`(0 0 * * *) 디스패처 신설** — news-crawl·admin-daily-digest·payment-reconcile를 병렬 호출(`Promise.allSettled`, 각 실패 격리). Hobby 보장 대상 3→1, 세 잡 동반 실행을 코드로 보장.
- vercel.json: 개별 3크론 제거, 디스패처 1개로(21→19). 기존 라우트는 그대로 두고 디스패처가 호출 → 서브 호출도 미들웨어 하트비트로 cron_runs 기록 유지(모니터링 보존).
- **검증(2026-07-18 01:30 UTC 수동 실행)**: 응답 3잡 전부 status 200·ok, 총 39초(60초 한도 내). cron_runs에 dispatch→admin-daily-digest·payment-reconcile·news-crawl 4행 즉시 기록 확인.
- 남은 옵션(미구현): payment-reconcile은 돈 관련이라 GitHub Actions 이중 호출까지 두면 더 안전. Pro 승급($20)은 WAU 6엔 과함.
- 롤백: daily-dispatch 제거 + 3개 개별 크론 복구.

**정정**: 어제 "engagement-push만 유령"이라 했으나, admin-daily-digest도 간헐 결행(플랫폼 배달 문제)으로 추가. payment-reconcile은 7/17·7/18 2일 연속 정상(회복).

## §6. USB 백업 — 다른 PC로 작업 이전 준비
다른 PC로 옮기기 위해 **git에 없는 것들** 중심으로 USB(D:) 전체 백업 생성.
- 위치: `D:\city-backup-20260718\` (총 28MB). 구성:
  - `repo/city.bundle` — `git bundle --all`(전체 히스토리, HEAD=15a01e8). `git bundle verify` OK(오프라인 복원 가능).
  - `secrets/.env.local` + `secrets/signing.keystore`(재발급 불가 서명키).
  - `claude-memory/` — 프로젝트 메모리 17개.
  - `RESTORE.md`(수동 4단계+체크리스트) + `restore.ps1`(clone→origin GitHub 교체→.env 복사→npm install 자동) + `MANIFEST.txt`.
- 복원 요약: 새 PC에서 `restore.ps1` 실행(또는 `git clone` GitHub 최신) → keystore·메모리만 수동 배치.
- ⚠ 주의: USB에 **키 평문** 포함(분실=유출, 공용PC 복원 후 secrets/ 삭제 권장). **keystore 비밀번호는 백업에 미포함**(보안상) — 본인 비번관리자에서 확인 필수. 코드는 GitHub에도 최신이라 USB 핵심 가치는 시크릿·keystore·메모리 3종.

## §7. 크론 상태 확정 (7/18 02:04 UTC 관측)
디스패처 배포 후 실측 재확인. (하트비트=proxy.ts 미들웨어 → 행 부재=Vercel 미호출)
- **admin-daily-digest ❌ 7/18 결행 확정**: 00:00~01:00 창 빔(자동발화 없음, 창 닫힘). 7/16·7/18 miss = 간헐 결행(~40%) 재확인. ※01:30 발화는 내가 돌린 수동 디스패처라 자동 아님. → **오늘부로 개별 크론 제거됨**(vercel.json), 내일부터 daily-dispatch가 대신 호출.
- **payment-reconcile ✅ 회복**: 7/18 00:20:22 자동발화 + 7/17 정상 = 2일 연속. (00:20 스케줄 유지 중이었으나 이 역시 오늘부로 daily-dispatch로 흡수됨.)
- **daily-dispatch ✅ 배포·수동검증**(01:30 3잡 200 ok, 39초). **내일(7/19) 00:00 UTC 첫 자동발화**가 실전 검증 포인트 — 그때 news-crawl·admin-daily-digest·payment-reconcile가 dispatch 뒤 다 찍히면 결행 근본 해소 확인.
- **onboarding-nudge ❌ 확정 유령**(03:04 UTC 재확인): 7/18 창(02:00~02:59) 발화 없음 + **계측(7/13) 이후 전체 이력 0건**. engagement-push(수)에 이은 **weekly 유령 2번째**.
- **engagement-push ❌ 확정 유령**: 수요일 크론, 계측 이후 발화 0건.
- **결론**: Hobby가 daily뿐 아니라 **weekly 크론도 떨군다.** daily-dispatch는 00시대 3개만 커버 → weekly도 dispatcher 통합 or 제거 검토 필요. 단 engagement-push·onboarding-nudge·weekly-postcard-push는 저활용 푸시(WAU 6)라 **통합보다 제거가 나음**. **weekly-battle-payout(지급)·retention-report(지표)만 통합해 살릴 가치** — 특히 payout 결행은 지급 누락 위험.
- **미확인(폴링 안 함, 다음 세션 확인)**: ①daily-dispatch 첫 자동발화(7/19 00:00 UTC) = 결행 근본해소 실전검증 ②일요일 배치(weekly-battle-payout·retention-report·weekly-digest·postcard, 7/19 23:00 UTC 창).
- **크론 재확인 루프 종료.**

## §8. 비로그인 참여 — 펀드투표 + 게스트 구매 인프라
### 비로그인 펀드 투표 (커밋 1d9a5f7)
- `fund_votes_anon` 테이블 + `cast_fund_vote_anon`/`fund_vote_results`(합산) RPC. 기기 단위(localStorage anon_id) 1표.
- 한계(의도적): 로그인 없어 "1인 1표" 보장 불가 → 캐주얼 중복만 방지, 결과는 방향성 신호로 해석.
- ⚠ SQL 실행 필요: `box/supabase_fund_vote_anon_migration.sql`

### 게스트 구매 인프라 (커밋 b4b4e59 백엔드 + 02a71d4 UI)
- 보안 설계: anon RLS INSERT를 열지 않고 **SECURITY DEFINER RPC**로 서버가 금액·재고 재계산해 pending 주문 생성(기존 감사 철학 일치). 게스트는 order_number+guest_token으로만 접근.
- 백엔드: orders.user_id nullable+guest_token, owner_check, `create_guest_order`/`get_guest_order`/`cancel_guest_order` RPC. repo에 createGuestOrder/getGuestOrder/cancelGuestOrder + 기기 기억.
- UI: 게스트 장바구니 localStorage 분기(shop-repo), 상품상세 requireLogin 제거, 장바구니·체크아웃 로그인 게이트 제거, Toss ANONYMOUS 결제 분기.
- **결제 하드락 유지** → 지금은 담기~주문서까지만, 결제 버튼은 계속 잠김(안전). 프로덕션 /shop·/cart·/checkout 200.
- ⚠ SQL 실행 필요: `box/supabase_shop_guest_order_migration.sql`
- **결제 오픈 전 남은 것(Stage 4)**: ①payment/success 게스트 처리(guest 토큰으로 조회+게스트 장바구니 clear) ②게스트 주문조회 페이지(order_number+token) ③/api/payment/confirm·webhook가 user_id 없는 주문 정상 처리하는지 검증.

## 다음 (사장님/후속)
- [ ] **(사장님) SQL 2종 실행**: `supabase_fund_vote_anon_migration.sql`, `supabase_shop_guest_order_migration.sql` (실행 전엔 해당 기능 실패-안전)
- [ ] 온보딩 폰 육안 확인 + 홈 핸드오프 follow-up 여부 결정
- [x] ~~크론 디스패처 통합~~ 구현·배포·검증 완료(§5). 내일 00:00 UTC Vercel 자동 발화로 실전 확인.
- [ ] onboarding-nudge(토 02:00 UTC)·일요일 주간배치(7/19) 발화 확인 — Hobby 창 특성상 02시대/23시대 전체 지나야 판정
- [ ] (이월) 인터뷰 7/20 집계, keystore 2번째 사본, LBS 신고
