-- 도시공존 — 냥숏츠(고양이 릴스) 기능 완전 제거에 따른 DB 정리
-- 코드(라우트·크론·관리자 페이지)는 이미 삭제됨. shorts 테이블은
-- 유튜브 임베드 메타데이터만 있고(영상 파일 자체는 스토리지에 없음)
-- 조회수 합계 695(전체 1,713건 대비 사실상 거의 안 쓰임) — 삭제해도
-- 되돌릴 가치가 없다고 판단해서 테이블째 제거.
--
-- ⚠️ 되돌릴 수 없는 작업입니다. 실행 전에 필요하면 먼저 백업하세요:
--   select * from shorts;  -- CSV로 내보내기 등

drop table if exists public.shorts cascade;

select 'shorts 테이블 삭제 완료' as status;
