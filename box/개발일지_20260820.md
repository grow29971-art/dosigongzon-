# 개발일지 2026-08-20

## 오늘 한 일 요약
구매후기 기능 신규 → 원탁회의(12관점) → 회의 결론 실행 → 결제 오픈 게이트 착수. 커밋 6개 전부 배포 완료(state: success).

| 커밋 | 내용 |
|------|------|
| `6cc5267` | 쇼핑몰 구매후기 시스템(별점+사진, 실구매자만 RLS 강제) |
| `860a27c` | 첫밥 바 fallback — 봉인된 Day0 핵심 CTA 수리 |
| `7f93879` | 배송비 greatest → 품목당 합산 (게이트 3) |
| `5640806` | 포인트 상한 30% → 10% (게이트 5) |
| `d8f295a` | 반품비 3,000원 고정 → 주문 배송비 연동 (게이트 6) |
| `b19699d` | 환불·취소 시 후기 자동 숨김 트리거 SQL (리뷰 세탁 방어) |

---

## 1. 구매후기 기능 (요청)
- **신규**: `lib/shop-reviews-repo.ts`, `app/(main)/shop/[id]/ProductReviews.tsx`, `box/supabase_shop_reviews_migration.sql`
- 별점 1~5 + 내용 + 사진 최대 3장(cat-photos 버킷 재사용, webp 변환으로 EXIF 제거), 상품당 1인 1후기.
- **핵심**: "구매자만 작성"을 RLS insert/update `with check`가 주문 이력(paid 이후 상태)으로 강제. product_id 바꿔치기도 재검증으로 차단. 버튼 숨김이 아닌 DB 레벨 방어.
- 가상상품(후원)은 후기 대상에서 제외.
- 상세 페이지(`ProductDetailClient.tsx`)에 섹션 마운트.

## 2. 원탁회의 (사장님 지시로 "뭐할까 회의 기각 독트린" 번복)
11개 관점(그로스·BM·리서치·한국심리·개발·UX·보안·인프라·B2G·법률·프로파일러) + 악마의 변호인 종합.
- **실측 발견**: 주간 케어기록 44건/7명 → 9건/3명 급락(-80%). 첫밥 CTA(FirstFeedBar)가 빈사 상태 pick 산출물에 묶여 실노출 ~0. 인스타 유입이 트래픽 71%인데 가입 귀속 배선 없음. pick은 "사망"이 아니라 빈사(8/8 이후 2건).
- **P0 압축(악마의 변호인)**: ① 성동구 접수 ② service 키 정합/하트비트 ③ 급락 진단 → 첫밥 fallback.
- 결제 게이트 패키지는 통판신고 "수리" 통보가 트리거인 다음 주 패키지로 분류(당시).
- 회의록: `~/.claude/.../memory/project_council_20260820.md`.

## 3. 회의 결론 실행
### (a) service 키 401 — 오탐 판정
- 원인: PowerShell `Invoke-WebRequest`가 브라우저 User-Agent를 보내 Supabase가 신형 `sb_secret_` 키를 차단("Forbidden use of secret API key in browser"). **curl로는 200 정상.**
- **교훈: sb_secret 키 프로브는 반드시 curl로.** IWR 금지.
- `cron_runs` 실측: 크론 23종이 8/13 이후 매일 정상 발화(payment-reconcile 포함). 7월 결행 재발 없음.
- 부수 발견: `.env.local`의 URL·anon 키 값 끝에 리터럴 `\n` 오염 잔존(실키는 정상, 로컬 추출 시 제거 필요).

### (b) 케어 급락 진단 — 시스템 아님
- 헤비유저 '경기푸리'(4/24 가입) 1명이 8/12 이후 완전 침묵(방문·글·댓글 0, 푸시 미구독이라 도달 수단 없음)이 급락분 절반. 나머지는 2명 페이드.
- 브로드캐스트는 과녁이 아님(도달 대상에 이 유저 없음).

### (c) 첫밥 바 fallback (`FirstFeedBar.tsx`)
- 기존: pick 산출물(`pending_care`) 일치 시에만 노출 → pick 빈사로 실노출 0.
- 추가: **가입 7일 이내 && care_logs 0건**이면 모든 고양이 상세에서 노출. 문구는 "○○에게 첫 밥 기록하기"로 분기.
- `signup_home` 계측은 pending 경로에서만 발화 유지(정의 보존, 계측 오염 방지).
- 판정 9/2: 첫밥 바 노출 유저 > 0(현재 0) + first_feed 주간 3건.

## 4. 결제 오픈 게이트 패키지 (사장님 "지금 못함?" → 착수)
근거: `box/쇼핑오픈_Dday_체크리스트_20260807.md`. PAYMENT_ENABLED=true(마지막 단추)만 통판 수리 대기, 나머지는 지금 처리.

### 출혈 코드 3종
- **게이트 3 배송비**: `computeCartTotal`(shop-repo)·`payment/confirm`·`payment/webhook` 세 계산 지점을 `Σ(배송비×수량)`으로 통일. 게스트 RPC는 `box/supabase_shop_shipping_sum_migration.sql`(실행 대기). ⚠ 네 지점 식이 다르면 금액 검증(integrity) 어긋나 결제 전부 거부되므로 반드시 동일.
- **게이트 5 포인트**: `POINTS_MAX_USE_RATE` 0.3 → 0.1. 하드코딩 "30%" 문구 3곳(order-repo 에러·checkout UI·confirm 주석)을 상수 연동으로 교체(재발 방지).
- **게이트 6 반품비**: `RETURN_SHIPPING_FEE` 3,000 고정 → `returnShippingFeeFor(주문 배송비)`. 주문 배송비 > 0이면 그 금액(반품 편도), 무료배송이면 왕복 실비(RETURN_SHIPPING_FEE_BASE 5,500×2). `RefundOrderInput.shippingFee` 추가, 서버(refund-executor)·클라(주문 상세) 동기화. **`/shop/policy` 고지가 상수를 직접 import·렌더** → 코드-고지 불일치 원천 차단(전자상거래법 계약).

### 보안
- **환불·취소 → 후기 자동 숨김 트리거**: `box/supabase_shop_review_refund_guard_migration.sql`. product_reviews.hidden 컬럼 + 읽기 정책 교체(숨긴 후기는 본인·관리자만) + orders refunded/cancelled 전이 트리거. 다른 유효 주문 있으면 유지, 삭제 아닌 보존. "결제→별점5→환불" 리뷰 세탁 방어.
- **후기 침투 프로브**: A(anon insert)=42501 차단 OK, D(anon select)=200 공개 OK. B/C(비구매 insert·product_id 스왑)는 로그인 JWT 필요라 RLS 정적 검증으로 대체.
- **"결제 High 2건 잠복" = 오탐**: H-1·H-2는 8/4에 이미 수정 완료된 항목(refund-policy.ts 방어 로직 확인). 프로파일러가 옛 팬테스트 메모 참조.

---

## 실행 대기 (사장님)
1. **SQL 2건** (Supabase SQL Editor, RUN_ORDER 12·13번):
   - `box/supabase_shop_shipping_sum_migration.sql` (게스트 주문 배송비 — 코드와 식 불일치 시 결제 검증 전부 막힘, 필수)
   - `box/supabase_shop_review_refund_guard_migration.sql` (후기 세탁 방어)
2. **통신판매업 신고 "수리" 확인** (정부24, 접수≠수리) → 확인되면 남은 게이트(상품 3종 축소·무료배송 해제·법률 4건·기부율 단일화·재고 확정) + 마지막 `PAYMENT_ENABLED=true`.

## 남은 게이트 작업(코드)
- 웹훅 라이브 실사격(라이브 키 필요)
- 게이트 2/4/7~11 (상품 축소·무료배송 3종 해제·법률 placeholder·부가세 표시·게스트 결제 동의·미성년 취소권·사료성분 서면·기부율 단일화·재고 실수량)
- 상품 안전 문구(이동장·츄르·쉼터·영양제·급식그릇) + caution 컬럼
