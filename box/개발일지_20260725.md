# 개발일지 2026-07-25 — ag_meta_council 신규 (12인 회의 규율 × 에이전트 체계 융합)

> 안건: 7/17 전체 회의록과 에이전트 체계 문서를 융합해 "나만의 서브에이전트"를 만들어 달라는 요청.
> 산출물: 원탁회의 총괄(참모총장) 에이전트 1기 + README meta 계열 신설. 코드 변경 없음(앱 배포 불필요).

## §1. 만든 것 — `.claude/agents/ag_meta_council.md` (00f0901)

**정체**: 12개 전문 관점(BM·그로스·리서치·악마·한국심리·도메인·B2G·디자이너·개발·인프라·보안·시크릿·QA)을
한 에이전트 안에서 소집해 반박→재반박→합의까지 끌고 가는 **의사결정 엔진**. 결론 없는 회의 금지 —
산출물은 토론이 아니라 결정(P0/P1/P2 + 판정 기준 박제).

**융합 설계**:
- 7/17 회의록의 만장일치 원칙 → **헌법 6조**로 승격:
  ① 실측>추정>직감(수치에 `[실측]/[추정]/[벤치마크]` 라벨 강제) ② 판정 기준 선등록(사후 해석 조작 차단)
  ③ 인터뷰=가설·판정=행동데이터("대신 뭘 쓰나" 번역) ④ 악마 좌석 상설(반대 0개면 통과 불가)
  ⑤ 1인 사업자 리스크 상수(가용 시간 초과 P0 = 결함) ⑥ "대기"는 회피처 금지.
- 에이전트 체계 문서의 토론 조합 → **3가지 회의 모드**:
  - ⚡ 스피드 판정: 제안→악마→리서치 3인, 결론 5줄(결정/근거/반대/죽는 조건/첫 행동).
  - 🏛️ 전체 회의: 12관점 전원 → 교차 반박 → 합의/표결 → `box/회의록_YYYYMMDD_주제.md` 자동 박제
    (긴급 발견/합의/관점별 발언/🚫 하지 말 것/📋 P0·P1·P2 — 기존 회의록 포맷 그대로).
  - 🚦 게이트 체크: 빌드·배포·SQL 실행 직전 6문(북극성 기여? 판정 기준? 금지 목록? 롤백? RUN_ORDER? 가용 시간?).

**앱 상수 내장** (에이전트가 회의 전 자동 로드): 북극성=주간 케어기록 수, 병목=Day0 활성화(당일 ~78% 이탈),
보안 게이트=RUN_ORDER SQL 선행, 동결 금지 목록, 행정=LBS 신고>통신판매업.

**미래지향 장치 2개**:
- 하이브리드 소집: 시뮬레이션으로 부족한 안건(코드 검증·실데이터·침투)은 실제 `ag_*` 서브에이전트를 병렬 호출해 회의에 반입.
- 자기 진화 조항: 같은 결정 2회 반복 시 해당 에이전트 파일 독트린으로 승격 제안. 과거 회의록과 모순되는
  결정은 "무엇이 바뀌어서 뒤집는가" 명시 의무.

## §2. README 갱신 (같은 커밋)

- 계열에 **meta**(총괄) 신설, 목록 최상단에 ag_meta_council 등재.
- 변경 이력에 2026.07.25 융합 배경 1줄 추가.

## 사용법

- "회의 소집해줘" / "뭐부터 해야 돼?" / "이거 할까 말까" / "우선순위 정해줘" → ag_meta_council 반응.
- 큰 결정은 🏛️ 전체 회의 모드가 box/에 회의록을 남기므로 기존 회의록 아카이브와 연속성 유지.

## 롤백

- `git revert 00f0901` 한 방(파일 2개: 에이전트 신규 + README 2줄). DB·배포 무관.

## 남은 것 (기존과 동일 — 오늘 변동 없음)

- 사장님 SQL: RUN_ORDER 순서 실행 (dm 하드닝은 즉시 가능, profiles/supplier는 브랜치 머지 게이트).
- 퍼널 6스텝 전환율 첫 실측 대기 (7/24 저녁 부활 배포 후 수일 누적 필요).
- 크론: 다음 자동발화에서 서브잡 하트비트 검증.
