# 개발일지 2026-08-12

## 통신판매업 신고 제출 확인 → 화이트리스트 ① 발동

사장님이 토스페이먼츠 구매안전서비스 확인증으로 통신판매업 신고 제출 완료(8/12).
8/10 최종판정의 화이트리스트 ①(찜 서버 계측 1커밋) 해제조건 충족 — 당일 구현·배포.

### 찜 서버 계측 (커밋 9b1f665)

- **왜**: 결제 오픈 게이트(유니크 15명·찜 40개·신고 수리) 중 앞 2개가 측정 불가였다
  (찜이 localStorage뿐, 서버 0건 — 8/10 실측). 계측 없이는 오픈 판정 자체가 안 됨.
- **설계**: funnel_events 재사용 불가(기기당 스텝 1회 unique라 찜 개수를 못 센다) →
  전용 이벤트 로그 `shop_wishlist_events` (add/remove, 현재 상태는 마지막 이벤트로 계산).
  anon_id·source는 퍼널과 공유(`lib/funnel-repo.ts`에서 export) — "유니크 N명"이
  퍼널 방문자와 같은 단위로 잡힌다.
- **경로**: toggleWishlist → fire-and-forget POST `/api/shop/wishlist`(service role +
  검증 + IP 분당 30회) → insert. RLS는 정책 0개로 전면 차단(클라이언트 직접 접근 없음).
- **⚠ 실행 게이트**: `box/supabase_shop_wishlist_events_migration.sql` — 사장님 SQL Editor
  실행 전까지 수집이 조용히 실패한다(퍼널 스텝 SQL 지연 전례 2회). 실행 후 라이브 프로브 검증.

### 아침 프로브 3종 (8일 플랜)

- **cron 하트비트 ✅**: 8/11·8/12 양일 팬아웃 정상 — daily-dispatch 09:26 발화,
  서브잡(payment-reconcile·weather-alert·engagement-push·admin-daily-digest·news-crawl)
  동분 실행. 결행 없음.
- **푸시 구독**: 레코드 39개 / 고유유저 33명.
- **청중 교집합**: 최근 7일 케어유저 7명 ∩ 푸시 구독 = **4명** (8/10 측정과 동일).

### 5일 트래픽 실측 (8/7~8/11, 어제 세션)

- 고유 방문 284명(일 51~61), 신규가입 16명, first_feed 1건, 케어 21건/5명.
- 귀속 1위 ig(91건) — 인스타 10만원이 유입은 만드는 중. **재방문 사실상 0**
  (일별 고유 합 285 vs 5일 전체 고유 284) — 깨진 독 상태 지속, 8/24 광고 판정 예정대로.

## 다음

1. **(사장님)** shop_wishlist_events SQL 실행 → 알려주면 라이브 검증
2. **(사장님)** daily_stats 공개 차단 SQL 30초 (유일 미실행 잔존)
3. 신고번호 수령(~3일) → 토스 라이브 전환 심사 신청 → D-day 12단계
   (`box/쇼핑오픈_Dday_체크리스트_20260807.md`)
4. 8/14 인터뷰 1차 집계, 8/18 판정 회의
