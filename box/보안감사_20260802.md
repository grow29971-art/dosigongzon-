# 도시공존 보안감사 — 2026-08-02

> 서브에이전트 4종(ag_ops_security · ag_dev_nextjs · profiler · ag_qa_devils-advocate)이 실제 코드(`C:\Users\grow2\city`)를 Read/Grep으로 정독 + anon 키 읽기전용 REST 프로브로 대조. **프로덕션 DB 쓰기 0.** 7/30 감사(`보안감사_20260730.md`)·7/31 팬테스트(`팬테스트_20260731.md`) 이후 상태 재검.

## ⚡ 한 줄 결론
**라이브 코드에 즉시 터질 Critical/High 없음.** 지난 감사·팬테스트가 제 일을 했다. 남은 실질 구멍은 **"파일엔 있는데 프로덕션에 안 돌린 SQL 하나"**, 그리고 가장 위험한 건 **코드 밖(외부 채널 특허 표기)**에 있다.

---

## 🔴 노출 전 반드시 (3개 — 대부분 성우만 가능)

### 1. profiles 락다운 SQL 미실행 — 유일한 실질 기술 구멍 [Medium·실측 확정]
- **오늘 anon 실측**: `GET /rest/v1/profiles?select=id` → **345행 전부 반환**(`Content-Range: 0-0/345`). base SELECT 정책이 아직 `USING(true)`.
- anon은 `invite_code/coins/email` 컬럼이 revoke돼 막히지만(anon 롤 한정), **행 정책이 전면 공개**라 **로그인한 아무 계정이나** 자기 JWT로 `profiles?select=invite_code,coins,invited_by,marketing_push_enabled` 한 방에 **345명 전원의 초대코드·코인잔액·초대관계·마케팅수신동의**를 덤프 가능. → 7/30 Medium이 미해결로 그대로.
- **앱 코드는 이미 `profiles_public` 뷰로 전량 이관 완료**(직접조회 0건 실측). 즉 **락다운 SQL만 돌리면 앱 안 깨지고 닫힘.**
- **조치(성우, Supabase SQL Editor)**: `box/supabase_profiles_lockdown_RUN_ORDER_20260730.md`의 **step 0 → 2 → 4**.
  - step 0: `select policyname,cmd,qual from pg_policies where tablename='profiles' and cmd='SELECT';` — 실제 정책명이 락다운 SQL의 drop 대상과 다르면 그 이름 추가.
  - step 2: `supabase_profiles_lockdown_rpcs_migration.sql` (무해·추가만)
  - step 4: `supabase_profiles_authenticated_lockdown_migration.sql` (실제 잠금)
- **검증(합격 기준)**: 비관리자 **로그인 JWT**로 `GET /profiles?select=invite_code,coins` → **본인 1행만**. (SQL Editor `count(*)`는 postgres 롤이라 RLS 우회 → 무의미.)

### 2. zone_reports anon 프로브 미실시 [검증 필요·악마의변호인]
- 학대 제보 자유서술(특정인 지목 가능한 고발문) 테이블. RLS는 파일상 admin-only(`box/supabase_guardian_qr_migration.sql:74-90`)로 **완벽하게** 있으나, **7/30 이후 신설이라 anon 프로브를 한 번도 안 돌렸다.**
- 이 프로젝트는 "SQL 파일 존재 ≠ 프로덕션 적용"이 **이미 두 번 실증**된 이력(profiles가 그 증거).
- **조치**: anon 키로 `GET /rest/v1/zone_reports?select=*` 한 번 찔러 **빈 배열이 아니면 즉시 노출 중단·SQL 재적용.**

### 3. 외부 채널 "특허" 표기 훑기 — 결과 최악, 근데 코드 밖 [형사]
- 앱 코드는 이미 깨끗(특허 단정 표기 0건 실측). `f3f99de` 커밋은 **내부 문서만** 고쳤고 앱스토어·크라우드펀딩·홈페이지·정부지원 서류는 못 건드림.
- 거기 "특허 완료/등록/보유" 한 줄 살아있으면 → **스크린샷 한 장 = 특허법 제224조의2 허위표시(3년 이하 징역).** 되돌릴 수 없음.
- **조치(성우)**: 모든 외부 노출 채널을 눈으로 훑어 **"출원 중(10-2024-0030613)"** 외 표현 전멸 확인.

---

## ✅ 7/30 이후 닫힌 것 (실측 검증)
- **GPS 실좌표 서버 전송 (위치법 무신고 구성)** — 홈 날씨 ~5km 격자, 활동지역 행정동 스냅, 지도 watchPosition은 표시전용, 고양이 등록 ±444m 브라우저 오프셋 후 전송. anon `cats?select=lat,lng` → 42501. **실좌표 서버 도달 경로 없음.**
- **EXIF/GPS strip** — 업로드 11개 경로 전부 canvas 재인코딩(webp)만 업로드. 원본 직업로드 0건.
- **특허 앱 표기** — "출원 중(출원번호)" 통일, UI 단정표기 0건.
- **시크릿** — service_role `import "server-only"` 하드경계. NEXT_PUBLIC·클라 번들에 service_role/Toss/Gemini/VAPID-priv 유출 0. 에러 stack 유출 0.
- **7/31 팬테스트 발견분**(QR XSS·push SSRF·chat/OG 비용·작성자 위조 등) 전부 수정·배포.
- **머니패스**(shop/buy·coins·points·기부 5%) 원자 RPC + fail-closed + 멱등키, IDOR 없음. 결제 웹훅 서명검증·금액 재계산·중복재전송 방어 유지.

---

## 🟡 코드 수정 (중요, 비긴급 — VS코드 처리)
- **[profiler] care_logs 무검증 조작** — INSERT가 `cat_id` 소유·근접 검증 없이 통과, 사진 없어도 됨. 하루 150건으로 **공개 랭킹·카드 경험치·코인**을 실제 돌봄 없이 부풀림 + 실제 캣맘에게 "누가 챙김" 오알림(방치 유도). → 코인/경험치/랭킹 반영을 `photo_url IS NOT NULL`로 한정 or (author,cat) 쿨다운.
  - `lib/care-logs-repo.ts`, `box/supabase_care_logs_migration.sql`, `lib/ranking-repo.ts`
- **[profiler] 학대경보(alert) 댓글 = 공개 무고 벡터** — 정식 신고(reports/zone-report)는 24h dedup·익명·Turnstile로 방어되는데, 같은 심각도 공개 낙인 기능인 `alert` 댓글만 대상별 dedup·admin 큐가 없음. → `alert` kind에 (author,cat) 24h dedup or admin 큐 우선.
  - `lib/cats-repo.ts:987-997, 1041`
- **[profiler] DM에 위치·신상 필터 누락** — 다른 모든 입력창엔 있는 `findLocationViolations`가 `sendDM()`엔 import조차 안 됨. 스토킹 신상 전달 경로. → DM·circle 채팅에도 적용.
  - `lib/dm-repo.ts:3, 37-40`

## 💸 비용·프로세스 (자는 새벽 대비)
- **[dev] AI 카드생성 글로벌 총량 상한 없음** — per-user 인메모리 리밋뿐, Vercel 다중 인스턴스에서 ×N 무력화. → `ai_usage_counter(date)` + security-definer RPC로 플랫폼 일일 상한, 초과 시 랜덤 카드 폴백.
  - `app/api/cats/generate-card/route.ts:108`, `lib/rate-limit.ts`
- **[dev] 인메모리 rate limit 다중인스턴스 무력화** — search·funnel·chat·push·notify-inquiry. 남용 방어 중요 경로를 DB 카운터/Postgres RPC로 이전(zone-report가 정답 패턴).
- **[dev] cron/admin 시크릿 비교 `timingSafeEqual` 아님** — 저위험이나 위생. 상수시간 비교로.
- **[악마의변호인] 배포 게이트·회귀 테스트 부재** — GPS 무신고 불변식이 "테스트"가 아니라 `lib/geo.ts` **주석**. 누가 리팩터로 방문 텔레메트리에 좌표 한 줄 얹으면 에러 없이 미신고 LBS 운영. → 좌표가 어떤 요청 body에도 안 실림을 CI로 강제.

## ⏸️ 결제 오픈 전 필수 (지금 봉인 — PAYMENT_ENABLED=false)
- **[dev·잠복 High] 환불정책 결함 4건** — 가상상품 기한우회 즉시환불(H-1)·발송후 상품편취(H-2)·update 0행 성공간주 이중복원(M-1)·셀프환불 멱등키 재발급(M-2). `lib/refund-policy.ts`, `lib/refund-executor.ts`. **통신판매업 신고하고 결제 켜기 전 반드시 수정.** 현재 언론노출과는 무관.

## 🟢 잔여 위생 (Low)
- nickname `lower()` unique 인덱스 부재(도용 근본차단 미비) / `profiles_public` 뷰는 definer라 향후 컬럼 추가 시 base 락다운 무시 주의 / `.env.local` anon 키 끝 리터럴 `\n` / 서버 에러 message 클라 노출 소수(admin·cron 한정).

---

## 노출 전 딱 하나만 검증한다면 (악마의변호인)
**외부 채널 특허 표기.** 기술 리스크는 고쳐도, 특허 허위표시는 "이미 찍힌 스크린샷"을 되돌릴 수 없다. 그 다음이 zone_reports anon 프로브.

## 이 감사가 성립하려면 참이어야 할 조건 3
1. 모든 **외부 노출 채널** 특허 문구가 "출원 중"으로 통일(앱 코드가 깨끗한 것과 무관).
2. profiles·zone_reports 락다운 SQL이 **프로덕션에 실제 적용**되고 anon REST로 실측됨("파일에 있음"은 이미 두 번 배신).
3. GPS 무신고 불변식이 **주석이 아니라 테스트**로 강제됨.
