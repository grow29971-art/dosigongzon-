# 2026-05-20 개발일지

D-5 (5/25 정식 출시). 짧은 새벽 세션 — 보안 마무리 + 카운트다운 정정. 3 커밋 + DB 마이그레이션 1건.

## 한 일

### 1. cats·comments·messages 서버사이드 입력 검증 trigger (4fb8283, 전일 작업 적용)
**⚠️ 보안 약점 #5 마무리**. 클라이언트 우회 공격(공격자가 Supabase REST API로 직접 insert) 대비.

`box/supabase_cats_server_validation_migration.sql` Supabase 적용 완료:
- `cats_validate_text_input()` BEFORE INSERT/UPDATE trigger — name + description 검사
- 5개 테이블에 적용: `cats`, `cat_comments`, `post_comments`, `direct_messages`, `circle_messages`
- 차단 패턴:
  - zero-width·invisible 문자 정규화 (검사 우회 방어)
  - 전화번호 (010-/02-/031- 등)
  - 주민번호 형태
  - 차량번호 (12가 3456)
  - N번 출구
  - 동·호수
  - 동물 살해 위협 표현
  - 독극물 사용 의도

세밀한 패턴은 여전히 클라이언트가 1차 가드. DB trigger는 명백한 케이스만 backstop.

### 2. auth-error-logs 봇넷 폭격 방어 강화 (89b0f36, 01:51)
**⚠️ 보안 약점 #7**. 기존 IP당 20/분 제한만으로는 봇넷 IP 회전 공격 시 `auth_error_logs` 테이블 누적 무한.

수정:
- IP당 20→**5/분** 강화 (정상 사용자 OAuth retry는 5회로 충분)
- 전역 **100/분** 캡 추가 — IP 회전 봇넷도 자동 차단
- `app/api/auth/log-error/route.ts`

### 3. PostgREST 메타문자 escape (64217e9, 전일 작업)
**⚠️ 보안 약점 #8**. `escapeForIlike`이 `,()` 미처리로 검색 쿼리 abuse 가능했음.

`app/api/search/route.ts`: `,()` 공백 치환 후 `% _ \\` literal escape.

### 4. LaunchCountdown 날짜 정정 + 카피 단순화 (96c0665·6b684f0, 02:00~02:16)
사용자 확인: 정식 출시일은 5/28이 아니라 **5/25**. 이전엔 잘못 잡혀 있었음.

- `LaunchCountdown.tsx` `LAUNCH_DATE` 2026-05-28 → **2026-05-25**
- 메모리 `project_dosigongzon_beta_deadline.md` 갱신 (출시 D-day 명시)
- 카피 "도시공존 신청 · 앱 출시 D-N" → "도시공존 정식 출시 D-N" (신청은 이미 시작됨, 표현 단순화)
- D-3 이하 isUrgent 모드에서 "🔥" 이모지 prefix 추가
- **/signup 페이지 최상단**에도 LaunchCountdown 노출 — 가입 직전 긴급감

## 그래서 어떻게 됐나

- **보안 ⚠️ 약점 3건 마감** — 서버사이드 검증, 봇넷 방어, 검색 escape. 자체 감사 발견 10건 중 7건(🚨 4 + ⚠️ 3) 정리 완료. 남은 ⚠️ #6·#9·#10은 마감 후 처리.
- **D-day 정정** — 카운트다운 잘못된 날짜로 운영되던 거 사용자 발견·즉시 수정. D-3부터 빨강 펄스 자동 작동.
- **가입 마지막 push** — signup 페이지에도 카운트다운 노출, D-5 임박감 어필.

## 미해결·다음 액션
- ⚠️ #6 rate-limit TOCTOU race (multi-tab 동시 요청 backstop) — 마감 후 처리
- ⚠️ #9 cat-photos URL 추측 가능성 (UUID + timestamp 조합 강화) — 마감 후
- ⚠️ #10 push/send PostgREST .or() 필터 인자 검증 — 마감 후
- Phase 2 v1.2 — 다중 서클·세분화·통계 private 제외 옵션
- Meta Pixel CAPI 서버사이드 전환 추적
- 위치정보사업 LBS 신고 검토 (변호사 1시간 상담)
- TWA 프로덕션 승급 (Play Console)

## 참고
- 3 커밋 + DB 마이그레이션 1건 (server-side validation trigger) Supabase 적용
- 빌드·타입체크 모든 커밋에서 통과
- 메모리 업데이트: `project_dosigongzon_beta_deadline.md` (5/25 정식 출시 D-day)
- 자동 배포 모드 ([[feedback-auto-deploy]]) 적용 — 코드 변경 + 타입 통과 시 즉시 commit·push·vercel
