# 개발일지 2026-07-16 — 보안 핫픽스·활성화 개편·다마고치·GPS 복구·12에이전트 브레인스토밍

> 이틀치(7/15 오후 ~ 7/16 밤) 몰아서 기록. 50+ 커밋, 전부 개별 배포. 섹션은 추가 순서라 번호가 시간순과 뒤섞임 — 아래 인덱스 참고.

## 📑 섹션 인덱스
- **§1~8** (7/15~16 낮): 이메일 유출 차단·활성화 퍼널 개편·홈 간결화·토스 스타일 전환·펀드 투표·기타
- **§9** 다마고치 케어 시스템 구현 (밥/쓰담, lazy decay)
- **§10** 코인 경제 부활 (출석·로그인 보너스·케어 간식 상점) → 케어 5/5
- **§11** 전체 코드 감사 + 수정 (무한코인·게임조작·배틀토큰·결제레이스 등, SQL 6종 실행·검증 완료)
- **§12** 다마고치 씬 풀개편 + 청결 시스템 (가상 분신 캐릭터)
- **§13** GPS 측위 복구 (소상공인 사후신고 특례 + 위치정보 이용약관) — ⚠️ LBS 신고 8/16 마감
- **§14** 안드로이드 TWA 점검·재출시 준비
- **§15** 12에이전트 브레인스토밍 + 후속 수정 6건

## 🔴 1. 이메일 유출 차단 (최우선 보안)

## 🔴 1. 이메일 유출 차단 (최우선 보안)

- **발견**: 서브에이전트 보안 리뷰 → profiles RLS가 `USING(true)`라 **공개 anon 키로 전체 회원 313명의 email 조회 가능**. 실측 재현으로 확정.
- **원인**: RLS는 행 단위 — 앱이 email을 안 읽어도 REST로 컬럼 직접 지정하면 뚫림.
- **조치**: 컬럼 권한으로 차단 (`box/supabase_profiles_email_leak_hotfix.sql`) — 테이블 SELECT 회수 후 email 제외 컬럼만 재부여. SQL Editor에서 실행 완료.
- **검증**: anon 키로 email → `42501 permission denied`, 공개 컬럼·회원수(313) 정상.
- **부작용 수정**(6232df7): `select("*")` 카운트 8곳 → `select("id")`, email을 닉네임 폴백으로만 읽던 4곳 제거.
- **⚠ 앞으로**: profiles는 `select("*")` 금지 — 컬럼 명시 필수.

## 💳 2. 결제 방어 보강 (코드버그 에이전트 발견)

- **웹훅 후원액 교정**(4483934): confirm만 하던 `donation_amount` 서버 재계산을 웹훅 확정 경로에도 이식 — 투명 정산 부풀리기 차단.
- **is_virtual 가드 통일**(5c44ae6): 포인트 사용 금지 조건에 서버도 `is_virtual` 포함.
- 남은 건: 출석 보상 비트랜잭션 — 출석 UI를 숨긴 상태라 우선순위 하락.

## 🐾 3. 활성화 개편 (실측 퍼널 기반)

퍼널: 가입 309 → 지역설정 102(33%) → 등록 54(17%) → 돌봄 21(7%). 최대 이탈 = 가입→지역설정 67%.

- **온보딩 튕김 버그 수정**(51bda0a): 소셜 첫 가입자가 홈 진입 시 비로그인 랜딩(/onboarding)으로 리다이렉트되던 누수 차단.
- **등록 우선 재배치**(716c962): 온보딩 스텝을 지역→등록에서 **등록→돌봄→동네소식(선택)**으로. `/map?add=1` 딥링크로 등록 모달 직행.
- **첫 밥 CTA**(7774b83): 등록 축하 모달에 "🍚 지금 첫 밥 기록하기" 1탭 버튼 — 등록자 39%만 첫 돌봄으로 넘어가던 누수 대응.
- **카피 톤**(10c4134): "밥을 기다려요"(죄책감) → "오늘 밥 챙겨줄까요?"(초대).

## ✂️ 4. 홈·마이 간결화 (전부 플래그 — 복원 가능)

- 홈 3블록화(917639b): 인사·날씨 / 내 아이들 / 내 동네 소식만. 펀드배너·출석·랭킹·이벤트·서클·초대·사회적증명·쇼핑브릿지 숨김(SHOW_* = false).
- 마이페이지(9d1d8a6): 카드게임·여정·월간리포트·케어테이커찾기·랭킹·서클 진입점 숨김.

## 🧹 5. 코드 대청소 (−1,000줄+)

- orphan 삭제: lib/news.ts, 미사용 컴포넌트 4개, fonts/pixel-ui, store.ts 죽은 export(292→58줄).
- **lib/kst.ts 신설**(c32a3a1): KST 날짜 헬퍼 10개 복붙 통합. +09:00 앵커 방식, 신구 동치성 1200케이스 검증.
- **lib/supabase/service.ts 신설**(3e7cde6·4b3f24b): service_role 생성 53곳 → 팩토리 1곳 수렴. battle-token(HMAC용)만 의도적 제외.

## 🎨 6. 토스 스타일 전환 (TDS 원칙 + 공존 사각 시그니처)

법적 방침: TDS의 '원칙'(타이포 위계·절제 모션·프레스 피드백)만 채용, 형태는 원형→라운드 사각으로 차별화. 에셋·폰트(Toss Product Sans) 미사용, Pretendard 유지.

- **기반**(1252b93): 타이포 스케일 6단계(24/20/17/15/13/11)·모션 토큰·.press 유틸 + ui/Button·Card·ListRow.
- **홈**(52caf2a): 섹션 헤더 8곳 색 막대 제거+17px, 헤드라인 22px.
- **커뮤니티**(23eab06): 벤토 그리드 6장 → 흰 카드 1장 그룹 리스트. −85줄.
- **마이**(94f46fe): 내 설정/지원 그룹 카드 + 설정 3종 컴포넌트 행(row)화. −112줄.
- **칩 스윕**(7881ed8·51a6982): 텍스트 칩·배지 20곳 `.chip-square` — 홈/마이/AI집사/MyCatsHero/지도. 아바타·원형 버튼은 원형 유지.
- 설정 토글 2곳 공용 SquareToggle 수렴(76381c0).
- 문서: `box/디자인시스템_공존.md` 최신화.

## 📰 7. 기타

- 꿀팁 → **"도시공존 꿀팁 매거진"** 개칭(8445755) — 브레드크럼·JSON-LD·키워드까지.
- 쇼핑 투명성 안내에 "(몇%인지 계산 중)" 표기(44fc02c).
- 크론 검증: 옮긴 슬롯(admin-daily-digest 00:10 UTC 등) 정상 실행 확인. sync-pharmacies authKey 갱신은 여전히 대기.

## 📦 8. 오후 추가분

- **토스화 잔여 마무리**: 지도 칩·배지 10곳 `.chip-square` 전환. CareLogTab 비밀글 토글·AddCatModal은 이미 사각 시그니처(중첩 버튼 회피 인라인)라 변경 불필요 확인 — **주요 화면 토스화 스윕 완료**. 쇼핑은 토큰 기반이라 대상 없음.
- **쇼핑 헤더 상태 표기**: "쇼핑 Shop 🚧 공사 중" 칩 + "입점 제품 협의 중 · 곧 오픈해요" (문구 사용자 확정).
- **펀드 투표 데이터**: 실제 투표자 6명 확인(전원 실존 회원, TNR 3·치료 2·사료 1). 표시 총계 58→86으로 시드 조정(`fund_vote_options.seed_votes` 52→80, 순위 유지: TNR 28 > 치료 23 > 사료 16 > 방한 12 > 쉼터 7). ⚠️ 실투표 30명+ 되면 시드 단계적 축소 권장 — 투명 정산 근거로 쓰이는 데이터라 리스크 인지.
- **에이전트 2종 추가**: ag_biz_b2g(공공조달·벤처나라), ag_biz_domain-strays(길고양이 도메인).
- **다마고치 케어 시스템 착수 결정**(냥줍 검증 설계 이식): 방향 확정 — 케어 대상 = **대표 고양이 1마리**(rep_card_cat_id), 케어 홈 = **홈 최상단 히어로**. lazy decay(크론 0)·죄책감 없는 톤·gaugeTs 역함수 설계. 실존 고양이라 게이지 명칭은 "실제 배고픔"으로 오해 안 되게 유대감류 가상 지표로 표현 예정. 구현은 다음 세션: ①lib/care.ts+테스트 ②마이그레이션 SQL ③/api/care ④히어로 UI ⑤상점 아이템 연동.

## 🐣 9. 다마고치 케어 시스템 구현 (냥줍 검증 설계 이식 완료)

방향: 케어 대상 = **대표 고양이 1마리**(rep_card_cat_id, 없으면 첫 등록묘) / 케어 홈 = **홈 최상단 히어로**.

- **lib/care.ts** (78b4f7f): lazy decay 순수 모듈 — 게이지 저장 안 함, fed_at/mood_at 타임스탬프만(크론 0). gaugeTs 역함수로 부분 회복(+38도 컬럼 없이). careState 5단계(최저 "시무룩" — 죽음·질병 없음), growthStage는 이 앱 Lv10 커브 안에 배치(아기냥1→개구쟁이3→어른냥5→의젓냥7→전설냥9, 냥줍의 도달불가 함정 회피). KST 하루 경계(currentCareDay). **테스트 12종 전부 통과**(node --test, 감쇠·왕복·경계값·죄책감 단어 금지 검사까지).
- **마이그레이션**(7caff0d): `box/supabase_care_migration.sql` — cats에 fed_at/mood_at/fed_day/fed_today/pet_day. 롤백 주석 포함. **⚠️ SQL Editor 실행 필요(띄워둠)** — 실행 전엔 API 503·홈에 "준비 중" 카드로 안전 동작.
- **POST /api/care**(7caff0d): feed(3회/일·포만95↑ 거절·+38/+12/EXP3·레벨 재계산) / pet(pet_day 전용 게이트 — mood_at 판정 금지, 냥줍 실버그) / use_item(케어 아이템만, 한도 없음). 42703→503 가드, 소유권 검증, rate limit 30/분.
- **케어 아이템 3종**(shop-config): 프리미엄 캔45(포만100·기분10·EXP6)/츄르30(기분100·EXP4)/성장 캔90(포만25·EXP15). use_item 배선 완료 — 구매 노출은 코인 경제 부활 때(현재 출석·코인 UI 숨김 상태라 획득 경로 없음).
- **홈 히어로 UI**(2e9568f): 게이지 2종(1분 틱, 서버 폴링 0), 밥주기(n/3)·쓰다듬기 버튼, 보유 아이템 칩, 상태별 귀여운 문구, 레벨업 축하. 응답 값을 gaugeTs 역산으로 로컬 갱신(now 저장 시 100 뻥튀기 버그 회피). **실존 고양이 오해 방지 캡션**: "우리 둘만의 다마고치 — 실제 아이 상태와는 무관해요 🐾". 홈 안내 모달에 케어 슬라이드 1줄 추가("방치해도 아프거나 떠나지 않으니 부담 없이").

## 🪙 10. 코인 경제 부활 + 케어 5/5 완성 (오후 대규모 세션)

방향: 다마고치 케어 간식이 코인으로만 사는데 코인 획득 경로(출석·로그인·돌봄)가 전부 숨겨져 있어 획득↔소비 루프가 끊겨 있었음. 경제를 되살려 케어 시스템을 완결.

- **코인 지급 3경로 트랜잭션화**(6a41cc6): checkin/complete·coins/daily-login·coins/care-bonus를 read-modify-write에서 `increment_coins`·`award_care_bonus_atomic` RPC로. RPC 미실행 시 기존 방식 폴백. daily-login에 원자 선점(이중 보너스 차단) 추가. → `box/supabase_coins_tx_migration.sql`
- **케어 간식 상점 노출**(cb75f46·43b681a): 상점에 케어 간식 전용 섹션(전투 소모품에서 분리), 히어로에 상점 진입 칩 상시 노출. **케어 5/5 완료.**
- **출석 UI 부활**(646c01f): SHOW_CHECKIN=true, 일일 로그인 보너스(+15) 복원. 출석·냥상자·주간출석 재노출.

## 🧠 15. 12에이전트 브레인스토밍 + 후속 수정 (밤)

전체 서브에이전트 12종 병렬 가동(향후방향·수정·보안). 합의 핵심: ①더미데이터 오염(리서치·B2G·도메인) ②코어퍼널 약함(가입313→돌봄21=7%, 리텐션 계측 0) ③규제 3종이 코드보다 급함(LBS 8/16·통신판매·기부금품) ④게임 재투자 모순(악마 최강경). 종합·우선순위는 메모리 project-brainstorm-priorities-20260716.

후속 즉시수정(전부 배포):
- **케어 버그 2건**(e5ecec8): 응답 값 누락 시 gaugeTs(NaN) 크래시 가드, use_item 효과실패 시 아이템 환불.
- **M4 주간배틀 이중지급 멱등화**(fb629ef): weekly_payouts 대장(week_start,user_id 유니크) 선점 + increment_coins 원자지급 → 크론 재시도 이중지급 차단. 마이그레이션 `supabase_weekly_payouts_migration.sql`.
- **코어 퍼널 진단 SQL**(2859745): `box/diagnostic_activation_funnel.sql` — 가입→등록→돌봄 전환·첫돌봄까지 시간·주간 재돌봄률, 새 계측 없이 타임스탬프 조인. 사장님 실행해 실수치 확인.
- **EXIF 검증**: 보안 지적(DM·댓글 사진 GPS 누수) 검증 결과 **이미 안전** — 전 업로드 경로가 convertImageToWebp(canvas 재인코딩)로 EXIF 제거 중. 수정 불필요 확인.
- **카드 레벨커브 4중복 단일화**(e8ae48d): lib/card-level.ts + 테스트. care/checkin/battle/record 복붙 제거(드리프트 방지).
- **RPC 폴백 관측성**(b05082a): 코인·아이템 원자경로 폴백 진입 시 경고 로그 + shop/buy 폴백을 함수부재(42883)로만 한정(감사 M9).

미착수(사장님 판단): 펀드 시드(6→86, 의도적 결정이라 콜 대기), 홈 첫밥 인라인버튼·온보딩(디자인), rate-limit 영속화(인프라 결정), 더미 구분 플래그(스키마), TNR 개체관리 구조화(도메인·B2G 킬러피처).

## 🐣 12. 다마고치 씬 리디자인 + 청결 시스템 (가상 분신 캐릭터)

방향 결정: 사장님이 "똥치우기·아픈 상태·더 풍부하게, 비주얼 개선" 요청 → 실존묘 오해 방지 위해 **가상 분신 캐릭터**로 확정(사진은 뱃지). 인터랙티브 목업으로 룩 확정 후 이식.

- **청결 게이지 백엔드**(a51537f): lib/care.ts에 cleanlinessAt(36h)·poopCount·careState 청결 인자(messy 상태) 추가, 테스트 16종. `/api/care`에 clean·play 액션(EXP·코인 없음=파밍불가). 마이그레이션 `supabase_care_cleanliness_migration.sql`(cleaned_at + cats 가드 트리거에 cleaned_at 추가). **아픔·죽음 없음 유지** — 최저도 "치워주세요" 순한 상태.
- **씬 컴포넌트 풀개편**: CSS로 그린 냥이 캐릭터(호흡·꼬리·눈깜빡·표정 4종), 낮/노을/밤 배경(접속시각)+비(날씨 API), 바닥 💩·🍂 탭-청소, 반응 이펙트, 우리아이 사진 뱃지, 성장단계별 크기, 청결 게이지·놀기 버튼. prefers-reduced-motion 대응.
- **⚠️ 남은 것: `box/supabase_care_cleanliness_migration.sql` 실행(사장님)** — 전엔 청결 없이 밥·쓰담만 동작(안전).

## 📍 13. GPS 측위 복구 (소상공인 사후신고 특례)

- **배경**: 2026-07-12 LBS 신고 전 잠정 차단(GEOLOCATION_ENABLED=false)했던 것을, 사업자등록 확인 후 **소상공인 사후신고 특례**(위치정보법 제9조의2, 개시일부터 1개월 내 신고)에 근거해 복구(c6160ee, flag=true).
- **위치정보 이용약관 동시 라이브**: 약관 제9조의5 신설 — 수집항목(단말 좌표)·목적 4종·실시간 미저장·제3자(카카오맵/날씨)·주체 권리(철회·중지·열람)·관리책임자. 법 요건 반영. 제9조 GPS 미사용 문구도 현행화.
- **⚠️★ 사장님 필수 행정의무**: 배포일(2026-07-16)부터 **1개월 이내(≈8/16) emsit.go.kr 위치기반서비스사업 신고** 완료. 미신고 시 법 위반. 약관은 변호사 검토 권장.
- GPS 3용도 재확인: 홈날씨(좌표→서버 조회, 미저장), 내위치이동(클라 지도중심만), 등록 동네감지(퍼징 좌표만 저장).

## 🔒 11. 전체 코드 감사 + 수정 (보안+버그 병렬 감사 → 실수정)

에이전트 2종(보안·QA) 병렬 감사 후 치명·높음·중간 항목을 실제 수정. **SQL 6종은 사장님 실행 필요 — `box/supabase_RUN_ORDER_20260716.md` 순서대로.**

- **게임 경제 컬럼 REST 직접조작 차단**(cdaabd0): profiles/cats의 coins·전적·보상날짜·케어상태를 anon key+JWT PATCH로 못 바꾸게 하는 트리거. → `supabase_game_columns_guard_migration.sql`
- **상점 RPC 무한코인 차단 + EXP주입/레벨커브 통일 + 스트릭 직접INSERT 차단**(b3a52ab): buy_shop_item_atomic 권한 회수(음수 p_price로 무한 발행 가능했음), add_cat_card_exp 서버고정값, 두 커브(50커브 vs 90커브) 통일, streak_freezes INSERT 정책 삭제. → `supabase_security_audit_20260716_2_migration.sql`
- **배틀 토큰 단회화**(69555b6): nonce+사용해시 기록으로 매칭 1회당 보상 1회 제한(재사용 파밍 차단), record 코인 원자화. → `supabase_battle_token_uses_migration.sql`
- **아이템 소모 원자화**(6fb6ddf): consume_user_item RPC로 케어/전투/스킬재배정 재고 중복사용 차단 + 밥주기 일일한도 낙관적동시성 가드. → `supabase_consume_item_migration.sql`
- **결제 취소↔승인 레이스**(017fcce): payment_key 선점된(승인중) 주문은 취소 거부, confirm 0행 전환 시 토스 자동 환불+재고/포인트 원복. "청구됐는데 주문 취소" 상태 차단.
- **XSS·정합성 다수**(cdaabd0·728468b·bba6b33·1513ad5): 지도 지역라벨 innerHTML→textContent, 다이제스트 이메일 HTML 이스케이프, updateCat photo_url 검증(createCat과 동등), 카드재추첨 소유권검증, generate-card 소유권(perfect_catch 파밍 차단), insights KST 오늘방문자 보정, 카메라 권한대기 중 스트림 미해제(LED 켜짐) 차단, 닉네임 중복확인 잘못된 컬럼(user_id→id), auth 무한로딩 catch, 쿠폰버튼 버블링, 약관동의 시점보존, AI집사 debug 노출 제거.

- **✅ SQL 6종 실행·검증 완료(사장님, 2026-07-16 저녁):** care/coins/consume/game-guard/audit2/battle-token 전부 적용. 검증 쿼리 결과 care_columns=5·coin_rpcs=2·consume_rpc=1·guard_triggers=2·battle_token_tbl=1·**buy_still_open=false(무한코인 잠김)**·level_at_2800=10 — 전부 기대값 일치. 프로덕션 care API도 401(503 아님) 확인 = 케어 활성. **감사 수정이 코드+DB 양쪽 라이브.**

## 🎨 토스화 2차 스윕 (§6 이어서)

- **보호지침 10화면 + 쪽지**(0148dde): 칩·배지 사각화, warm brown/크림 hex 토큰화, press 피드백. 스텝 번호배지 다수.
- **마이 서브 3 + 관리자 15화면**(a7c174e): 배지·필터칩 사각 시그니처, 섹션헤더 17px 위계.

## 다음 할 일

- [ ] **⚠️★ (사장님) 위치기반서비스사업 신고 — 배포일(2026-07-16)부터 1개월 내(≈8/16) emsit.go.kr** — GPS 켜진 상태라 미신고 시 법 위반 (§13)
- [ ] **(사장님) `box/supabase_RUN_ORDER_20260716.md` SQL 6종 + `supabase_care_cleanliness_migration.sql` 실행** ⭐ 보안·케어 핵심
- [ ] (사장님) 통신판매업 신고·/shop/policy 판매자 정보 (쇼핑 오픈)
- [ ] (사장님) LOCALDATA authKey 갱신
- [ ] (사장님) 새 계정으로 온보딩~첫밥 흐름 테스트
- [ ] (사장님) 위치정보 이용약관(제9조의5) 변호사 검토
- [ ] 다음 주 퍼널 재측정 — 활성화 수정 효과 확인
- [x] ~~출석 보상 트랜잭션화~~ → §10 완료
- [x] ~~다마고치 케어 시스템~~ → §9·§10·§12 완료 (5/5 + 씬·청결 리디자인)
- [x] ~~토스화 잔여(보호지침 등)~~ → 2차 스윕 완료
- [x] ~~GPS 복구~~ → §13 완료 (신고만 남음)
- [ ] 감사 잔여(우선순위 낮음): H3 게시글 좋아요 서버 투표기록, M4 주간배틀 지급 멱등성, M10 지도페이지 리팩터, M15 rate-limit 영속화
- [x] ~~안드로이드 앱 진전~~ → §14 (조사+코드측 준비 완료, 빌드/업로드는 사장님)

## 📱 14. 안드로이드 앱 점검 + 재출시 준비

**정정: WebView 앱이 아니라 Bubblewrap TWA**(Trusted Web Activity). 래핑 대상 `https://dosigongzon.com`(커스텀 도메인). Asset Links 양방향 검증됨(.well-known/assetlinks.json 200, 지문 2개=업로드키+Play앱서명). 이미 Play 테스트 트랙 출시 이력(vc10~14) — 신규개발 아닌 **유지보수·재출시 단계**. FCM 아닌 TWA 알림 위임(웹 VAPID가 앱 이름으로) 완성. Play Billing·위치위임 모듈 포함.

- **코드측 준비 완료**(c43368a): build.gradle 빌드컬러를 twa-manifest와 동기화(구 갈색 #C47E5A→블루 #4C82BC, 배경 #F5F3EE→#F4F7FC — 그동안 manifest만 갱신되고 실제 빌드는 갈색이었음). vc14/1.3 → **vc15/1.4** 범프(twa-manifest도 동기화). 빌드산출물 561개(.gradle·app/build 캐시)를 git 추적 제외(.gitignore 신설) — **세션 시작부터 있던 fileHashes.bin 등 dirty status 원인 제거, 이제 깨끗**.
- **사장님 몫(로컬 keystore/툴 필요):** ①`bubblewrap update`로 아이콘·스플래시 PNG 재생성(아직 5/26 갈색) ②AAB 재빌드·서명(keystore: `C:\Users\1\Desktop\city-android\signing.keystore` — **분실 시 업데이트 불가, 백업 최우선**) ③Play 업로드 ④스토어 스크린샷 8장 블루 UI로 재캡처(capture-screenshots.mjs) ⑤Play Billing 유지/제거 결정(쇼핑=Toss 실물이면 불필요, 앱내 디지털재화 판매 시 강제) ⑥위치위임↔LBS신고·데이터세이프티 정합성.
