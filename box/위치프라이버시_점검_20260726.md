# (중복 — 삭제 예정)

정본: `box/위치프라이버시_권한모델_제안_20260726.md` (같은 날 병렬 작성으로 생긴 중복).

이 파일에만 있던 보충 포인트 2가지는 정본 검토 시 함께 볼 것:

1. **저장 오프셋의 실행 위치는 브라우저다** — `applyLocationOffset()`은 `lib/cats-repo.ts`
   (클라이언트 실행)에 있어, 주석의 "서버측 단일 방어선"과 실제 실행 위치가 다르다.
   정상 유저 경로에선 항상 적용되지만, 방어선 문서화가 실제와 어긋난 상태.
2. **오프셋 재추첨 수렴 리스크** — 좌표 수정이 반복되면 오프셋이 매번 재추첨되어
   이론상 표본 평균이 실위치로 수렴한다. 좌표가 ±30m 이내로만 바뀐 수정에는
   기존 저장값을 유지하는 가드를 Phase 2 후보로 검토.
