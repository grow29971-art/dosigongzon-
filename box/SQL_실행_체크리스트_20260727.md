# SQL 실행 체크리스트 (2026-07-27) — 사장님이 SQL Editor에서 한 번에

> **실행 전 공통 안전 절차 (매번):**
> 1. Supabase Dashboard 로그인
> 2. **좌상단 프로젝트가 CITY(`sozxbnvgsougkliibnxl`)인지 URL/프로젝트명으로 눈 확인** ← 가장 중요
> 3. SQL Editor에 해당 파일 전문 붙여넣기 → **Run**
> 4. 결과(성공/rows/에러) 확인 후 아래 체크박스 표시
>
> 모든 마이그레이션은 멱등(재실행 안전)하게 작성됨. `drop table`/`delete` 등 데이터 파괴 구문 없음.
> 애매하면 실행하지 말고 사장님이 킹콩에게 물어볼 것.

---

## 실행 순서 (위→아래)

### ① (선택·읽기전용) 북극성 실측 — 가장 먼저 해도 무해
- [ ] `box/diagnostic_core_journey_baseline.sql` (185줄, **SELECT only, 스키마 변경 없음**)
- 용도: WAU·주간 케어기록(북극성)·Day0 활성화율·퍼널·푸시옵트인 7개 지표 1회 산출
- 실행 후: 결과값을 `개발일지_20260726_핵심여정개편.md` §4-3 표에 기입
- **회의 3팀 만장일치 "진짜 P0". 이거 없이는 어떤 개선도 효과 측정 불가.**

### ② 실험 인프라 마이그레이션 (미실행 확인됨)
- [ ] `box/supabase_experiment_migration.sql` (5테이블 + RLS + 롤백)
- 근거: 개발일지_20260726.md — "**사장님 SQL Editor 실행 필요**, 실행 전엔 /experiment API가 500"
- 실행 안 하면 실험/AB 기능이 500 에러

### ③ P3 돌봄교대 마이그레이션 (61회 다뤘으나 DB 미적용)
- [ ] `box/supabase_care_shifts_migration.sql` (201줄, 테이블+트리거+RLS, `begin`/`commit` 트랜잭션 래핑)
- 근거: P3-4~P3-62 내내 "운영 미실행(not_ready)" 상태. `caretaker_circles` 테이블 선존재 필요.
- **이게 P3 완료의 유일한 게이트.** 실행 후 flag `NEXT_PUBLIC_FF_CJ_CARE_SHIFT` 켜고 스모크 테스트.

### ④ (검토 후) 미적용 보안 하드닝 — 실행 전 킹콩과 확인
- [ ] `box/supabase_economy_rpc_lockdown_migration.sql` — 개발일지_보안패치.md에 "운영 미적용" 표기
- [ ] `box/supabase_ai_chat_rate_migration.sql` — 개발일지_보안강화.md에 "실행 대기" 표기 (service_role 전용, 멱등)
- ⚠️ 이 둘은 RPC 권한·rate limit을 잠그는 거라 **실행 순서·부작용 검토 후** 진행 권장. 지금 당장 아니어도 됨.

---

## 실행 후 킹콩에게 알려줄 것
각 SQL 실행 결과(성공 / rows 수 / 에러 메시지)를 알려주시면:
- ①의 지표값 → §4-3 표 기입 + 회의 결론 재조정
- ③ 성공 → VS Code Claude에게 P3 스모크 테스트 지시
- 에러 발생 → 원인 진단

_작성: 킹콩. 근거: box 폴더 SQL 스캔 + 개발일지 4종 교차 확인 (2026-07-27)._
