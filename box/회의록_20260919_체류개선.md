# 회의록 2026-09-19 — "유저가 앱에 체류하게" 9관점 + 악마의 변호인

소집: 사장님 지시("서브에이전트 총가동해서 개선점"). 참여: 그로스·행동심리·인지심리·한국심리·디자이너·현장도메인·리서치·BM·인프라 + 악마.
모든 항목은 코드 파일:줄 또는 Supabase service 프로브(읽기)로 실측. 이 파일은 기록이며 실행은 사장님 승인 후.

## 0. 실측 (2026-09-19)

- 30일: 가입 199(9/2 하루 143), 케어 25건·13명(상위 1명 11건), 고양이 등록 24, 커뮤니티 유저 글 1/봇 28, 퍼널 intro 702→pick 14→first_feed 1.
- 재방문: 가입 다음날 23/199(12%), Day7+ 17(9%). 뭐라도 쓴 유저 합집합 23/583.
- 도달 채널: 푸시 실청중(동의∩구독) 8명(비관리자 6), 케어 13명 중 도달 2명. 운영자 DM 261건→답장 0(7/17~). 이메일 다이제스트 52명 전원 4~5월 가입, **신규 199명 중 0**.
- 8/29 "인터뷰 전 3커밋 초과 금지" 이후 실제 커밋 **140개**, 인터뷰 0/3.

## 1. 발견된 결함 (버그 — 기능 변경 아님)

| # | 위치 | 내용 |
|---|---|---|
| B1 | `app/components/FirstFeedPushPrompt.tsx:72,79,80` | 전체 흰 글씨. 9/16 리디자인으로 부모(`FirstFeedBar.tsx:99`, `PendingCareHandoff.tsx:127`) 배경이 흰색 → 첫밥 직후 푸시 옵트인(7/24 P0-3)이 안 보임 |
| B2 | `app/components/CareLogCelebration.tsx:118` | "저녁에 리마인더를 보내드릴게요" — `care-commit-date` 쓰기만 있고 읽는 코드 0. 거짓 약속 |
| B3 | `lib/weekly-digest.ts:223,241` + `box/supabase_email_optin_migration.sql:12` | 본문에 "(광고)" 표기 = 스스로 광고성 정보 선언. 수신 52명은 동의 기록 없는 default-true("동의한 것으로 간주"). **무동의 광고 발송 상태**(망법 50조) |
| B4 | `app/api/auth/callback/route.ts:139` | `email_opt_in` 메타를 세우는 UI가 리포에 없음 → 신규 이메일 동의 구조적 0 |
| B5 | `app/welcome/page.tsx:149` | `canPromptPush()` false(카톡 인앱·iOS 미설치)면 알림 스텝 무음 스킵 → 아무 동의도 못 받음 |
| B6 | `app/api/cron/care-cue/route.ts` | 쿨다운 없음 — 같은 5명에게 30일 연속 매일 발송(이들 케어 0~3건) |
| B7 | `profiles.signup_source` 0/584 null | `SourceCapture`→`syncSignupSourceOnce` 배선 사망. 9/2 143명 kakao 가입인데 signup_view 14 → /signup 안 거친 진입 경로 |
| B8 | `app/welcome/page.tsx:244` | user_metadata.intent 저장만, 읽는 곳 0 |
| B9 | `app/guide/page.tsx:80,180` | 폐지된 "돌봄다이어리 탭"·점수제 안내 잔존. `mypage` 인트로 배너 "레벨·업적·streak·+15점"(:301)도 잔재 |
| B10 | `HomeAuthed.tsx:254`, `MyCatsHero.tsx:57` | 같은 쿼리 복제 — 한쪽만 고치면 헤드라인 숫자 불일치 |
| B11 | 죽은 크론 4종 | onboarding-nudge·like-digest·health-alert·weekly-postcard 30일 발송 0 |
| B12 | 신규 구독 10명 중 동의 6명 누락 | welcome 구독→동의 순차 실행, 실패 경로 미확인(사장님 계정 재현 필요) |

## 2. 구조 진단 (2관점 이상 독립 지목)

- **A. 홈 1탭이 "내가 등록한 고양이"에만** — `MyCatsHero.tsx:57` caretaker_id=me. 남의 고양이에 밥 준 사람(신규 중 등록 15·케어 12)은 홈이 빈 화면. 9/2 코호트 "1건 후 0"과 일치. (디자이너·현장)
  - 악마 조건: 더미(`caretaker_id IS NULL`)를 반드시 제외해야 함(전국 분산 더미에 시험 밥 준 유저 홈에 "아직 N마리"가 매일 뜸). B10 동시 수정. care_logs에 source 없어 "홈에서 눌렀다" 판정 불가 → 판정은 "재방문자 중 케어 2회+" 비율.
- **B. 상세 하단 밥바(FirstFeedBar) "7일·0건" 게이트** → 첫 기록 후 소멸. (디자이너) — 악마 **기각**: `cats/[id]/page.tsx:489` CareLogTab 폼이 이미 상시 노출(9/18). 같은 화면 밥 버튼 2개.
- **C. 첫 세션 과부하** — 비로그인 /map IntroSheet→IntroModal→ChatGuideModal 3연속; 로그인 신규 welcome 4장+알림+의도 → FeatureTour 7장 → PageIntro → AppOpenGuide → 지도 Intro+ChatGuide+코치마크. intro→detail 25%. 비로그인 마커 tier 강제 1(`map/page.tsx:1483`) → 탭하면 임의 1마리(`:1622`), detail→pick 8%. (인지)
- **D. 죄책감 카피** — "아직 N마리"(`MyCatsHero.tsx:127,184`)·"끊길 수 있어요"(`HomeStreakCard.tsx:41`)·기록 0인데 "지켜주셔서 고마워요"(`HomeAuthed.tsx:671`). (한국심리·디자이너)
- **E. 월동 시즌 미반영** — 팁이 체감 ≤5℃에서만(`HomeAuthed.tsx:924`), 10월 겨울집 준비기엔 안 뜸. 확인서(`/mypage/report`, PDF 완성)는 구청 겨울 급식소·TNR 신청 시즌에 걸어야 함. (현장)

## 3. 통계 경고 (리서치)

- 케어 주 4~8건은 포아송 잡음. 2주 판정 가능한 최소 변화 = 2주 합 13→26(2배). 30% 개선은 못 잰다.
- first_feed 1/14 CI 0.2~34%. pick 누적 50 전엔 첫밥 코드 수정 근거 없음.
- 9/2 143명은 코호트 분리. onboarding_intro 702는 봇·크롤러 제외 미확인. DM 261→0은 `read_at`으로 "안 연다/읽고 무시" 분리 필요.

## 4. 판정 (악마 반영)

**전제**: 3주째 인터뷰 0/3이 진짜 병목. 코드로 만든 어떤 것도 "누가 왜 쓰는지" 모른 채로는 판정 불가.

1. **코드 0줄, 먼저** — 9/2 코호트(실명 10명·143명) 주최자/경로 확인 → 인터뷰 3명 확보. 케어 13명은 auth 이메일로 개인 연락(DM 아님). 첫 기록·등록자에게 24h 내 고양이 페이지 공개 댓글/하트(운영 5분/일, Joyce&Kraut 44→56%).
2. **버그 1커밋** — B1 색 토큰, B2 문구 삭제, B3 52명 다이제스트 발송 보류(동의 근거 없음 — 재동의 받기 전까지).
3. **A 더미 제외판 1커밋** — MyCatsHero+HomeAuthed 헤드라인 동시.
4. 인터뷰 후: C 오버레이 정리·마커 강제 해제, D 카피, B4/B5 이메일 동의 UI(사전체크 금지·광고성 동의 문구 법률 확인), B6 care-cue 쿨다운, B11 크론 정리, E 월동 큐, B7 signup_source 수리, B9 잔재 삭제.

**살려면 참이어야 할 조건 3**: ① 9/2 143명이 조직 유입이고 주최자에게 닿는다 ② 케어 13명 중 3명이 인터뷰에 응한다 ③ 140커밋 속도를 멈춘다.

## 5. 기각·보류

- FirstFeedBar 게이트 완화(중복 버튼), 밥주기 2슬롯(리마인더 없이는 무의미), 지명 책임 가짜 문(인터뷰 후), 푸시 카피 전면 수정(청중 8명), 쇼핑·후원 재방문 훅(오픈 전 무변화), 돌봄 교대 P3 플래그 ON(서클 멤버 필수라 켜도 0).

## 6. 후속 실측 (9/19 저녁) — A(MyCatsHero 확장) 착수 후 취소

구현·tsc·테스트까지 마치고 프로브했더니 전제가 틀렸다:
- 30일 케어 25건 중 "남이 등록한 고양이"에 남긴 기록 6건 — **전부 더미(caretaker_id null)**. 더미 제외 조건을 붙이면 헬퍼가 추가하는 고양이 0마리.
- 전체 기간으로도 타인 실묘 기록은 5건뿐.
- 9/2 등록자 7명(영진·루나 등)은 본인 등록이라 홈에 이미 1탭이 떠 있었다. "홈이 비어서 안 돌아왔다"는 이들에겐 성립 안 함.
→ 코드 되돌림(커밋 안 함). 근거 없는 커밋 1개 추가는 140커밋 문제의 반복. 인터뷰 회신에서 "남의 고양이에 밥 준다"가 나오면 재개(구현 스케치: care_logs 30일 cat_id ∪ caretaker_id=me, `.not("caretaker_id","is",null).is("memorial_at",null)`, MyCatsHero·HomeAuthed 헤드라인 동시).

남은 판정 항목: ①인터뷰 회신 대기(10통 발송) ②첫 기록자 24h 내 공개 댓글(사장님 손) — 코드 작업 없음.
