# 개발일지 2026-09-17 — 쇼핑 정리 · 앱인토스 미니앱 착수 · iOS 심사 재제출 · 토스 계약심사 회신

사장님 "쇼핑몰쪽 손봐야할거없나"로 시작해 하루에 외부 채널 셋(앱인토스·App Store·토스페이먼츠)을 전부 대기 상태로 넘겼다.
커밋 25개. 전부 배포·실측 후 기록.

## 1. 쇼핑몰 점검 (오전)

라이브 5화면 + 코드 실측. 콘솔 에러 0, 크론 그린, 부가세 표기 정상.

| 커밋 | 내용 | 검증 |
|---|---|---|
| 3ee0c03b | **배송비 품목당 합산 → 주문당 1회(합포장, 최대값)** — 사장님 "한포장임". computeCartTotal·confirm·webhook 3곳 + `box/supabase_shop_shipping_bundle_20260917.sql`(게스트 RPC) | SQL 실행 후 anon RPC 프로브: 사료 2봉 = 145,000원(합산이면 150,000). 테스트 주문은 service 키로 삭제 |
| cbcdf1f0 | 카테고리 칩을 상품 있는 카테고리만 노출(상품 2개인데 칩 6개가 "준비 중"만 보여주던 것) | 배포 후 칩 3개(전체·사료·굿즈) |
| 906869d3 | 결제 준비 문구 "통신판매업 신고 완료 후" → 사실과 어긋남(푸터에 신고번호 있음) → "결제 시스템 연결 중"으로, 🛒 제거 | — |
| 6cfae1da | business-rules에 배송비 규칙(4곳 동일 식 필수) 기록 | — |

8/20에 "20kg 3개면 택배 1건 불가"로 합산으로 바꿨던 걸 사장님 결정으로 되돌린 것 — 이유는 business-rules에 남김.

## 2. 앱인토스(토스 미니앱) — 오전 "보류" → 오후 "착수" (decisions/0008)

개발자센터 확인: 외부 URL 등록 불가(정적 .ait 번들·토스 호스팅), SSR 금지, **토스 로그인 전용**, **토스페이 전용**,
외부 이동·iframe 금지. 즉 PWA 래핑이 아니라 별도 SPA가 필요. 오전엔 "인터뷰 후 착수"로 기록했으나 사장님이
"올리게 해보자"로 번복 → 당일 구현.

- `city-toss/` (a04d3690) — Vite+React, `@apps-in-toss/web-framework` 3.4.1, appName `dosigongzon`.
  토스 로그인 → 카카오맵 고양이 핀 → 상세 → 돌봄 기록. 위치 권한 없음(decisions/0001 유지). `npm run build` → `dosigongzon.ait` 189KB.
- 브릿지 `app/api/toss/login` + `lib/toss-ait.ts` (ee785d9c) — mTLS generate-token → login-me(userKey) → `toss_identities` 매핑 →
  magiclink token_hash → 클라이언트 verifyOtp. CORS는 next.config `has` 정규식으로 tossmini 4종 오리진.
- SQL `box/supabase_toss_identities_20260917.sql` 실행·프로브(anon 401·service 0행).
- 콘솔 작업(브라우저로 같이): 워크스페이스·앱 등록, mTLS 인증서 발급 → Vercel env 2종, 콘솔 API 키 → `npx ait deploy` 첫 번들 업로드,
  카카오 JS 키 `city-toss` 신설 + tossmini 도메인 4개, 제작자 이름 "도시공존", 앱 정보 재검토 요청(반려 사유 = 아이콘 투명/둥근 모서리 →
  꽉 찬 정사각형 1024 생성, 0bc04f04).
- **막힌 곳**: 토스 로그인·약관 등록 메뉴는 정산 정보 검토(약 2일) 후 열림. 사장님이 정산·상호는 토스 비즈니스에서 직접 입력.

남은 것: 정산 검토 → 토스 로그인 설정(약관 URL·scope 이름/이메일) → 복호화 키·AAD Vercel env → 실로그인 테스트 → 검토 요청.

## 3. iOS — 반려 원인 수정 + Mac 없는 파이프라인 + 심사 재제출

App Store Connect 실측: 7/5 빌드 1.0(24)이 **2.1(a) "Sign in with Apple 후 무한 로딩"** 반려. status.md의 "별도 브랜치" 표기는
오류였고 코드는 main `city-ios/`. 7월엔 Codemagic으로 빌드했고 인증서 개인키는 우리에게 없음.

- b34b46fd — 네이티브 Sign in with Apple 복귀(ASAuthorizationController → identityToken → `lib/native-apple-signin.ts` signInWithIdToken →
  `/api/auth/callback?native=1`). 7/6 카드 커밋이 지운 웹 브릿지 분기 복원. Supabase Apple 제공자에 번들 ID `kr.dosigongzon.app` 추가.
- `.github/workflows/ios-release.yml` (7fc99f5d → d9823153 → af560436 → cca1ee4e): 자동 서명은 기기 0대라 실패 → ASC API로 배포 인증서
  DZ96GBZDQX·프로파일 생성해 Secrets 6종으로 수동 서명 → iOS 18 SDK 거절 → `macos-26`/Xcode 26.6. 5차 실행에서 업로드 성공(빌드 5).
- ASC API로 버전 1.0.1 PATCH·빌드 연결·`reviewSubmissionItems` resolved → Apple 답신 작성 → **심사 재제출(심사 대기 중)**.
  실기기 미검증은 사장님 결정. 서명 원본은 Desktop/애플앱키/github-actions-배포서명/.

## 4. 포니테일 설치 (`claude plugin install ponytail@ponytail`, user 범위, 4.10.0)

## 5. 잔여 findings 2건 + 문서

- 6eeda5f2 — OG 이미지 webp: sharp로 jpeg data URI 변환(고양이·커뮤니티·뉴스). 실측: 고양이 OG 445KB, 사진 영역 픽셀 실색.
- 611933ea — `/tips` sanitizer: Node 22로 올려도 매 요청 "DOMPurify 로드 실패 폴백"이 로그에 찍힘(실측) → `sanitize-html`로 교체,
  jsdom 제거. 배포 후 폴백 0건. XSS 벡터 5종 로컬 검증.
- cb9db062·9d17afed — 쇼핑오픈 체크리스트 현행화, engineering-notes에 오늘 함정 6건.

## 6. 토스페이먼츠 계약심사 회신 (오후, 10/3 취소 시한)

메일 내용 파악 → 필수 질문 10항목 답변 + **결제경로 PPT 15장** 제작(가이드 형식: 표지 가맹점정보→하단정보→환불규정→로그인/비회원→
상품·장바구니·주문서·결제창 호출→결제창·BC 선택·신한 인증창→참고). 캡처는 PowerShell 전체 화면(도메인·PC 시계 포함, 크롬 자동화
배너는 sharp로 제거). 상품 상세에 배송기간 표기 추가(90edb41c, 토스 요건). Gmail 초안 생성 → 첨부 → 사장님 승인 → **전송**.
BC카드 ISP/페이북 팝업은 자동화 환경에서 about:blank로 멈춰 신한 인증창으로 대체하고 PPT에 사유 기재.

## 오늘 알게 된 것 (engineering-notes에 반영)

`.env.local` URL 값 끝 리터럴 `\n` · Bash heredoc 백슬래시 변형(Edit 툴 사용) · SQL 에디터 Chrome 번역 · 앱인토스 아이콘 규격 ·
iOS 자동 서명은 기기 필요 · ASC 반려 재제출은 API PATCH로 활성화 · 브라우저 확장 캡처는 보이는 탭만(Ctrl+N으로 탭 활성화).

## 대기 중 (전부 외부)

Apple 심사(1~3일) · 앱인토스 검토 2건(~2일) · 토스페이먼츠 답변(1~2개월). 사장님 손에 남은 건 없음.
