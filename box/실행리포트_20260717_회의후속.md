# 실행 리포트 — 2026-07-17 12에이전트 회의 후속

> "하지 말 것 빼고 전부 실행" 지시에 따라, 회의에서 나온 실행 항목을 프로덕션 실측·문서화로 처리한 결과.

## ✅ P0 — 긴급 3건 (전부 확인 완료)

### 1. anon 키 "stale" → **오진. 실제는 위생 문제, 장애 아님**
- 원인: `.env.local`의 `NEXT_PUBLIC_SUPABASE_ANON_KEY` 값 끝에 **리터럴 `\n`**이 붙어 있음(`vercel env pull` 산출물). 그 `\n`째로 요청하면 401, trim하면 200.
- 프로덕션 번들에도 `...LPT\n`이 인라인돼 있으나, HTTP 계층이 헤더 후행 개행을 trim → **실제 개행을 붙여 요청해도 200**. 프로덕션 로그인·조회 정상(코어가 몇 달째 사용 중인 것과 일치).
- 같은 `\n` 오염이 여러 env 값에 있음(KAKAO_REST, LOCALDATA, SUPABASE_URL, TURNSTILE 등). **장애는 아니지만 정리 권장**(특히 SUPABASE_URL 끝 `\n`).
- 조치: 실측이 막혔던 건 stale이 아니라 이 `\n` 때문 → 키 회전 불필요.

### 2. 보안 SQL 6종 "미실행" → **오진. 이미 적용됨 (프로덕션 프로브로 확인)**
- anon 키 프로브 4발:
  - profiles coins PATCH(실존 행) → **42501 permission denied** ✅
  - profiles email/coins SELECT → **42501** ✅
  - buy_shop_item_atomic RPC 무인증 → 파라미터 없이 미발견(무인증 불가) ✅
  - (존재하지 않는 id PATCH가 204였던 건 "0행 매칭 성공"일 뿐 — 실존 행으로 재검증하니 차단)
- 결론: 1·2차 감사 SQL 전부 DB 반영됨. 개발일지 7/16 기록이 정확, 메모리의 "실행 대기"가 outdated였음. **보안 마무리 조건 해소.** 메모리 업데이트 완료.

### 2-b. 시크릿 유출 스캔 (추가 확인)
- `git log -- .env*` → **커밋 이력 없음** ✅
- 전체 히스토리 `git grep sb_secret_wf` → **service_role 키 문자열 유출 없음** ✅
- `.env.local`의 `\n` 오염은 7줄(KAKAO_REST, LOCALDATA, ANON, SUPABASE_URL, TURNSTILE_SITE, VAPID 등). 장애 아님, 위생 정리 대상.

### 3. keystore 백업
- `C:\Users\1\Desktop\city-android\signing.keystore` (2804 bytes) → `C:\Users\1\Documents\city-keystore-backup\signing.keystore.20260717.bak` 복사 완료.
- SHA256: `1C9A8EC573DD5EEC354985288DF45528E07E48175B2F4F6F584F80439E1C5D9A`
- ⚠️ **아직 로컬 1곳뿐.** 회의 권고는 "오프라인 2곳(USB+클라우드 암호화 zip)". **사장님이 클라우드/USB 2번째 사본을 직접 만들어야 완결.** (재발급 불가 자산)

## 📊 실측 결과 (읽기전용, 운영자 제외)

### 좀비 퍼널 — **가장 무거운 숫자**
| 구분 | 수 | 비율 |
|---|---|---|
| 전체 가입 | 317 | 100% |
| 활동 1건+ | 67 | 21% |
| **좀비(활동 0)** | **250** | **79%** |
- 활동자 67명 중 **69%가 가입 1시간 내 첫 활동**, 30일 후 첫 활동은 4명뿐.
- → **활성화는 가입 직후 첫 세션이 전부.** 첫날 활동 없으면 91%는 영영 안 옴.

### 코어 유저 해부 — **반복 행동 = care(밥주기)로 특정됨**
- 최근 30일 활동 13명, 7일 6명. 충성 코어(가입 30일+ & 최근 7일 활동) 5명: 순설꽁·아리보리·포근한집사361·신이시여·포근한시민868.
- 5명 중 4명이 care 반복자. 최근 30일 이벤트: **care 22 vs cat 11 vs dm 6 vs post 2**. care_type: feed 9·water 5·health 3·treat 2.
- **결론: 이 앱의 진짜 코어 루프 = 개체별 밥주기(care) 기록.** 커뮤니티 글·게임·DM은 유기적으로 죽어 있음. → 판정 기준 조건 B에 반영.

### 함정
- cats 228 중 **136(60%)이 caretaker_id=null 시드**(부녀회 등). 실유저 고양이 ~90.
- dm 235 중 **229가 운영자 발신**. dm은 기능이 아니라 운영자 방송채널. 유저 발신 6건 전부 순설꽁.

### 크론 신뢰성
- Vercel Hobby "2개 제한"은 **관측 안 됨** — 15개 실제 발화 중(계정이 사실상 Pro급 크론 허용).
- 진짜 문제는 **신뢰성**: 매일 크론 5개(care-cue·health-alert-push·like-digest·admin-daily-digest·payment-reconcile)가 날짜별 산발 결행. digest·reconcile은 00:10/00:20 이동 후에도 7/16 하루 빠짐.
- 확정 유령: **engagement-push** 1개. 주간 크론 5개(일/토)는 계측 후 스케줄 미도래 → **7/18(토)·7/19(일) 재확인 필요**.
- 권고: 21개는 오버엔지니어링. 핵심 5~6개만 Vercel 크론 유지 + 나머지는 디스패처 1개로 통합. payment-reconcile은 실금전 정합이라 결행 내성 필요(단, 현재 결제 하드락이라 즉시 리스크는 낮음).

## 🗂️ box/ SQL 대장화 (부채청산)
- 총 **193개** 분류: 적용 153 · 미적용/대기 2 · 불명 10 · 폐기/시드 28. → `box/_SQL_대장_20260717.md`
- **확인 필요 상위 5** (사장님 SQL Editor 확인): care_cleanliness / cat_photos_storage_fix / security_invoker_views / admin_rate_limit_exempt / weekly_points.
- 그중 **cat_photos_storage_fix는 프로브로 부분 검증**: 버킷 public 읽기 200(정상), **anon 업로드 → 403 RLS 위반(차단됨 ✅)**, 버킷목록 anon 비노출. 무인증 경로는 안전. (authenticated 유저의 타 폴더 IDOR는 실유저 토큰 필요 → 미검증으로 남김)

## 📝 작성한 문서 (동결 위반 아님 — 전부 문서/진단)
1. `box/회의록_20260717_12에이전트_전체회의.md` — 12명 발언 종합 + 실행계획
2. `box/인터뷰_판정기준_선등록_20260717.md` — 답장 열기 전 판정 기준 박제(악마의 변호인 권고)
3. `box/인터뷰_리마인드_문구_20260717.md` — 무응답자 1회 리마인드(7/21~22) + 후속질문 + 목격쪽지
4. `box/실행리포트_20260717_회의후속.md` — 본 문서
5. `box/_SQL_대장_20260717.md` — box/ SQL 193개 적용/미적용/폐기 대장 ✅

## 🙋 사장님(사람)이 해야 할 것 — 코드로 못 하는 것
- [ ] **keystore 2번째 사본**(USB/클라우드 암호화) — 재발급 불가, 최우선
- [ ] **판정 기준(문서 2) 읽고 확정** — 답장 열기 전에
- [ ] **7/20 밤 답장 집계** → 3통 미만이면 코어 직접 접촉으로 전환
- [ ] **7/21~22 무응답자 리마인드 1회**(문서 3 문구)
- [ ] **주 1회 코어 8명 목격 쪽지**(문서 3 §4) — 주 10분
- [ ] **LBS 신고 착수**(통신판매업보다 먼저 — B2G 결격 방지 + 위치정보법)
- [ ] **7/18·19 주간 크론 재확인** (engagement-push 외 유령 추가 확정)
- [ ] anon `\n` 오염 정리 여부 결정(위생, 비긴급)

## 결론
- 회의가 지목한 **"긴급 2건(로그인 장애·보안 미완)"은 둘 다 오진**으로 판명 — 실제로는 안전. 좋은 소식이자, 메모리/브리핑이 실제보다 비관적이었다는 교훈.
- 진짜 무거운 숫자는 **좀비 79%**와 **코어 루프 = care(밥주기)**. 다음 "단 하나"는 이 두 사실에 정박해야 한다: (1) 첫 세션 활성화, (2) 밥주기 경험.
- 빌드 동결은 유지. 오늘 한 것은 전부 실측·문서·백업 — 동결 위반 없음.
