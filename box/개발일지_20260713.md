# 개발일지 2026-07-13 — 홈 퀵 돌봄 확장 · 어제 숙제 점검

## 요약

어제 남긴 숙제 3건(크론 GET 검증, 지도 관리자 버튼, 홈 돌봄 버튼)을 정리.
크론은 이상 없음 확인, 지도 버튼은 계정 원인으로 좁힘, 홈 히어로에
고양이별 퀵 돌봄 기록(물/간식/건강/쉼터)을 추가·배포. 커밋 1개 배포.

## 오늘 한 것

### 1. 크론 GET 이슈 최종 확인 (이상 없음)
- 크론 라우트 20개 전수 조사 — 전부 `export const GET = POST`(또는 별도 GET) 존재
- 프로덕션 무인증 호출 시 GET/POST 모두 401 (라우팅 정상, 405 아님)
- 결제 대사 크론 불일치 시 관리자 DM이 오게 돼 있으므로 조용하면 정상

### 2. 지도 관리자 버튼 미표시 — 원인 규명 + 수정 (d5f80d6, 배포)
- 데이터는 전부 정상: RLS(`admins_read_self`), admins 행
  (grow29971 = google/kakao/apple 통합 1계정, rias21125).
  ekgo9428@naver.com은 사용자 계정 아님(일반 유저) — 계정 원인 배제
- **실제 결함**: 관리자 체크가 첫 페인트 직후 idle 콜백에서 **1회만** 실행 —
  세션 복원이 늦거나 그 순간 네트워크가 튀면 isAdmin=false로 영구 고정, 재시도 없음
- **수정**: useAuth(onAuthStateChange 구독)의 user.id 변화에 반응하는 effect로 이동.
  세션이 늦게 복원돼도 그 시점에 재확인, 로그아웃 시 false 복귀

### 3. 홈 히어로 퀵 돌봄 확장 (00870ae, 배포)
- 밥주기 옆 `+` 버튼 → 카드 내 오버레이로 물/간식/건강/쉼터 1탭 기록
- 메모·사진이 필요한 기록은 "메모·사진과 함께 기록 →"으로 고양이 상세 연결
- **버그 수정**: "오늘 밥" 뱃지가 care_type 구분 없이 아무 돌봄 기록에나 켜지던 것
  → feed 유형만 집계하도록 수정 (헤더의 n/m 카운트도 동일)

### 4. 지도 마커 배회 애니메이션 — 위치 보호 2차 레이어 (39067d4, 배포)
- 사용자 요청: 등록 고양이가 실제 활동하듯 반경 5km를 불규칙하게 이동
- `roamCoord()` (cats-repo): id 해시 위상 + 서로 소 주기 3개(≈55/13/4분) 사인 합성
  → 결정적 배회 궤적. Date.now() 기반이라 서버 없이 모든 접속자가 같은 위치를 봄
- 마커(개별/동 클러스터) 주기 갱신(200ms). 반경·속도 변천:
  5km/54km/h → 2배 상향(6a2782c, 103km/h) → **최종: 동 이탈 방지 위해
  반경 400m·시속 ~15km(29edb2f)** — 기반 좌표가 이미 동단위 ±444m 오프셋이라
  400m 배회면 합산해도 대부분 동 내부. 주기 14분/3분/1.2분으로 생동감 유지
- 시뮬레이션 검증: 최대 반경 389m(√2 정규화로 반경 이내 보장)
- 헤드리스 브라우저로 프로덕션 실측 — 마커 실제 이동 확인 (안 움직여 보이면
  배포 전에 열어둔 세션이거나 광역 줌 — 앱 재시작 + 동네 줌으로 확인)
- 보안 구조 정리: ① 등록 시 ±444m 오프셋(진짜 위치는 DB에도 없음)
  ② 비로그인 +500m 퍼징 ③ 화면 배회 5km — 화면 관찰로 급식소 특정 불가
- 반경은 `CAT_ROAM_RADIUS_M` 상수 하나로 조절 (5km면 동 라벨과 마커 위치가
  어긋나 보일 수 있음 — 어색하면 500m~1km로 낮추면 됨)

### 4-1. 배회 행동 시스템 — 생체리듬 + 상태 3종 (000cd57, 배포)
- 활동량 = 고양이별 파동(21분/6.4분 주기) × 하루 생체리듬(박명성: 새벽 5~8시·
  저녁 17~22시 활발, 한낮 14시 최저) — 연속 함수라 위치 안 튐
- 활동량이 반경·속도를 조절: 쉴 땐 아지트(등록 지점) 근처 웅크림, 활발할 땐 넓게
- 상태 노출: 사진 마커에 💤/🐾/💨 이모지 뱃지(변경 시만 DOM 갱신),
  고양이 카드에 "지금 쉬는 중/산책 중/우다다 중" 칩(30초 틱)
- 24h 시뮬 밸런스: 쉼 24%/산책 58%/우다다 18%, 평균 시속 6.7km, 최대 313m

### 5. 위치 노출 일관성 점검 + 안내 문구 현행화 (c4d9fe9, 배포)
- 고양이 좌표를 화면에 그리는 곳은 /map뿐임을 전수 확인
  (홈은 반경 필터 계산만, 상세·동네 페이지는 지도 없음)
- 지도 고양이 카드에 "마커는 실제 위치와 다르게 계속 움직여요" 안내 추가
- 고양이 상세의 낡은 문구("정확한 위치는 로그인 후 지도에서 공개") 교체

### 6. 홈 섹션 재배치 — Figma Make 4화면 구조 1단계 (6458b54, 배포)
- 사용자가 Figma Make 결과물(저장 HTML) 전달 — 비주얼/코드는 Figma 클라우드에
  있어 저장본에 없고, 4화면 구조표만 추출 가능했음
- 적용: 1화면 브리핑+내 아이들 / 1.5 동네소식+LIVE 3건 / 2.5 내활동+오늘할일
  (냥상자·스트릭·도감·돌봄왕·TOP5) / 3.5 냥식+입양+HOT / 홍보류 그 아래
- 재배치는 scratchpad 라인범위 스크립트로 수행(경계 assert), tsc+build 통과
- 2단계(비주얼 리스킨) 대기: Figma Make에서 코드 내보내기(Download code)
  또는 화면 스크린샷 필요

### 7. 홈 고양이 사회 소식 섹션 제거 (e075517, 배포)
- 사용자 요청. 뉴스 카드 3건 + listNews fetch/state/임포트 정리. /news 페이지는 유지

### 8. 앱 설명에 위치 보호 안내 추가 (2dd2e55, 배포)
- /about "고양이 위치는 이렇게 지켜요" 섹션 — 3중 보호 설명 카드
- /faq 낡은 위치 답변(100m 퍼징·검증된 이웃 — 실제와 불일치) 교체
  + "마커가 움직이는 이유" Q&A 신규 (JSON-LD 동기화)

### 9. 홈 리디자인 시안 아티팩트 (코드 반영 대기)
- Figma Make 대신 HTML 시안 제작·공유: claude.ai/code/artifact/5c08b3a0-…
- 실제 토큰(#3182F6/#F9FAFB/라운드 20) 기반, 오늘 할 일 가로 스와이프 제안 포함
- 사용자 피드백 후 실코드 반영 예정

### 10. 운영 점검 스윕 → 크론 무실행 발견·대응
- 페이지 스모크 전부 200 (홈만 1.9s — 서버 인증, 나머지 0.15s대)
- 인증 에러 0건 / 주문 0건(테스트 정리됨 정상) / 최근 7일 고양이 7·돌봄 8·글 0
- **크론 무실행 발견**: 오늘 08:00 운영 요약 DM 안 옴 → 수동 GET은 200 (코드·
  시크릿 정상) → 스케줄 호출 자체가 안 온 것. 디제스트 DM 역사상 0건.
  rescue_hospitals 4/16 이후 미갱신(18:00 UTC sync-pharmacies도 의심).
  news-crawl(00:00 UTC)만 실행 확인. Hobby 한도는 100개라 개수 문제 아님
- **대응**: ① proxy.ts 크론 하트비트(모든 /api/cron/* 호출을 cron_runs에 기록)
  ② digest 23:00→00:10 UTC(09:10 KST)·reconcile 22:30→00:20 UTC(09:20 KST) 이동
  ③ 오늘자 운영 요약 수동 발송(12:29, 2건)
- ⚠ SQL 실행 필요: `box/supabase_cron_runs_migration.sql` (RUN_ORDER 무관, 단독)
- ⚠ 발견: **미처리 문의 13건** — /admin/inbox 확인 필요

### 11. "전부 ㄱ" 10종 일괄 처리 (오후)
1. **홈 TTFB**: getUser→getSession + **Vercel 함수 리전 서울(icn1) 이전** —
   1.1~1.4s → 0.4~0.6s (콜드 제외). 전 API·결제도 국내 왕복으로 단축
2. **배회 날씨 연동**: 비/눈/폭염/한파 시 고양이들이 아지트에 숨음.
   쉼 뱃지 사유별(☔☃️🥵🧣). 지도 진입 시 /api/weather 1회
3. **밥주기 반응**: 돌봄 기록 → 마커가 2.5s에 아지트로 달려와 60s 밥(🍚) 후 복귀
4. **커뮤니티 소생**: AI 집사 나비 자동 토픽 크론(화·금 09:30 KST, Gemini+폴백,
   활성 시 침묵 가드). 첫 글 수동 발사 완료("여름철 눅눅해진 사료 관리…")
5. 쇼핑 오픈 체크리스트 (box/쇼핑오픈_체크리스트.md)
6. 플레이스토어 리스팅 문안 (box/플레이스토어_리스팅_문안.md)
7. **SEO**: sitemap에 /shop·상품 전체 추가(오픈 전 인덱싱 선점), 상품 Product JSON-LD
8. **온보딩 퍼널 실측** (box/온보딩_퍼널_리포트_20260713.md):
   가입 309 → 지역 33% → 등록 17% → 돌봄 7%. **병목 = 가입→지역 설정(67% 이탈)**,
   등록은 첫 세션 결판(중앙값 6분). 개선 1순위: 가입 직후 지역 설정 1스텝화
9. **홈 비주얼 2단계 1차**: 오늘 할 일 가로 칩 행(냥상자/스트릭/도감/랭킹),
   도감 대형 카드 흡수, 날씨 팁 1개로 압축
10. 크론 전면 복구 — 내일 cron_runs 하트비트 데이터 확인 후 진행 (대기)

### 12. 주간 출석체크 + 쇼핑 포인트 (저녁)
- 주간 보드(홈): 월~일 스탬프(일일 출석체크 완료 시 checkin_days 기록),
  마일스톤 3일 100P/5일 200P/7일 300P — 주 최대 600P(=600원 할인)
- 포인트: user_points 잔액 + point_ledger 이력. 지급/차감 전부 service_role RPC
  (grant_points reason 유니크 멱등, spend_points 원자적 조건부 차감)
- 결제 연동: 체크아웃 포인트 입력(1P=1원, 전액 버튼) → confirm이 실가격
  재계산에 반영 + 재고 확보 후 차감, 실패 시 자동 환급, 취소/웹훅 반환.
  후원·가상 상품 주문은 포인트 사용 불가(실입금 없는 후원 왜곡 방지),
  최종 결제액 100원 이상 유지
- ⚠ SQL 실행 필요: `box/supabase_weekly_points_migration.sql` (롤백 주석 포함)
  — 실행 전까지 보드/포인트 UI는 조용히 숨김(배포 안전)
- ⚠ 후속: 약관에 포인트 조항(적립·사용·소멸·환불 시 처리) 추가 검토 필요

## 다음 할 것 (어제 목록에서 이월)
1. **사업자등록 신청** (홈택스) — 통신판매업·LBS 신고·PG 심사 전부 여기 묶임
2. 후원 상품 테스트 결제 1건 (배송지 생략 흐름, 아직 미확인)
3. 지도 관리자 버튼 — 수정 배포됨. 문제 기기에서 재확인만 (고양이 카드 열고 좌상단)
4. 지도 화면 피그마 리디자인 ([화면 2] 프롬프트로 목업 생성 → 전달)
5. 실제 판매 상품 확정·등록 (21개 시드 교체)
