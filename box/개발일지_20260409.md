# 도시공존 개발일지 — 2026년 4월 9일 (수)

## 작업 시간
- **1차 작업**: 오전 11:00 ~ 11:40 (약 40분)
- **2차 작업**: 오후 4:00 ~ 5:20 (약 80분)
- **총 작업 시간**: 약 2시간

---

## 1. Vercel 최초 배포 (11:00~11:10)

### 작업 내용
- Vercel CLI 로그인 및 최초 배포
- 프로젝트 스코프 연결: `grow29971-6597s-projects/city`
- 프로덕션 배포 완료

### 배포 URL
- **https://city-amber-omega.vercel.app**

---

## 2. Supabase 인증 시스템 구축 (11:10~11:40)

### 신규 파일
| 파일 | 설명 |
|------|------|
| `lib/supabase/client.ts` | 브라우저용 Supabase 클라이언트 |
| `lib/supabase/server.ts` | 서버용 Supabase 클라이언트 |
| `lib/supabase/proxy.ts` | 세션 갱신 유틸리티 |
| `lib/auth-context.tsx` | 앱 전체 인증 상태 관리 (AuthProvider) |
| `app/api/auth/callback/route.ts` | OAuth 콜백 처리 API 라우트 |
| `proxy.ts` | Next.js 16 프록시 (구 middleware) — 세션 유지 |

### 수정 파일
| 파일 | 변경 내용 |
|------|------|
| `app/layout.tsx` | AuthProvider로 앱 전체 감싸기 |
| `app/login/page.tsx` | Supabase 이메일/비밀번호 로그인 + Google/Kakao OAuth 연동 |
| `app/signup/page.tsx` | Supabase 이메일 회원가입 + 이메일 인증 안내 화면 추가 |
| `app/(main)/mypage/page.tsx` | 로그인 상태에 따른 유저 정보 표시 + 로그아웃 기능 |
| `package.json` | `@supabase/supabase-js`, `@supabase/ssr` 추가 |
| `.gitignore` | `.vercel` 디렉토리 제외 추가 |

### 구현 내용
- **이메일/비밀번호 회원가입**: 닉네임 + 이메일 + 비밀번호로 실제 Supabase 회원가입
- **이메일/비밀번호 로그인**: Supabase `signInWithPassword` 연동
- **소셜 로그인 UI**: Google, Kakao OAuth 플로우 구현
- **AuthProvider 컨텍스트**: `useAuth()` 훅으로 앱 전체에서 유저 상태 접근
- **마이페이지**: 로그인 시 닉네임/이메일/아바타 표시, 비로그인 시 게스트 + 로그인 유도
- **로그아웃**: 마이페이지에서 로그아웃 버튼 클릭 시 세션 제거
- **이메일 인증 안내**: 회원가입 후 "인증 메일을 보냈습니다" 화면 표시
- **에러 핸들링**: 잘못된 비밀번호, 이미 가입된 이메일 등 한국어 에러 메시지
- **Next.js 16 대응**: `middleware.ts` → `proxy.ts` 변경

### Supabase 설정
- 프로젝트: `sozxbnvgsougkliibnxl.supabase.co` (동북아시아/도쿄)
- Vercel 환경변수 등록 완료
- Site URL → `https://city-amber-omega.vercel.app` 변경

---

## 3. Google OAuth 설정 (16:00~16:35)

### 작업 내용
- Google Cloud Console에서 OAuth 동의 화면 생성
- OAuth 2.0 웹 클라이언트 ID 생성
- 리디렉션 URI: `https://sozxbnvgsougkliibnxl.supabase.co/auth/v1/callback`
- Supabase Authentication → Providers → Google 활성화 + Client ID/Secret 입력
- 앱 게시 상태를 프로덕션으로 변경

### 현재 상태
- ⚠️ 403 오류 발생 — 추가 설정 필요 (내일 해결 예정)

---

## 4. Kakao OAuth 설정 (16:35~17:00)

### 작업 내용
- Kakao Developers에서 "도시공존" 앱 생성 (ID: 1427312)
- 카카오 로그인 활성화 (ON)
- 웹 도메인 등록: `https://sozxbnvgsougkliibnxl.supabase.co`
- 로그아웃 리다이렉트 URI 등록
- 동의항목: 닉네임(필수), 프로필사진(필수) 설정
- Supabase Authentication → Providers → Kakao 활성화 + REST API Key/Client Secret 입력

### 현재 상태
- ⚠️ KOE205 오류 발생 — Redirect URI 설정 추가 필요 (내일 해결 예정)

---

## 5. 온보딩 첫 방문자 전용 처리 (17:00~17:20)

### 수정 파일
| 파일 | 변경 내용 |
|------|------|
| `app/(main)/page.tsx` | 첫 방문 시 온보딩 리다이렉트 (localStorage 체크) |
| `app/onboarding/page.tsx` | 완료/건너뛰기 시 localStorage 플래그 저장 + 홈으로 이동 |

### 구현 내용
- 처음 방문 → 자동으로 `/onboarding` 표시
- 온보딩 완료 or 건너뛰기 → `dosigongzon_onboarded` 플래그 저장
- 재방문 시 → 바로 홈 표시
- 온보딩 후 목적지를 `/login` → `/` (홈)으로 변경

---

## 커밋 내역
```
c0bb4f3 feat: Supabase 인증 연동 — 이메일 회원가입/로그인 + 소셜 로그인 UI
f6d992c feat: 온보딩 첫 방문자 전용 처리 + 개발일지 추가
```

---

## 미해결 이슈
- [ ] Google OAuth 403 오류 해결 (OAuth 동의 화면 또는 클라이언트 설정 점검)
- [ ] Kakao OAuth KOE205 오류 해결 (Redirect URI 등록 위치 확인)
- [ ] 이메일 인증 메일 미발송 (Supabase 무료 플랜 시간당 3~4건 제한 → Confirm email 끄기 또는 커스텀 SMTP 연결)

## 내일 할 일
- [ ] Google/Kakao 소셜 로그인 오류 해결
- [ ] 이메일 인증 설정 결정 (끄기 vs SMTP)
- [ ] 홈 화면 로그인 상태 반영 (인사말에 닉네임 표시)
- [ ] 커뮤니티 글쓰기 로그인 유도 처리
- [ ] 돌봄일지 (Phase 2 — 5단계) 구현 시작
