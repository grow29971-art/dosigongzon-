# 개발일지 2026-04-19

## 오늘 한 일 요약

유저 유입/리텐션 풀코스. 크게 8개 레버를 하루에 순차 배포.

| # | 주제 | 성격 |
|---|---|---|
| 1 | 지도 쉼터 기능 제거 | 정리 |
| 2 | 카카오 로그인 제거 (삼성인터넷 KOE205) | 버그 회피 |
| 3 | 공유 바이럴 (고양이 상세 + 긴급) | 유입 |
| 4 | 지역 SEO 랜딩 `/areas`, 25구 | 유입 |
| 5 | 외부 파트너십 인프라 `/about` + 공유 템플릿 | 유입 |
| 6 | PWA 설치 프롬프트 개선 + 마이페이지 수동 메뉴 | 리텐션 |
| 7 | 홈(`/`) 서버 컴포넌트 전환 + SEO 랜딩 | 유입 |
| 8 | 친구 초대 코드 시스템 + 초대 업적 | 바이럴 루프 |
| 9 | 동별(`/areas/[gu]/[dong]`) 276개 랜딩 | 롱테일 SEO |
| 10 | 이메일 위클리 다이제스트 (Resend + Vercel Cron) | 리텐션 |
| 11 | 돌봄일지 `treat` (간식 줌) 타입 추가 | 기능 |
| 12 | 더미 글 댓글 시드 + 고양이 설명 리라이트 | 데모 퀄 ↑ |

---

## 1. 지도 쉼터 기능 제거

- `/map` 페이지에서 "쉼터" 필터 칩, FAB 버튼(🏠), 마커 오버레이, 상태·모달 모두 제거
- 관련 파일 삭제: `app/components/AddShelterModal.tsx`, `lib/shelters-repo.ts`, `box/supabase_shelters_migration.sql`
- Supabase `shelters` 테이블은 일단 유지 (나중에 재도입 여지)

## 2. 카카오 로그인 제거

- 증상: 삼성인터넷에서 카카오 로그인 → KOE205 에러 (state 검증 실패 / 3rd party cookie drop)
- 원인: 삼성인터넷 "스마트 안티 트래킹" 기본값이 OAuth 리다이렉트 쿠키 차단
- 1차 시도: `detectSamsungInternet()` + 경고 배너 추가 → 유저 행동 변경 요구라 CR
- 최종: 카카오 로그인 자체 제거 (구글만 유지). `handleSocialLogin`의 kakao 분기 및 scope 제거
- `lib/in-app-browser.ts`의 `detectSamsungInternet`은 추후 재사용 가능하므로 유지
- 기존 카카오 가입 유저 세션과 `auth_error_logs`의 kakao 레코드 호환성 때문에 `auth-errors.ts` / admin 페이지의 분기는 유지

## 3. 공유 바이럴 (유입 레버 1)

**카카오톡 공유 버튼 3종:**

- **`app/components/ShareCatButton.tsx`** — 고양이 상세 페이지 (`/cats/[id]`) 바닥 CTA
  - 일반 모드: 노란 카카오 톤
  - **urgent 모드**: `health_status === "danger"`일 때 빨간 펄스 애니메이션 + "🚨 긴급 · 동네에 알리기" + 긴급 공유 문구 자동 생성
- **`app/components/ShareAreaButton.tsx`** — `/areas/[gu]` 지역 랜딩 + 홈 랜딩 공용
- 모든 공유 URL에 `utm_source=kakao&utm_medium=share&utm_campaign=...` 자동 부착
- Kakao SDK 실패 시 폴백: `navigator.clipboard.writeText`

## 4. 지역 SEO 랜딩 (유입 레버 2)

**`/areas`** 서울 25개 구 인덱스 + **`/areas/[slug]`** 25개 구별 랜딩

- `lib/seoul-regions.ts` — 25개 구 메타데이터 (슬러그, dongs 배열, 대표 좌표)
- `lib/cats-server.ts`에 `getCatsByRegionServer`, `getCatCountByRegionServer` 추가
- `lib/supabase/anon.ts` 신규 — 쿠키 없는 anonymous client로 ISR 호환
- `generateStaticParams` + `revalidate = 3600` → 25개 구 SSG+ISR
- JSON-LD: `Place` + `ItemList`
- 히어로 + 통계 + 고양이 그리드 + 지역 병원 + 주요 동네 + 다른 구 네비
- `app/sitemap.ts`에 26개 URL 추가 (인덱스 + 25구)

## 5. 외부 파트너십 인프라 (유입 레버 3)

- **`/about`** — 제휴/언론/블로그용 공개 페이지
  - 실시간 통계 (등록 고양이, 동네 이웃, 치료 병원)
  - 핵심 기능 4종
  - 4원칙 (안전 우선, 중립성, 검증된 정보, 비영리)
  - `mailto:grow29971@gmail.com` CTA
- **`box/외부공유_템플릿.md`** — 당근 동네생활, 맘카페, 인스타, X, 언론 콜드메일, 동물보호단체 협력 제안, 로컬 SEO 체크리스트, UTM 규칙 총정리

## 6. PWA 개선

- **`PwaInstallPrompt`** 노출 타이밍 개선
  - 첫 방문엔 안 띄움 (2회차 방문 이상)
  - 세션당 10초 체류 후 노출
  - Vercel Analytics 이벤트 4종: `pwa_install_prompted/choice/dismissed/installed`
- **`InstallAppMenuItem`** — 마이페이지 "내 설정"에 수동 설치 메뉴
  - 이미 설치(`standalone`)됐으면 "앱 설치 완료" 표시
  - 크롬/엣지: 저장된 `beforeinstallprompt`로 바로 설치
  - iOS 사파리: 3단계 안내 모달 (공유 → 홈 화면에 추가 → 추가)
  - iOS 크롬/파이어폭스: 사파리 안내

## 7. 홈(`/`) 서버 컴포넌트 전환

기존: `(main)/page.tsx`가 완전 client. 비로그인 유저가 `/map`으로 즉시 리다이렉트 → 크롤러·소셜 스크랩에 SEO 본문 없음.

**변경:**
- 기존 클라이언트 대시보드를 `app/components/HomeAuthed.tsx`로 이동 (`git mv`)
- 새 `(main)/page.tsx`는 서버 컴포넌트 — 쿠키 auth 체크 후 분기
- 비로그인: **`HomeLanding.tsx`** (서버 컴포넌트) SEO 랜딩
  - 히어로 + 실시간 통계 + 최근 고양이 4마리 + 6개 주요 구 링크 + 4가지 가치 + FAQ 4개 + 공유 CTA + /about 링크
  - JSON-LD `WebApplication` + `FAQPage` (구글 리치 스니펫 자격)
- 로그인: `HomeAuthed.tsx` 기존 대시보드 그대로
- 온보딩 리다이렉트는 `LandingOnboardingGate.tsx` (작은 client gate)로 분리 — localStorage 체크는 hydration 후

루트가 `ƒ` (dynamic SSR)로 잡히지만 cookie-기반 auth 분기라 정상. 비로그인 요청 응답은 빠르고 SEO 풍부.

## 8. 친구 초대 코드 시스템 (바이럴 루프)

**SQL (`supabase_invites_migration.sql`)**
- `profiles.invite_code` (unique) + `profiles.invited_by`
- `invite_events` 테이블 (감사 로그, invitee 기준 unique)
- `generate_invite_code()` — 6자 영숫자, 혼동 문자(0·O·1·I) 제외
- `apply_invite_code(text)` RPC — 원자적 적용 (중복/자기초대/코드 없음 모두 차단)
- `my_invite_stats` view
- 기존 유저에게 invite_code 일괄 부여

**코드**
- `lib/invites-repo.ts` — 조회/적용/포맷 검증 + localStorage pending 코드 관리
- `/signup`에 `?invite=XXX` 쿼리 수용 + 수동 입력 필드 (Gift 아이콘)
- `PendingInviteApplier.tsx` — 레이아웃에 붙어서 세션 열리면 자동 적용
- `InviteSection.tsx` — 마이페이지 최상단 핑크 카드
  - 내 코드 대문짝만하게 표시 (글자 간격 0.18em, 22px black)
  - 링크 복사 / 카카오 공유 (utm_medium=invite)
  - "N명 초대" 카운트 뱃지
- 이벤트: `invite_link_copied`, `invite_share_kakao_started/sent`

**업적 연동 (`titles.ts`)**
- `MyActivitySummary.inviteCount` 필드 추가 (invite_events 카운트)
- 새 카테고리 "invite" (#E86B8C)
- 3개 업적:
  - 🌱 씨앗을 심는 자 (1명)
  - 🤝 다리를 잇는 자 (3명)
  - 🌳 마을을 가꾸는 자 (10명)

## 9. 동별 세부 랜딩 (롱테일 SEO)

- **`/areas/[slug]/[dong]`** — 서울 25구 하위 276개 동
- URL 예: `/areas/gangnam/역삼동`, `/areas/mapo/연남동`, `/areas/songpa/잠실동`
- `generateStaticParams`로 모든 276개 조합 pre-render → SSG + 1h ISR
- 빈 동(고양이 0마리) `robots: noindex` — thin content 방어
- JSON-LD: `Place` + `BreadcrumbList` (서울 → 구 → 동)
- Breadcrumb navigation, 같은 구 다른 동 링크, 구 병원 리스트
- 사이트맵에 276개 URL 추가

## 10. 이메일 위클리 다이제스트 (리텐션)

**SQL (`supabase_email_digest_migration.sql`)**
- `profiles.email_digest_enabled` (기본 true) + `last_digest_sent_at`
- `profiles_update_self_digest` RLS 정책

**코드**
- `lib/weekly-digest.ts` — 수신자/컨텐츠 조회, HTML 렌더
  - `listDigestRecipients`: opt-in + 최근 7일 미발송 유저만
  - `getDigestBaseContent`: 이번 주 새 고양이/새 이웃/긴급/인기
  - `renderDigestHtml`: 인라인 스타일 HTML + 플레인텍스트 폴백
- `app/api/cron/weekly-digest/route.ts` — `CRON_SECRET` 인증, `RESEND_API_KEY` 없으면 dryRun
- `vercel.json` cron: `0 23 * * 0` (일 23:00 UTC = 월 08:00 KST)
- `EmailDigestToggle.tsx` — 마이페이지 수신 on/off 스위치 (핑크)
- `resend` 패키지 설치

**운영 상태**
- Resend 가입 + API 키 발급 + Vercel env 등록 완료
- 1차 수동 테스트: 48/48 API 성공 (실제 delivery는 Resend Logs 확인 필요)
- 도메인 검증(`dosigongzon.com` SPF/DKIM) 후 `noreply@dosigongzon.com`로 교체 예정

## 11. 돌봄일지 `treat` 타입

- `CareType`에 `"treat"` 추가, `CARE_TYPE_MAP`에 `🍗 간식 줌` (#E8B040, 골드)
- `care_logs.care_type` CHECK 제약 교체 (SQL: `supabase_care_logs_add_treat.sql`)
- 입력 UI는 `CARE_TYPE_MAP` 자동 순회라 코드 수정 없이 옵션 노출됨

## 12. 더미 데이터 퀄리티 업

- **`supabase_post_comments_dummy_seed.sql`** — 58개 더미 포스트에 카테고리별 톤 다른 댓글 2~4개씩 약 150~230개 삽입. 닉네임 풀 30개, 타임스탬프 1~20h 분산, `posts.comment_count` 동기화
- **`supabase_cats_dummy_humanize_descriptions.sql`** — 41마리 더미 고양이 설명을 2~3문장 개인 에피소드로 교체. 일부 이름도 변경 (치즈 → 치즈돌이, 모카 → 모카라떼)
- 안전장치: `photo_url LIKE 'https://placehold.co/%'` 조건으로 실제 유저 등록 보호

---

## 메모 · TO-DO

- **더미 고양이 사진 → 실제 길고양이 사진**: 저작권 이슈로 보류. Unsplash outdoor cat 10~15장 풀 만들거나 본인이 직접 찍어 업로드. 시간 날 때.
- **Resend 도메인 검증**: DNS(SPF/DKIM) 설정 후 from email을 `noreply@dosigongzon.com`로. 지금은 `onboarding@resend.dev` (본인 Gmail에만 실제 배달 가능)
- **다음 레버 후보**:
  - 9번 — `/rescue` 실시간 긴급 구조 피드 페이지
  - 초대 코드 CTA를 다른 페이지(홈, 고양이 상세)에도 노출
  - 친구가 가입 완료했을 때 초대자에게 알림 푸시
- **SEO 인덱싱 체크**: 2~4주 후 구글 서치콘솔에서 `/areas/*` 인덱싱 수 확인

---

## 배포 기록

하루에 최소 7번 이상 `vercel --prod --yes`. 모두 정상 배포.
마지막 배포: `dpl_...` (10번 이메일 다이제스트 반영 + env 재적용)

---

## 저녁 세션 추가 작업 (4/19 18:00~)

### 13. Google / Naver 서치 콘솔 등록 + 사이트맵 제출

- Google Search Console: HTML 태그 방식 소유권 확인 (`layout.tsx` verification 코드 `bBcue9CIj5JLafNxn_u41zPHPsKPxnDcFJXoHnVScj4` 로 업데이트)
- Naver 서치어드바이저: 기존 등록 활용, `sitemap.xml` 재제출 (전체 URL로), 주요 10개 URL 웹페이지 수집 요청
- Google 사이트맵 370페이지 인식 성공 (25구 + 276동 + 나머지)
- Google 색인 요청: `/`, `/about`, `/areas`, `/areas/gangnam`, `/map`, `/protection/feeding-guide`, `/protection/shelter-guide`

### 14. PWA 아이콘 디자인 교체

- 기존 저품질 1KB 아이콘 → 주황 그라디언트 + 흰 paw 디자인으로 교체
- `box/icon-source.svg` 소스, `box/build-icons.mjs` 빌드 스크립트 (sharp 사용)
- 결과물: `icon-192.png`, `icon-512.png`, `apple-touch-icon.png` (180×180), `favicon-48.png`
- `manifest.json`에 apple-touch-icon 추가, layout.tsx에 apple icon 업데이트

### 15. 컨텐츠 SEO — `/protection` 가이드 2편 추가 (B번 작업)

- **`/protection/feeding-guide`** — "길고양이 먹이 가이드"
  - 키워드: 길고양이 먹이, 고양이 우유, 참치캔, 길고양이 급식
  - 금지 음식 10 + 권장 음식 6 + 급식 5원칙 + 계절별 주의사항
  - JSON-LD: Article + FAQPage
- **`/protection/shelter-guide`** — "겨울나기 · 숨숨집 만들기"
  - 키워드: 길고양이 쉼터, 숨숨집, 스티로폼 고양이 집
  - 준비물 + 5단계 제작법 + 주의사항 5 + 계절별 운영
  - JSON-LD: Article + HowTo + FAQPage
- `/protection` 인덱스에 2개 카드 추가, sitemap 등록

### 16. 초대 알림 (C번 작업)

- `NotificationType`에 `invite_accepted` 추가
- `getNotifications`에 `invite_events` 조회 추가 — invitee 프로필(닉네임·아바타) 포함
- `getUnreadNotificationCount`에 최근 7일 invite 이벤트 카운트 반영
- 알림 페이지에 🎁 Gift 아이콘 + 핑크 (#E86B8C) 스타일
- 클릭 시 `/users/[invitee-id]` 로 이동

### 17. 실시간 댓글·돌봄일지 푸시 알림

- `createComment` 성공 후 고양이 주인에게 webpush (경보는 🚨 강조)
- `createCareLog` 성공 후 고양이 주인에게 webpush (care_type별 이모지)
- 본인이 본인 고양이에 기록하면 푸시 안 감
- 기존 DM 푸시와 동일 패턴 (`/api/push/send` + session access_token)

### 18. 주기적 재참여 푸시 Cron

- `/api/cron/engagement-push` 신규
- 매주 수요일 19:00 KST (10:00 UTC) 실행
- 이번 주 새 고양이 수 / 긴급 돌봄 필요 수 집계 후 전체 구독자 broadcast
- 콘텐츠 자동 선택: 신규+긴급 / 신규만 / 긴급만 / 기본 인사 4패턴
- 410/404 응답 받은 구독은 자동 삭제 (만료 정리)
- vercel.json crons 추가

---

## 운영 TO-DO (서치 콘솔 관련)

- [ ] 1~3일 후 `site:dosigongzon.com` 구글 검색으로 색인 확인
- [ ] 네이버 웹페이지 수집에 새 2페이지(`/protection/feeding-guide`, `/protection/shelter-guide`) 추가 요청
- [ ] 당근/맘카페/네이버 블로그 1편씩 포스팅 (템플릿은 `box/외부공유_템플릿.md`)
- [ ] 이메일 다이제스트 실제 배달 확인 (Resend Dashboard → Logs)
- [ ] Resend 도메인 검증 (`dosigongzon.com` DNS SPF/DKIM)
- [ ] VAPID_EMAIL 환경변수 설정 확인 (engagement-push에서 사용)

---

## 밤 세션 추가 작업 (4/19 19:00~)

### 19. streak 포인트 · 업적 연동

- `MyActivitySummary`에 `currentStreak`, `weeklyGoalAchieved` 필드 추가
- `getMyActivitySummary`가 최근 60일 `care_logs` 조회 후 KST 기준 streak/주간 목표 계산 (streak-repo 로직 재사용)
- `computeScore` 보너스:
  - 7일 연속 +10점 / 30일 +30점 / 100일 +100점
  - 이번 주 월~일 개근 시 추가 +5점
  - 친구 초대 1명당 +15점 (신규)
- `titles.ts`에 "꾸준함" 카테고리(#E88D5A) + 4개 업적: 🔥 한 주의 약속 / 📅 주간 개근 / 🔥🔥 한 달의 약속 / 🏆 한결같은 마음
- 홈 streak 카드 하단에 현재 상태 기반 보상 힌트 표시

### 20. FeatureTipsCard — 개인화 기능 유도

- `app/components/FeatureTipsCard.tsx` 신규 (client)
- 유저의 activity + regions 상태로 7개 팁 중 **가장 적합한 1개 자동 선택**
- 우선순위: 활동 지역 → 첫 고양이 → 첫 돌봄 → streak 도전 → 친구 초대 → 커뮤니티 → 보호 가이드(폴백)
- X 버튼으로 영구 dismiss (localStorage)
- HomeAuthed에서 OnboardingCard 닫힌 후 노출

### 21. SignupNudgeBar — 비로그인 유저 가입 유도

- `app/components/SignupNudgeBar.tsx` 신규 (client, layout에 global)
- 비로그인 + 화이트리스트 경로(`/`, `/map`, `/cats/*`, `/areas/*`, `/protection/*`, `/hospitals`, `/shelters`, `/about`)에서만 노출
- 페이지 진입 4초 후 하단에 스티키 카드 (주황 그라디언트)
- X → 24h dismiss
- `signup?next={현재경로}` 링크 자동 부착 → 가입 후 원래 페이지로 복귀

### 22. 지역 채팅 유도 푸시 Cron

- `/api/cron/area-chat-nudge` 신규
- 매주 **금 20:00 KST**(11:00 UTC 금) 실행
- `user_activity_regions`에서 primary 지역 있는 유저 전체 조회
- 유저 지역명 → `SEOUL_GUS`로 정규화 (역삼동 → 강남구 매핑)
- 해당 구의 최근 7일 `area_chats` 메시지 3개 이상이고 본인 참여 이력 없을 때만 푸시
- 메시지: "💬 {구이름} 이웃들이 대화 중이에요 · 이번 주 N개의 이야기 · 들러주세요"
- URL: `/map`
- vercel.json crons 추가

### 23. 카카오 재도입 시도 → 실패 (비즈 앱 필요)

- 에러 로그 22건 분석: `invalid_scope: account_email` — 카카오 개인 앱은 account_email 권한 없음
- Supabase Kakao provider는 `account_email` 스코프 하드코딩 요청 → 개인 앱으로 회피 불가
- 카카오 개발자 콘솔 확인: "카카오계정(이메일)" 행이 "권한 없음" 상태, 설정 버튼 자체 없음
- **결론:** 사업자 등록 → 카카오 비즈 앱 전환 심사 통과해야만 재도입 가능
- 액션: 내일(4/20~) 사업자 등록 예정. 승인 후 복구 배포 진행
- 메모리에 계획 저장 (`project_kakao_biz_app.md`)

---

## 주간 자동화 스케줄 정리 (4/19 기준)

| 요일·시간 (KST) | 작업 | 대상 |
|---|---|---|
| 월 08:00 | 이메일 다이제스트 | Email 구독자 (opt-in) |
| 수 19:00 | 전체 동네 활동 브로드캐스트 푸시 | 푸시 구독자 전체 |
| 금 20:00 | 지역 채팅 유도 푸시 | 활동 지역 유저 중 미참여자 |
| 매일 03:00 | 동물약국 데이터 동기화 | - (배경 작업) |

## 다음 세션 TO-DO (메모)

- [ ] 사업자 등록 후 카카오 비즈 앱 전환 신청
- [ ] 비즈 앱 심사 통과 후 카카오 로그인 버튼 복구 + 삼성인터넷 KOE205 경고 배너 재활성
- [ ] 구글/네이버 인덱싱 확인 (1~3주 후)
- [ ] 당근/맘카페/블로그 포스팅 (운영)
- [ ] Resend 도메인 검증 → `noreply@dosigongzon.com` 발신 교체

---

## 밤늦은 세션 추가 (4/19 20:00~)

### 24. 지도에 내 위치 마커 추가

- 기존: GPS로 지도 중심만 이동, 시각적 마커 없음
- 추가: 파란 점(18×18px) + 펄스 링 CustomOverlay — 구글맵 스타일
- `userPos` 변경 시 자동 갱신, `handleLocateMe`에서도 setUserPos 호출로 버튼 눌러도 마커 최신화
- pointer-events: none → 다른 마커 클릭 방해 안 함, zIndex 100
- 전역 keyframes `dosi-user-pulse` 한 번만 주입

### 25. FeatureTipsCard 빈도·풀 개선

- dismiss TTL: 영구 → 3일 → **2시간** (하루 3~5번 로테이션)
- localStorage 키 v3로 갱신해 기존 기록 초기화
- 신규 팁 2종:
  - 💬 지역 채팅 참여하기 (활동 지역 또는 고양이 있을 때)
  - ➕ 주변에 또 다른 아이 있나요? (1~19마리 유저 계속 권장)
- 기존 팁 7종 + 신규 2종 = 9종, 우선순위 재정비

### 26. 고양이 등록 유도 강화

- MapCoachmark dismiss 영구 → **24시간 TTL** (localStorage 키 v2)
- 고양이 0마리 로그인 유저의 지도 "+ FAB" 버튼에:
  - `animate-ping` 주황 펄스 링
  - 우상단 핑크 **NEW** 뱃지
- 등록 시 자동 비활성

### 27. 주간 푸시 cron 추가 (지역 채팅 유도)

- `/api/cron/area-chat-nudge` — 매주 금 20:00 KST
- 활동 지역(primary) 있는 유저 → 해당 구의 최근 7일 채팅 3+개이고 본인 미참여면 푸시
- `SEOUL_GUS`로 동 → 구 매핑 (역삼동 입력해도 강남구 채팅과 매칭)

### 28. 가입 유도 플로팅 바

- `SignupNudgeBar.tsx` 신규 — 비로그인 + 공개 페이지에서만
- 페이지 진입 4초 뒤 하단 주황 그라디언트 카드
- X로 24h dismiss, 가입 후 원래 경로 복귀
- 화이트리스트: `/`, `/map`, `/cats/*`, `/areas/*`, `/protection/*`, `/hospitals`, `/shelters`, `/about`

### 29. Galaxy A34 레이아웃 밀림 수정

- 증상: 커뮤니티 상세 페이지가 가로로 좌우 스크롤되어 콘텐츠 잘림
- 원인: 반응 바 7개 버튼(좋/싫/조회/댓글/카톡/공유/신고)이 한 줄에 안 들어가 overflow
- 수정:
  - 반응 바를 좌측 그룹(좋/싫/조회/댓글) + 우측 그룹(카톡/공유/신고)으로 `flex-wrap + justify-between` 분리
  - 좁은 화면에서 자연스럽게 2줄로 배치
  - 아이콘·gap 약간 축소
- 방어: `(main)/layout.tsx`에 `overflow-x-hidden` 이중 적용 (외부·내부 모두)

### 30. 카카오 재도입 시도 → 실패 (2차)

- 스코프 문제 원인 규명: Supabase Kakao provider가 `account_email`을 하드코딩 요청. 개인 앱은 이 스코프 권한 없음 → `invalid_scope` 반환
- Supabase UI에 Scopes 필드 없음 (대시보드에서 수정 불가)
- 카카오 콘솔에서 `account_email` "권한 없음" 상태 — 설정 버튼 자체 없음
- 결론: 사업자 등록 → 비즈 앱 심사 통과가 유일한 해결. 내일 사업자 등록 예정
- 계획은 메모리(`project_kakao_biz_app.md`)에 저장

---

## 오늘 총 5커밋 예상

1. `473dbca` 오후 — 유입·리텐션·바이럴 풀코스
2. `41bf7c7` 저녁 — 서치콘솔·컨텐츠 SEO·실시간 푸시
3. `21cbe1e` 밤 — streak·기능유도·가입유도·지역채팅
4. `f33720c` 밤 늦게 — 내 위치 마커·팁 빈도·등록 유도·A34 fix
5. (이번 커밋) — 안드로이드 지도 로딩 빈 흰 화면 fix

---

## 31. 갤럭시 폴드6 / A34 지도 빈 흰 화면 수정

- 증상: 특정 안드로이드 기기(Fold6, A34)에서 `/map` 접속 시 지도 영역이 빈 흰 화면으로 렌더
- 원인 추정:
  1. 직전 A34 커뮤니티 상세 overflow 수정 때 `(main)/layout.tsx`에 추가한 `overflow-x-hidden`이 일부 안드로이드 브라우저에서 자식의 `position: absolute` + `100dvh` height 계산을 깨트림 (CSS containment 부작용)
  2. `100dvh`가 Fold6 접은 화면에서 0에 가까운 값으로 계산되는 기기 이슈
- 수정:
  - `(main)/layout.tsx`에서 `overflow-x-hidden` 제거 (루트 레벨 적용 롤백)
  - 지도 페이지 루트에 `minHeight: calc(100vh - 5rem)` 폴백 추가 (dvh 부정확 대비)
  - 지도 컨테이너(`mapContainerRef`)에 `minHeight: 400` 추가 (부모 계산 실패해도 보이도록)
  - `/community/[id]` 루트에만 `overflow-x-hidden` 지역 적용 (A34 커뮤니티 밀림 방어는 유지)
- 미확정 추가 원인:
  - 카카오 개발자 콘솔 "플랫폼 → Web"에 `https://dosigongzon.com` 등록 여부 (모든 모바일 유저에 영향 가능). 유저 확인 요청 남김

---

하루에 커밋 5건, 배포 15+회. 대부분 성공. 이제 정말 잔다.
