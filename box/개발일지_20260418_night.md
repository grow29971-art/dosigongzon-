# 개발일지 2026-04-18 야간

## 오늘 추가한 것 요약
4/18 하루에 대규모 기능 추가. 70개 이상 파일 변경/신규.

**리텐션 5종 완성** · **SEO 풀셋업** · **고양이 상세페이지 + 동적 OG** · **관리자 대시보드 분리** · **알림 확장** · **로그인 가드** · **버그픽스 3건** · **더미 데이터 대량 추가**

---

## 1. 활동 지역 (당근마켓 스타일)

### DB
- `user_activity_regions(user_id, slot, name, lat, lng, radius_m, is_primary)` — 유저당 최대 2곳
- 유니크 부분 인덱스로 `is_primary=true` 한 개 보장
- `box/supabase_activity_regions_migration.sql`

### 기능
- `/mypage/activity-regions` 설정 페이지 (Kakao 지도 + 반경 칩 + 검색)
- 지도에 Circle 오버레이 + 탭 클릭 시 해당 지역으로 카메라 이동
- **고양이는 반경 밖도 전부 표시** (숨기지 않음 — 학대 경보 놓치지 않도록)
- 홈 "내 동네 소식" · 커뮤니티 "내 동네만" 필터

---

## 2. 로그인 에러 진단 시스템

### DB
- `auth_error_logs(provider, stage, error_code, error_desc, user_agent, url, referrer)` — 관리자만 읽기
- `box/supabase_auth_error_logs_migration.sql`

### 로깅
- OAuth callback: `?error=...&error_description=...` 돌아오면 자동 기록
- exchange 실패 기록
- 클라이언트 `/api/auth/log-error` 로 signInWithOAuth 실패 기록

### 친절한 가이드
- `lib/auth-errors.ts` — KOE205/redirect_uri_mismatch/access_denied 등 10+ 패턴을 한국어 메시지로
- 에러 코드 원문을 monospace로 노출 (유저 스크린샷 지원용)

### 매직링크 폴백
- `signInWithOtp({ email })` — 비밀번호 없이 메일 링크로 로그인
- 시크릿 모드/쿠키 차단 환경 최후의 수단

---

## 3. SEO 풀셋업

### 루트 메타데이터 (`app/layout.tsx`)
- `metadataBase`, `title.template`, 16개 키워드
- OG / Twitter / robots / canonical
- Apple Web App / manifest / favicon
- **JSON-LD**: Organization + WebSite + SearchAction
- Google verification: `GYsqQQsXSHohScWs04azTgn87IS7JS0SY07hirxXp-8`
- Naver verification: `58daf8950fc151db07a0b92aded034390411475c`

### 파일
- `app/sitemap.ts` — **동적** (고양이 5000개까지 포함, 1시간 revalidate)
- `app/robots.ts` — `/api /admin /mypage /community /neighborhood /messages` 차단
- `app/opengraph-image.tsx` — 루트 OG (1200×630, 브랜드 그라디언트)
- 페이지별 layout.tsx: map/community/protection/hospitals/shelters/login/signup/neighborhood

### 등록 완료
- 구글 서치 콘솔 + sitemap 제출 + 주요 URL 색인 요청 5개
- 네이버 웹마스터도구 + sitemap 제출 + 웹페이지 수집 요청 5개

---

## 4. 고양이 상세페이지 + 동적 OG

### `/cats/[id]`
- 서버 컴포넌트로 `generateMetadata` 지원
- 사진 커버 + 이름/동 + 뱃지 + 설명
- 좋아요 · 돌봄 일지 · 댓글 카운트 카드
- **JSON-LD Article** 스키마 (구글 검색 썸네일 ↑)
- 정확 좌표는 노출 안 함

### `/cats/[id]/opengraph-image.tsx`
- 좌측 고양이 사진 / 우측 이름·동·설명·응원 카운트
- 카톡 공유 시 1200×630 카드 자동 생성

### 지도 카드
- 💬 카카오톡 공유 (노란색)
- 📤 일반 공유 (Web Share API / 복사 폴백)

---

## 5. 관리자 대시보드 분리

### `/admin`
- 마이페이지에 길게 나열되던 메뉴 7개를 **대시보드 단일 진입점**으로 교체
- 다크 헤더로 일반 페이지와 구분
- **통계 카드 6개**: 가입자 · 고양이 · 글/댓글 · 방문자 · 미처리 신고·문의 · 7일 로그인 실패
- 메뉴 그리드: 신고·문의 / 가입자 / 로그인 에러 / 뉴스 / 병원 / 약품 / 푸시
- 미처리 뱃지 숫자 표시
- `lib/admin-stats.ts` — 통계 집계

### `/admin/auth-errors`
- 최근 1/7/30일 필터
- TOP 에러 코드 + provider 별 breakdown
- 상세 로그 리스트 (UA 파서로 "카카오톡 인앱" / "Chrome iOS" 등)
- 개별 로그 삭제 / 기간 전체 삭제

### `/admin/users` 개선
- **Provider 뱃지** (🟦 구글 / 🟨 카카오 / ✉️ 이메일)
- 정렬 토글 (최신 가입순 ↔ 오래된순)
- Provider 필터 칩
- 정지/해제 버튼 (프롬프트로 일수·사유 받음, 0=영구)
- `box/supabase_list_all_users_with_provider.sql`

---

## 6. 리텐션 5종

### ① 🔥 돌봄 streak
- `lib/streak-repo.ts` — KST 기준 연속 일수 + 이번 주 카운트
- 홈 상단 카드: 🔥 이모지 (1→2→3개 증가), 이번 주 진행률 바, 월~일 도트

### ② 🏆 주간 랭킹·리더보드
- `lib/leaderboard-repo.ts`
- 이번 주 돌봄 왕 TOP 3 (🥇🥈🥉 메달)
- 이번 주 인기 고양이 TOP 5 (좋아요×3 + 댓글×2)

### ③ 🟢 실시간 동네 피드
- `lib/live-feed-repo.ts`
- 최근 24h 돌봄·댓글 통합 피드
- 주 활동 지역 반경 내만 필터
- LIVE 펄스 뱃지, 60초 폴링

### ④ 🎂 오늘의 기념일
- `lib/anniversaries-repo.ts`
- 등록일과 월/일이 일치하는 고양이들
- "만난 지 N주년 🎉" — 연수별 색상 그라디언트

### ⑤ 👥 캣맘 팔로우
- `user_follows(follower_id, following_id)` + RLS
- `lib/follows-repo.ts` · `<FollowButton />` 컴포넌트
- 배치: `/cats/[id]` 돌보미 옆 / `/messages` 헤더
- 알림에 팔로우 유저 24h 돌봄 활동 통합

---

## 7. 알림 센터 확장

### 카운트 (홈 헤더 벨 뱃지)
- 읽지 않은 DM
- 내 고양이 24h 댓글·돌봄
- **내 커뮤니티 글 24h 댓글** (신규)
- **내 문의 7일 내 처리됨** (신규)
- **팔로우 유저 24h 돌봄** (신규)

### 알림 리스트
- 6가지 타입 색상·아이콘 구분
- 타입별 자연스러운 링크 (글 → `/community/[id]`, 고양이 → `/cats/[id]`, 문의 → `/mypage`)

### 홈 헤더
- 다크모드 옆에 🔔 Bell 버튼
- 핑크-빨강 뱃지 (99+ 처리)
- 30초 폴링

---

## 8. 고양이 좋아요 ❤️

### DB
- `cat_likes(cat_id, user_id)` + `cats.like_count` 컬럼 + 트리거로 자동 동기화
- `box/supabase_cat_likes_migration.sql`

### UI
- 지도 고양이 카드 하단 하트 버튼 (핑크 그라디언트)
- 낙관적 업데이트 + 서버 동기화 + 롤백
- 마이페이지 "응원하는 고양이" 가로 스크롤 섹션

---

## 9. 커뮤니티 강화

### 동적 metadata + OG
- `/community/[id]/layout.tsx` — 글 제목·카테고리·지역·발췌 자동
- `/community/[id]/opengraph-image.tsx` — 카테고리 색상바 + 제목 + 발췌 + 작성자/지역/좋아요 + 첨부 이미지

### 공유 버튼
- 💬 카톡 공유 (Kakao SDK)
- 📤 일반 공유 (Web Share API)

### 로그인 가드
- `/community`, `/community/[id]`, `/community/category/[cat]`, `/neighborhood`
- 비로그인 시 `<LoginRequired />` 화면
- `?next=...` 로 원래 경로로 복귀
- robots.txt + sitemap에서 제외

### 카페 연동
- 커뮤니티 하단 "함께하는 커뮤니티" 카드에 네이버 카페 "냥이네" 링크
- JSON-LD Organization.sameAs 에도 추가

---

## 10. 버그 픽스

### 동네 채팅 크로스-구 유출
- `region_2depth_name`만 저장 → 서울 중구/인천 중구/부산 중구 공유되던 문제
- 수정: `region_1depth_name + region_2depth_name` ("인천 남동구")
- 구 이동 시 이전 구 메시지가 state에 남던 버그도 수정 (`setChatMessages([])` + `firstFetchDone` 플래그)

### AddCatModal 동네 타이핑 버그
- "변경" 누르고 한 글자 치면 바로 표시모드로 돌아가던 문제
- `editingDong` 별도 state로 해결

### 경기도 구리시 누락
- `/protection/district-contacts`에 15개 시·군 추가 (구리·동두천·과천·오산·군포·의왕·하남·이천·안성·양주·포천·여주·연천·가평·양평)

---

## 11. 카카오 로그인 제거

### 이유
- `invalid_scope: account_email` 에러 지속 발생
- 카카오 비즈앱 승인 필요한데 사업자등록증 없음
- 유저 경험 해친다고 판단

### 조치
- 로그인 페이지에서 노란 카카오 버튼 제거
- `handleSocialLogin` 을 google 전용으로 축소
- 구글 + 이메일 + 매직링크 3종으로 운영

---

## 12. 더미 데이터 대량 추가

### 고양이 (41마리)
- `box/supabase_cats_dummy_40_seed.sql` — 41마리 시드
- `box/supabase_cats_dummy_40_photos_update.sql` — Unsplash 실제 사진 URL 매핑
- `box/supabase_cats_dummy_40_spread_nationwide.sql` — 전국 17개 권역 분산
- `box/supabase_cats_dummy_40_rename.sql` — 지역명 → 길고양이 애칭(까망이·젤리·꿀떡·루루 등)

### 커뮤니티 글 (58개)
- `supabase_posts_dummy_20_seed.sql` — 초기 20개 (카테고리 4×5)
- `supabase_posts_dummy_30_seed_part2.sql` — 추가 30개
- `supabase_posts_dummy_8_seed_part3.sql` — 임보 3·입양 1·중고마켓 4
- 글들 사이 스토리 연속성 (예: "소심한 치즈 2살" 글에 "입양완료" 후기 이어짐)

---

## 13. 기타

- **Vercel Analytics + Speed Insights** 추가
- **활동 지역** 지도 탭·필터·마이페이지 링크 통합
- **좋아요한 고양이** 섹션 마이페이지 추가
- **고양이 검색·필터** (이름/태그/TNR 필요/건강 주의/학대 경보)
- **쪽지 헤더 팔로우 버튼**
- **활동지역 없는 유저용** 홈 CTA

---

## 14. 변경 파일

| 카테고리 | 파일 수 |
|---------|--------|
| 신규 page.tsx / layout.tsx | 18 |
| 신규 lib repo | 11 |
| 신규 컴포넌트 | 2 (FollowButton, LoginRequired) |
| 수정 | 15 |
| 신규 SQL | 15 |
| 개발일지 | 이 파일 |
| **합계** | **70+** |

---

## 15. 배포 전 DB 실행 체크리스트

Supabase SQL Editor에서 아직 실행 안 했으면 순서대로:

1. `box/supabase_activity_regions_migration.sql`
2. `box/supabase_auth_error_logs_migration.sql`
3. `box/supabase_cat_likes_migration.sql`
4. `box/supabase_user_follows_migration.sql`
5. `box/supabase_list_all_users_with_provider.sql`
6. (선택) `box/cleanup_area_chats.sql` — 옛날 동네채팅 데이터 정리
7. (선택) 더미 데이터 시드들

모두 RLS 기반 안전. service_role 환경에서만 실행 가능하도록 설계됨.

---

## 16. 다음에 할 만한 것

- **쉼터 지도** — 유저가 만든 겨울집·급식소 공유
- **공동 돌봄 그룹** — 여러 명이 하나의 고양이 관리
- **사진 여러 장 업로드** — 현재 1장 제한
- **PWA 설치 프롬프트** — 홈 화면 추가 배너
- **유저 프로필 페이지** `/users/[id]` (팔로우 목록 포함)
- **문의 상세 페이지** — 알림에서 inquiry 클릭 시 대화식 UI

---

끝.
