# 2026-05-15 개발일지

출시 D-5(5/20 신청 마감). 광고로 가입자 +163명 모았으나 retention 정체. "큰 변경 자제·핫픽스만" 모드에서 마케팅/retention 핫픽스 집중.

## 한 일

### 1. Supabase RPC 적용 점검·핫픽스
- `box/supabase_ranking_function_migration.sql` Supabase Dashboard 수동 실행 완료 (`get_top_caretakers`, `get_caretaker_rank`)
- anon REST API로 `get_top_caretakers` 호출 → http=200, 1위 똑똑한파수꾼560(score 250), 2위 박규리(score 등) 정상 반환 확인
- 코드의 12개 RPC 중 정의 위치 매핑:
  - `cat_count_by_region` → 적용됨 (200 OK)
  - `get_top_caretakers`, `get_caretaker_rank` → 미적용이었음 → 적용 완료
  - 나머지 9개(`list_all_users`, `shorts_admin_stats`, `increment_daily_visit`, `increment_tip_view`, `apply_invite_code`, `post_view_inc`, `post_vote_update`, `use_streak_freeze`, `increment_short_like`) → anon 호출만으론 권한 분리 못 함. service_role 키가 새 `sb_secret_*` 포맷이라 invalid 응답. 사용자가 Dashboard `pg_proc` 조회 SQL로 검증 필요(가이드 메시지 제공).

### 2. 환경변수 trim·따옴표 핫픽스 (a633f34)
- `.env.local` 점검 결과 두 가지 오염:
  - **VAPID_PUBLIC_KEY** 끝에 literal `\n` → `atob()` 깨져 푸시 구독 silent fail 가능
  - **KAKAO_MAP_KEY** 양쪽 `"` 포함 → `Kakao.init()` 실패 가능
- `app/components/PushSubscriber.tsx`, `app/components/PushOptInCard.tsx`: `process.env.NEXT_PUBLIC_VAPID_PUBLIC_KEY?.trim()`
- `lib/kakao-share.ts`: trim + `^["']|["']$` 양쪽 따옴표 제거
- 메모리 `project_dosigongzon_env_trim` 업데이트 — KAKAO 따옴표 케이스 + VAPID 케이스 추가

### 3. HomeAuthed empty state 강화 (202220f)
**문제**: 우리 동네 등록 고양이 0인 사용자(160명 분산 → 동 단위 1-2명)는 "아직 우리 동네에 등록된 고양이가 없어요 🐾" 한 줄만 보고 닫음. retention 최대 leak.

**변경**: `app/components/HomeAuthed.tsx:1352-1366` 13줄 → 90줄로 확장
- 🐾 아이콘 + "{primaryRegion.name}의 첫 등록자가 되어주세요" CTA
- 이미 fetch된 `popularCats.slice(0, 4)` 재활용 (신규 fetch 없음) — "다른 동네 인기 고양이 둘러보기"
- 지도 진입 primary 버튼

### 4. HomeLanding 가입 CTA 강화 (48fd6a4)
**문제**: 우측 ghost CTA "돌봄 시작하기"가 가입 의미가 약함 (transparent 배경 + 단순 텍스트).

**변경**: `app/components/HomeLanding.tsx:193-205`
- `background: transparent` → `rgba(196,126,90,0.09)` (살짝 채움)
- border 색 진하게
- `<Sparkles size={13}>` 아이콘 추가
- "10초 가입 · 무료" 부제 추가

### 5. HomeLanding 활동 캣맘 TOP 3 사회적 증명 (cd5e7a7)
**핵심 마케팅 무기**: 광고로 들어온 비로그인 방문자에게 "실제 활동 중인 사람이 있다"는 가장 강력한 증거.

**변경**: `app/components/HomeLanding.tsx`
- import 추가: `unstable_cache`, `ChevronRight`, `Trophy`, `getTopCaretakersServer`, `RankingRow`
- `getCachedTopCaretakers` — 10분 캐시(egress 절감), `landing-top-caretakers` 태그
- 위치: 1000명 이벤트 배너 직후, 감성 인용 직전 (가입 동기 최고조 구간)
- 1-3위 메달 그라데이션 (금/은/동) + 닉네임 + `🐱 N마리 · 💛 돌봄 N회 · N점`
- "전체 랭킹 보기" CTA → `/ranking`

### 6. retention 진단 SQL 작성
- `box/supabase_retention_diagnosis.sql` (9블록): 가입 추이, 활성화 전환율, D1/D3/D7/D14/D30 retention, DAU/WAU/MAU 분포, 동(region) 활성도, 푸시 구독률, 유령 가입자 비율, 가입 시간대
- `box/supabase_retention_core3.sql` (3블록만 추출): ④ retention, ⑥ region 활성도, ⑧ ghost 비율
- 사용자가 결과를 채팅에 못 옮기는 문제 발생(스크린샷 너무 작음, 표 복사 안 됨) → 분석 보류

## 그래서 어떻게 됐나
- 광고 클릭자가 보는 새 흐름: HomeLanding 사회적 증명 강화 → CTA 가입 명시 → TOP 3 활동가 노출 → 가입 → HomeAuthed empty state 강화 → 다음 행동 명확
- 빈 동네 첫 화면 leak point 차단
- 살아있는 커뮤니티의 실제 활동가 노출 → 신뢰 ↑

## 다음 액션 (사용자 직접)
1. **광고 집중 지역 결정** — Supabase Dashboard `cats` 테이블 `region` 필터로 상위 3개 동 → 그곳에 광고 80% 예산 집중
2. **운영진 시드** — 광고 집중 동네에 cat 5-10마리·꿀팁 댓글 직접 등록 (cold start 해법)
3. **retention SQL 재시도** — 표 복사 어려우면 결과 5개 숫자만 텍스트로 (D7%, ghost%, 1-3위 동네명·활동가수)
4. **Vercel env 정리** — `KAKAO_MAP_KEY` 따옴표·기타 `NEXT_PUBLIC_*` 끝 `\n` 제거 후 재배포 (코드 trim 방어 있지만 청결성)

## 메모
- 오늘 변경한 마케팅 핫픽스 3건은 모두 작은 변경으로 큰 retention/conversion 효과 기대. 출시 D-5 안전 범위
- HomeLanding은 손볼 곳 거의 없을 정도로 잘 짜여있음 (SocialProofStrip, 3단계 가이드, 1000명 이벤트, 감성 인용 + 이제 TOP 3)
- HomeAuthed의 TodayChecklist는 Zeigarnik + 3축 균형으로 매일 retention 도구 이미 잘 갖춰짐
- PushOptInCard 위치는 의도적 하단 lazy(LCP 위해) — 손대지 않음

---

## 오후 추가 작업

### 7. robots.ts에 /sentry-example-page 차단 (751ad11)
스모크 테스트에서 Sentry 디버그 페이지가 robots 차단 안 됨 발견. SEO 위생 정리.

### 8. 채팅 일일 정리 + 안내 배너 + cron 매일 변경 (819f95f)
사용자 요청: "전체채팅/구단위채팅에 '하루에 한 번 채팅이 지워집니다' 안내 추가."
- map page 채팅 헤더 아래 🕓 안내 배너
- vercel.json cleanup-area-chats schedule `0 19 * * 0`(주간) → `0 19 * * *`(매일)
- cron route 주석 동기화
이후 카피 변경(d4e7f07): "정리돼요" → "정리됩니다 서버비 감당이안돼서요 ㅠㅠ" 솔직 어필.

### 9. 동네 채팅 일시 비활성·전체 채팅만 노출 (cca2d0f → 4b44d98)
사용자 요청: 가입자 163명을 25구로 분산하면 동네 채팅당 6명 → 빈 채팅 이탈 유발. 전체 채팅에 모이게.
- map FAB: 동네 채팅 버튼 JSX 주석으로 보존
- MapChatGuideModal: 동네 채팅 카드 `{false && (...)}` 토글 패턴으로 보존 (복원 친화)
- privacy 정책: "매주 월요일" → "매일 04:00", "동네 채팅" → "전체 채팅" 일관화
- 메모리 `project-dosigongzon-area-chat-disabled` 추가: 복원해야 할 3곳 명시.

### 10. Meta Pixel 통합 (b36776d)
인스타 광고 효율 낮은 진짜 원인 — 사용자가 "홍보하기"(boost) 광고만 돌려서 픽셀 없음 → Meta가 학습할 데이터 0개 → 한 달 +3명.
- `lib/meta-pixel.ts` — trackPixelEvent·trackPixelCustom·trackPixelOnce. env trim/따옴표 방어.
- `app/components/MetaPixel.tsx` — base code + usePathname/useSearchParams로 SPA PageView 자동 발사.
- ConsentManager에 통합 — 쿠키 동의 시에만 로드 (PIPA/GDPR 안전).
- signup OAuth redirect 직전 `fbq("track", "Lead")` 발사.
- welcome 페이지 진입 시 userId 기반 dedup `CompleteRegistration` 발사.
- privacy 페이지 Meta Platforms 제3자 항목 추가.
- Vercel env `NEXT_PUBLIC_META_PIXEL_ID` production 추가.

### 11. Pixel ID 정정 (849→1611, 강제 재배포)
사용자가 Meta 광고 캠페인 만들면서 다른 픽셀 ID 자동 잡혔음(`1611645713230757`). 처음 사용자가 알려준 ID(`849440401317822`)와 불일치 발견.
- Vercel env rm/add로 ID 교체
- 첫 재배포에서 픽셀 ID가 JS chunk에 인라인 안 됨 → Vercel이 캐시 빌드 사용한 듯
- `vercel --prod --yes --force`로 캐시 무시 재빌드 → `/_next/static/chunks/0hk9c~v_v935_.js`에 `1611645713230757` 인라인 확인
- 메모리 `project-dosigongzon-meta-pixel` ID 갱신.

### 12. Meta 광고 캠페인 생성·게시 (사용자 직접 액션 가이드)
사용자가 Meta Business Suite 가입 → Events Manager에서 픽셀 생성 → Ads Manager에서 전환 캠페인 생성:
- 캠페인: "도시공존 가입 전환" (Sales 목표)
- Ad set: Conversion event = **CompleteRegistration**, 서울/20-50/Cats 관심사 타게팅, $25/일 × 9일
- 첫 게시 시 URL 누락 에러(#2061015) → Fix errors로 해결
- 둘째 시도 게시 성공 → Processing → **Active 전환 확인** ✅

### 13. 도구 산출물
- `box/smoke_test_live.mjs` — puppeteer 모바일 모드로 12개 라우트 console error·응답시간 검사
- `box/supabase_retention_diagnosis.sql` (9블록), `box/supabase_retention_core3.sql` (④⑥⑧만), `box/supabase_ad_target_region.sql` (1행 결과)

## 출시 D-5 종합 점수
- ✅ 빌드/타입/린트/보안/smoke test 모두 통과
- ✅ 마케팅 핫픽스 5건 라이브 (empty state·CTA·TOP 3 캣맘·robots·서버비 카피)
- ✅ Meta 픽셀 + 광고 캠페인 활성화 (인스타 boost 한계 돌파)
- 🟡 LCP — /areas 8s, /protection 7s, /ranking 7s (출시 후 최적화)
- 🟡 일부 cat 사진 storage 404 (한 사용자 분량, 영향 제한적)
- 🔴 retention SQL 결과 미수령 (사용자 표 복사 어려움) — 광고 데이터로 보완 가능

## 다음 액션 (사용자 직접) 갱신
1. **본인 가입 시뮬레이션** — 시크릿 창에서 가입 1건 → `CompleteRegistration` 이벤트 1건 발사 → 광고 알고리즘 학습 즉시 trigger
2. **지인 가입 2-3건** — 이벤트 누적되면 학습 가속
3. **24-48h 후 광고 효과 모니터링** — Events Manager 이벤트 수, Ads Manager Cost per result
4. **1주 후 5/22** — 가입자 추이 비교, 광고 ROI 확인 후 예산·타게팅 조정
5. **광고 집중 지역·운영진 시드·외부 등재 메일** — 코드 외 운영 액션 (`box/seo_외부유입_운영가이드.md`)
6. **Vercel env 청소** — `KAKAO_MAP_KEY` 따옴표·`NEXT_PUBLIC_*` 끝 `\n` 정리 (코드 trim 방어 있지만)

## 메모리 갱신
- 추가: `project-dosigongzon-deploy`, `project-dosigongzon-area-chat-disabled`, `project-dosigongzon-meta-pixel`
- 업데이트: `project-dosigongzon-env-trim` (KAKAO 따옴표·VAPID 케이스 추가)

## 총합
**커밋 10건 / 배포 6회 (`a633f34` → `b36776d`, force rebuild 1회)** — 모두 dosigongzon.com 라이브.
