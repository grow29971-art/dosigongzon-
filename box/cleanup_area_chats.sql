-- ══════════════════════════════════════════
-- 동네 채팅 마이그레이션 — 구 이름 → "시·도 + 구" 로 전환
-- 실행 위치: Supabase SQL Editor
-- ⚠ Chrome 번역 OFF
-- ══════════════════════════════════════════
-- 배경: 기존에는 `area` 칼럼에 "남동구", "중구" 등 구 이름만 저장해
-- 서울 중구·인천 중구·부산 중구가 모두 같은 채팅방에 묶였음.
-- 앱 업데이트 후 "인천 남동구", "서울 중구" 형식으로 저장됨.
--
-- 이 파일은 기존 메시지를 정리하는 2가지 옵션을 제공합니다.
-- 상황에 맞는 하나만 주석 해제해서 실행하세요.
-- ══════════════════════════════════════════

-- ───────────────────────────────────────────
-- 옵션 A (권장): 기존 메시지 전부 삭제 — 깨끗한 시작
-- ───────────────────────────────────────────
-- DELETE FROM public.area_chats;


-- ───────────────────────────────────────────
-- 옵션 B: 남동구 주민이 대부분이면 "인천 XX구" 로 접두 추가
--   - 한 도시 위주로만 사용 중이었다면 유용
--   - 여러 도시 메시지가 섞여 있으면 이 방식은 부정확함
-- ───────────────────────────────────────────
-- UPDATE public.area_chats
-- SET area = '인천 ' || area
-- WHERE area IN (
--   '남동구','부평구','미추홀구','연수구','계양구','서구','동구','중구','강화군','옹진군'
-- ) AND area NOT LIKE '% %';


-- ───────────────────────────────────────────
-- 참고: 현재 area 분포 확인
-- ───────────────────────────────────────────
-- SELECT area, COUNT(*) FROM public.area_chats GROUP BY area ORDER BY COUNT(*) DESC;

-- 끝.
