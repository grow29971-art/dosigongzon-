# 도시공존 개발일지 — 2026년 4월 11일(야간) ~ 4월 13일

## 작업 개요
보안 전면 감사 → 기능 확장 → 전국 데이터 수집 → Vercel 프로덕션 배포까지 연속 3일 세션.

## 커밋
```
2ec0ff0 feat: 지도 마커 분류 + 병원/약국 오버레이 + 관리자 페이지 + 약품 가이드
548faba feat: 보안 강화 + 커뮤니티 DB 이전 + 타이틀 시스템
```
- 총 **68 파일 변경**, **5,727줄 추가**

---

## 1. 보안 강화 (4/11 야간)

### Task 1 — 관리자 권한 이중 방어
- `lib/admin-guard.ts` 신규: `requireAdmin()` + `NotAdminError`
- 15개 admin 전용 함수에 코드 레벨 가드 추가 (support-repo, news-repo, hospitals-repo)
- RLS + 코드 이중 방어 → 정책 실수 시에도 안전망

### Task 2 — API 라우트 인증
- `/api/auth/callback`: open-redirect 차단 (`//`, `/\` 방어)
- `/api/chat`: 로그인 필수 + 유저당 10req/분 레이트리밋 (인메모리)
- `/api/weather`: 로그인 필수 + lat/lon 숫자 범위 검증 + ip-api HTTPS화
- `/api/models`: admin 전용 + 에러 debug 필드 제거

### Task 3 — RLS 실제 적용 검증
- `box/supabase_rls_verify.sql` 작성: 6개 섹션 검증 스크립트
- **🔥 실제 사고 발견**: `supabase_suspensions_migration.sql` 미실행 상태
  - `user_suspensions` 정책 0개 → 정지 시스템 전체 우회됨
  - `cats_delete_admin`, `cat_comments_delete_admin` 누락
  - 마이그레이션 재실행으로 복구

### Task 4 — 입력 검증 / XSS
- `lib/url-validate.ts` 신규: `sanitizeImageUrl`, `sanitizeHttpUrl`
- 쓰기 경로: `createCat`, `createComment`, `createNews`, `updateNews`에 URL 검증
- 렌더 경로: `map/page.tsx` innerHTML의 `photoUrl` XSS 차단, `news/[id]` href 방어
- **XSS 취약점 1건 제거** (map 마커 photo_url 탈출)

### Task 5 — 보안 헤더
- `next.config.ts`에 6개 헤더 추가: CSP, X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy, HSTS
- CSP: Kakao Maps + Supabase allowlist, unsafe-inline 허용 (React 런타임 필요)

### Turnstile 봇 방어
- `app/components/TurnstileWidget.tsx`: Cloudflare Turnstile 위젯 (graceful fallback)
- `app/api/turnstile-verify/route.ts`: 서버 검증
- 가입 페이지(`/signup`)에 통합

---

## 2. 스팸/장난 방지

### 고양이 등록 제한
- `box/supabase_cats_write_limits_migration.sql`: 가입 24h 유예 + 하루 1마리
- `cats-repo.ts`: 클라이언트 사전 체크 (DB RLS와 이중 방어)
- AddCatModal에 "🐾 하루에 한 마리씩 등록 가능합니다" 안내 문구

### 신고 누적 자동 숨김
- `box/supabase_auto_hide_reported_migration.sql`: 3건 이상 → hidden=true 트리거
- `cats`, `cat_comments`, `post_comments`에 `hidden` 컬럼 + RLS 갱신

### 경보 레벨 게이트
- `box/supabase_alert_level_gate_migration.sql`: alert 댓글은 레벨 1+ 필수
- `cats-repo.ts` createComment에 클라이언트 체크

---

## 3. 커뮤니티 DB 이전

### localStorage → Supabase posts 테이블
- `box/supabase_posts_migration.sql`: posts 테이블 + RLS 7개 + auto_hide + comment_count 트리거
- `lib/posts-repo.ts` 신규: `listPosts`, `getPostById`, `createPost`, `deletePost`, `formatRelativeTime`
- 5개 페이지 마이그레이션: community, write, [id], category/[cat], neighborhood
- 사진 첨부 (최대 4장, Supabase Storage)

### 좋아요/싫어요/조회수
- `box/supabase_posts_counters_migration.sql`: dislike_count + RPC 함수 (post_view_inc, post_vote_update)
- 카테고리 페이지에서 낙관적 UI 업데이트 + 실패 시 롤백
- 투표 버튼 영역 stopPropagation 강화 (Link 내부 클릭 전파 방지)

### 커뮤니티 인기글
- 최근글 → 인기글: `좋아요×3 + 댓글×2 + 조회수` 가중 정렬

### 카테고리별 공지사항
- pinned 글을 공지 섹션으로 분리 (📢 Megaphone 아이콘)
- 포스트 상세에서 admin "공지 고정/해제" 버튼
- `box/supabase_community_notices_seed.sql`: 5개 카테고리 공지 시드

---

## 4. 타이틀(업적) 시스템

### 12개 타이틀
- `lib/titles.ts`: 등록(3) + 기록(3) + 경보(3) + 공감(3) = 12개
- 마이페이지 업적 섹션: 3열 그리드, 잠금/해제 시각화, 진행률 바

### 장착 시스템
- 마이페이지에서 해제된 타이틀 탭 → 장착 (Supabase user_metadata)
- `app/components/TitleBadge.tsx`: 작성자 이름 옆 타이틀 뱃지
- 글/댓글 작성 시 `author_title` 스냅샷 저장
- 표시: 커뮤니티 카드, 포스트 상세, 포스트 댓글, 지도 고양이 댓글

### 레벨 표시
- `box/supabase_author_level_posts_migration.sql`: posts, post_comments에 author_level 컬럼
- 커뮤니티 카드/상세/댓글에 Lv.N 뱃지 표시

---

## 5. 지도 기능 확장

### GPS 자동 중심
- `navigator.geolocation` 접속 시 요청 → 허용 시 지도 중심 이동
- 거부/실패 시 기본 중심(인천 남동구) 폴백

### 학대 경보 / 시민 참여 카드
- 접이식 카드: 경보 있으면 빨강, 없으면 초록
- 법적 배경 + 목격 시 행동 3단계 + 원탭 신고(112, 1577-0954)

### 마커 색상 분류
| 태그 | 색상 |
|---|---|
| 학대 경보 | 🔴 빨강 + 펄스 |
| TNR 필요 | 🟠 주황 |
| TNR 완료/이어팁 | 🟢 초록 |
| 기본 | 🟤 갈색 |

### 병원/약국 오버레이
- 병원: 🟢 초록 사각 ✚ 마커 (상시 표시)
- 약국: 🟣 보라 원형 💊 마커 (상시 표시)
- Kakao Geocoder로 주소 → 좌표 자동 변환
- 마커 탭 → 상세 카드 (이름/주소/전화/영업시간/메모 + 전화하기 버튼)
- 약국/병원 테마 색상 자동 분기

---

## 6. 전국 데이터

### 전국 더미 고양이 (31마리)
- `box/supabase_cats_nationwide_seed.sql`: 서울~제주 주요 도시 분산

### 전국 동물약국
- 대한동물약국협회(anipharm.net) 데이터 수집 (6개 병렬 에이전트)
- 서울 207 + 부산 119 + 대구 69 + 대전 28 + 울산 39 + 광주 30 + 경기 228 + 강원 20 + 충청 53 + 경상 107 + 전라 55 + 제주 28 = **~926개** 수집
- `box/supabase_animal_pharmacies_seed.sql` (서울 69건)
- `box/supabase_animal_pharmacies_nationwide.sql` (전국 107건)
- 동물약국 자동 동기화 Cron: `/api/cron/sync-pharmacies` + `vercel.json` (매일 03:00 KST)

### 투데이 팩트 확장
- CAT_FACTS 7개 → 30개

---

## 7. 관리자 기능

### 가입자 관리 (/admin/users)
- `list_all_users()` DB 함수 (SECURITY DEFINER, admin 체크)
- 전체 회원 조회, 이메일/닉네임 검색, 정지 현황 표시

### 뉴스 이미지 업로드
- URL 텍스트 입력 → 파일 선택 + 미리보기 + 삭제 UI
- `uploadNewsImage()`: cat-photos 버킷 공유

### 약품 가이드 관리 (/admin/pharmacy-guide)
- `box/supabase_pharmacy_guide_migration.sql`: pharmacy_guide_items 테이블
- `lib/pharmacy-guide-repo.ts`: CRUD + 이미지 업로드
- 관리자 페이지: 제품 추가/수정/삭제, 이미지 업로드, 색상·정렬 설정

---

## 8. 배포

### Vercel 프로덕션
- URL: **https://city-amber-omega.vercel.app**
- 환경변수: NEXT_PUBLIC_SUPABASE_URL/ANON_KEY, KAKAO_MAP_KEY, GOOGLE_AI_KEY, OPENWEATHERMAP_KEY, TURNSTILE keys
- Cron: `/api/cron/sync-pharmacies` (매일 UTC 18:00 = KST 03:00)

---

## SQL 마이그레이션 목록 (이번 세션)
1. `supabase_rls_verify.sql` — RLS 검증 스크립트
2. `supabase_cats_write_limits_migration.sql` — 등록 제한
3. `supabase_auto_hide_reported_migration.sql` — 자동 숨김
4. `supabase_alert_level_gate_migration.sql` — 경보 레벨 게이트
5. `supabase_author_title_migration.sql` — author_title 컬럼
6. `supabase_author_level_posts_migration.sql` — author_level 컬럼
7. `supabase_posts_migration.sql` — posts 테이블
8. `supabase_posts_counters_migration.sql` — 카운터 RPC
9. `supabase_community_notices_seed.sql` — 공지 시드
10. `supabase_cats_nationwide_seed.sql` — 전국 고양이 시드
11. `supabase_animal_pharmacies_seed.sql` — 서울 약국 시드
12. `supabase_animal_pharmacies_nationwide.sql` — 전국 약국 시드
13. `supabase_hospitals_latlng_migration.sql` — 병원 좌표 컬럼
14. `supabase_list_users_function.sql` — 유저 목록 함수
15. `supabase_pharmacy_guide_migration.sql` — 약품 가이드 테이블

---

## 신규 파일 요약
| 파일 | 용도 |
|---|---|
| `lib/admin-guard.ts` | requireAdmin() 헬퍼 |
| `lib/url-validate.ts` | URL 검증/소독 |
| `lib/posts-repo.ts` | 커뮤니티 게시글 CRUD |
| `lib/titles.ts` | 타이틀(업적) 시스템 |
| `lib/pharmacy-guide-repo.ts` | 약품 가이드 CRUD |
| `app/components/TurnstileWidget.tsx` | Turnstile 봇 방어 |
| `app/components/TitleBadge.tsx` | 타이틀 뱃지 |
| `app/api/turnstile-verify/route.ts` | Turnstile 서버 검증 |
| `app/api/cron/sync-pharmacies/route.ts` | 약국 자동 동기화 |
| `app/(main)/admin/users/page.tsx` | 가입자 관리 |
| `app/(main)/admin/pharmacy-guide/page.tsx` | 약품 가이드 관리 |
| `app/(main)/protection/pharmacy-guide/page.tsx` | 약품 가이드 페이지 |
| `vercel.json` | Cron 설정 |
| `scripts/` | 약국 스크래핑 스크립트 |

---

## 발견 및 해결한 사고
1. **정지 시스템 전체 우회** — suspensions 마이그레이션 미실행 → 정지된 유저도 글/댓글 작성 가능 → 재실행으로 복구
2. **XSS 취약점** — map innerHTML의 photo_url 탈출 → sanitizeImageUrl로 차단
3. **API 크레딧 무방비** — chat/weather/models 미인증 → 로그인 필수 + 레이트리밋
4. **open-redirect** — auth/callback의 next 파라미터 → safeNextPath 검증

## 남은 작업
- [ ] Supabase Auth URL Configuration (배포 URL 등록)
- [ ] Turnstile 시크릿 키 로테이션
- [ ] LOCALDATA API 키 발급 → 약국 자동 동기화 활성화
- [ ] npm audit (의존성 취약점)
- [ ] 미실행 SQL 마이그레이션 확인
