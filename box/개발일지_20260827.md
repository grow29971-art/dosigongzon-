# 개발일지 2026-08-27

## 오늘 한 일 요약
**카드·배틀 시스템 전면 폐지** (사장님 지시: "지도 고양이 카드 안 보이게, 데이터도 다 지워줘"). 7/20 배틀 삭제의 완결편 — 커밋 2개 배포(success), 코드 **-2,450줄**. DB 정리 SQL은 사장님 실행 대기(1차 실행이 뷰 의존성 2BP01로 실패 → 수정판 재실행 대기).

| 커밋 | 내용 |
|------|------|
| `fb4d0fd` | 카드·배틀 전면 제거 — 22개 파일, 라우트 7개·컴포넌트·lib 삭제 |
| `6463bf9` | 제거 SQL 수정 — profiles_public 뷰 선재정의로 2BP01 해소 |

---

## 1. 제거 범위
- **지도 시트**: 🃏 카드 탭(돌봄다이어리·커뮤니티만 남김), 등급 인카운터 리본(커먼/레어 뱃지·Lv·도감번호), 카드 플레이버 문구. 사진 링은 테라코타 단색.
- **라우트**: `/mypage/cards`(+`/inventory`), `api/cats/{generate-card, card-synthesis, check-rarity-upgrade, rare-alert, equip-item}`.
- **컴포넌트·lib**: `CatCard.tsx`, `cat-grade(+rules)`, `battle-config`(7/20 "카드 스탯용 유지" 결정 번복 — 카드 생성 자체가 폐지돼 존재 이유 소멸).
- **등록 플로우**: AddCatModal의 카드 생성(Gemini 호출)·희귀 카드 연출 제거 → 등록 빨라짐 + AI 비용 절감.
- **케어 플로우**: 돌봄일지 후 등급 진화 체크 fire-and-forget 제거.
- **코인 상점**: 테두리 코스메틱 10종 삭제 → 케어 간식 3종만. 마이페이지 카드 메뉴·SHOW_CARD_GAME 플래그 삭제.

## 2. 의도적 존치 (지우면 케어가 같이 죽는 것)
- `cats.card_level / card_exp` = **돌봄 레벨** — 이름만 card지 실체는 다마고치 Lv·출석 +50exp·/api/care 레벨업의 원천. 컬럼명 변경은 안 함(마이그레이션 비용 > 이득).
- `profiles.rep_card_cat_id` = 다마고치 대표묘. checkin·set-rep-card는 카드 조건만 제거하고 유지.
- `cats.art_key / art_colors` + backfill-cat-art 크론 = 지도 마커 AI 아트(카드와 무관, Gemini 직접 호출 확인).
- `/collection` 도감 = care_logs 파생 수집(카드 아님), 코인·케어 간식 경제.

## 3. DB 정리 SQL — `box/supabase_card_system_removal_20260827.sql`
- 7/20 배틀 정리 SQL(**한 달간 미실행이었음** — card_battles 실존 프로브로 확인) 내용을 통합한 단일판.
- 배틀 테이블·profiles/cats 배틀 컬럼·카드 정체성 6컬럼·배틀 스탯 8컬럼·테두리(컬럼+user_items 행) 제거.
- **1차 실행 실패(2BP01)**: profiles_public 뷰가 boss_defeats·best_win_streak을 투영 중. 에러 힌트의 CASCADE는 함정(뷰 삭제 = 닉네임/아바타 조회 전멸) — **뷰를 배틀 컬럼 없이 drop→create→grant로 선재정의**하는 0단계를 추가해 해결. 코드가 뷰에서 배틀 컬럼을 안 읽는 것 실측 후 진행.
- 재실행 대기. 실행 후 검증: card_rarity select=42703(삭제됨)·card_level 정상·border_% 아이템 0행·뷰 정상.

## 교훈
1. **컬럼 drop 전 뷰 의존성 확인** — profiles_public처럼 락다운용 뷰가 물고 있으면 2BP01. CASCADE는 절대 금지, 뷰 재정의가 정답.
2. 기능 폐지 때 "이름이 같은 것"과 "실체가 같은 것"을 가르기 — card_level은 이름은 카드지만 실체는 케어 자산이라 존치.
3. "선택 실행 대기" SQL은 한 달 뒤에도 대기 중이다(7/20 배틀 SQL) — 후속 작업에 통합해서 청산할 것.

## 실행 대기 / 다음
- **카드 제거 SQL 재실행**(사장님) → 프로브 검증
- 폰에서 지도 → 고양이 탭해서 카드 탭 사라진 것 확인
- (8/26 이월) 텔레그램 토큰 회전(토스 승인 날)·홈택스 과세유형·성동구 접수 여부·MCP 토큰 3개
- 토스 심사 회신 대기(~9/4-9/11), 9/2 판정(첫밥 fallback·찜·signup_source 분포)
