# 개발일지 2026-08-26

## 오늘 한 일 요약
발주 자동화(텔레그램 봇) 완성 → **앱 전면 리디자인 "D 아이보리 에디토리얼" 확정·당일 배포**. 커밋 11개 전부 배포(success). 시안 2라운드(다크 기각 → 밝은 3안)를 거쳐 사장님이 D 확정, 토큰→네비→세리프→색 일괄 전환→홈 매스트헤드까지 하루에 완주.

| 커밋 | 내용 |
|------|------|
| `aac7eac`~`38eb7e1` | 발주 다이제스트 크론 + 텔레그램 봇 연결(env·재배포) |
| `411d0b1`~`2648ee1` | 발주서 개선 3연타: 쉬운 문구·금액 → 원가·후원·남는 돈 → 토스 수수료 차감 "진짜 남는 돈" |
| `eab493e` | D 1단계: 전역 토큰 전환(아이보리·웜 잉크·헤어라인·radius 축소·딥 테라코타) |
| `54c75bc` | D 2단계①: 하단 네비 도킹 헤어라인 바(활성=잉크+테라코타 점) |
| `3ce5664` | D 2단계②: 전역 h1 세리프(Noto Serif KR) |
| `f28eda4` | D 2단계③: 하드코딩 색 일괄 웜 전환(warm-swap.mjs, 123파일·314건) |
| `412f386` | D 2단계④: 홈 에디토리얼 매스트헤드 + 랜딩 시트 세리프 |

---

## 1. 발주 자동화 — 텔레그램 봇 (오전)
- **매일 13:00 KST** `/api/cron/order-dispatch`(vercel.json `0 4 * * *`)가 paid 실물 주문을 발주서로 정리해 운영자 텔레그램(@dosigongzon2026_bot "제로투", chat_id 8055012810)으로 발송. `lib/telegram.ts`는 env 미설정 시 silent skip.
- **발주서 계산**: 주문별 결제금액(배송비 표기)·원가(product_costs 매입가)·후원 적립·카드 수수료(`TOSS_FEE_RATE` 3.3% 보수 추정, 계약 확정 시 이 상수만 교체)·**진짜 남는 돈** + 2건 이상이면 오늘 합계. 매입가 미입력 상품은 ⚠경고(원가 0 계산 명시).
- **설계 원칙**: 상태 자동 전환 없음 — paid로 남으면 다음날 재표시(발주 누락 방지 리마인더). 게스트/가상 제외, 0건이면 무발송.
- **끝-끝 검증 3회**(문구 버전마다): 테스트 주문 생성→paid→발송(sent 1)→즉시 삭제. env는 Vercel 대시보드 수동 등록(CLI 토큰 만료 403 + ByteString 버그, API도 불가).
- ⚠ 토큰 2개가 채팅에 노출됨 — 첫 토큰(@dosigongzon_bot, 오전달)은 Revoke 권고, 현 토큰도 안정 후 회전 권고.
- 2단계(OpenClaw 양방향 "발주 완료" 명령)는 실주문 발생 후.

## 2. 리디자인 "D 아이보리 에디토리얼" (오후~)
**경위**: 사장님 "테마·디자인·UIUX 다 바꾸고 싶어" → 7/21 테라코타 동결 해제. 디자인 캔버스(artifact `0106caca`)로 시안 2라운드 — 1차 다크 3안(앰버/포레스트/골드)은 "다크 방향 자체 별로+디테일 부족"으로 기각, 2차 밝은 3안(D 아이보리 에디토리얼 / E 소프트 대시보드 / F 코지 일러스트)은 레이아웃부터 다르게 제작 → **D 확정**.

**실행(각 커밋 = 리버트 단위, 역순 revert가 완전 롤백)**:
1. **토큰**: 배경 #FAF6F0·웜 잉크 스케일·헤어라인 보더·radius 20→14 등 축소·primary #B05C36. 토큰 중앙화(8/13~15 작업) 덕에 var() 화면 일괄 전환.
2. **네비**: 플로팅 필 → 화면 전폭 도킹 바 + 상단 헤어라인.
3. **세리프**: `h1 { font-family: var(--font-serif) }` 한 줄로 앱 전체 대제목 세리프. Noto Serif KR은 **jsdelivr fontsource**(700/900)로 로드 — 기존 CSP(style/font-src jsdelivr) 범위 내라 CSP 무변경.
4. **색 일괄 전환**: `scripts/warm-swap.mjs` — 색→색 리터럴 치환(style·SVG·canvas 안전). 뉴트럴 10종+rgba 변형+구테라코타.
5. **홈 매스트헤드**: 날짜 아이브로 + 세리프 "오늘의 돌봄" + `hungryCatName` 실데이터 헤드라인(받침 조사 처리). care-inbox h2는 "내 아이들"로 개명. 다마고치·MyCatsHero는 기능 보존.

**검증**: puppeteer 실렌더(scripts/shot_redesign.mjs) — 쇼핑 h1 세리프·아이보리·랜딩 세리프 헤드라인 확인. 로그인 화면(홈 매스트헤드·네비)은 헤드리스 매직링크 세션이 안 붙어 미촬영 — **사장님 실기기 확인 대기**.

**hex 잔여분은 의도적 종결**: 2차 빈도분석 결과 상위 잔여색(#D85555 에러·#6B8E6F 세이지·#2C2C2C 관리자다크 등)은 전부 의미색/웜톤 — 추가 일괄치환은 의미색 파괴라 금지. 이후는 화면 단위 개별 대응.

## 3. 기타
- fund-snapshot 크론 자동발화 확인은 진행 중 — 디스패처 실발화가 00:25 UTC(09:25 KST)라 09:35 감시 백그라운드 대기. 스크립트 2회 헛발(PS5.1이 BOM 없는 UTF-8 ps1의 한글을 ANSI로 깨뜨려 파스 에러) → **ps1은 ASCII 전용** 교훈.
- 어썸디자인 = VoltAgent/awesome-claude-design(DESIGN.md 모음, 설치 개념 아님) 안내, ~/awesome-claude-design 클론. 이미지→코드는 기본 기능.

## 교훈
1. **시안은 색놀이가 아니라 레이아웃·타이포가 달라야 고를 수 있다** — 1차 기각의 원인.
2. 토큰 중앙화가 돼 있으면 전면 리테마가 "값 교체 한 커밋"이 된다 — 7~8월의 토큰 작업이 오늘 회수됨.
3. 일괄 색치환은 색→색(리터럴)으로 해야 SVG·canvas까지 안전. var() 치환은 컨텍스트를 가린다.
4. PS5.1 스크립트 파일은 ASCII 전용(한글 주석 금지).

## 실행 대기 / 다음
- 사장님 실기기로 로그인 홈(매스트헤드·새 네비)·지도 확인 → 어색한 화면 개별 폴리시
- fund-snapshot 09:35 감시 결과 확인(오늘)
- 토스 심사 회신 대기(~9/4-9/11), 텔레그램 봇 토큰 회전
- 첫밥 fallback 판정 9/2
