# 개발일지 2026-07-26 (오후) — 보안 패치 (커밋·배포 안 함, 로컬 수정만)

지시: 로컬 코드 수정 + 검증까지. **커밋·푸시·배포·운영 DB SQL 실행 금지.** 시크릿 미출력.
선행 작업(실험 MVP·네비)은 이미 커밋·배포 완료 상태에서 시작.

## 완료 (P0)
1. **경제성 API fail-open 제거** — buy/use-item/equip + care(아이템)/checkin/daily-login/care-bonus.
   원자 RPC 실패 시 비원자 폴백 전부 삭제 → **503 fail-closed**. checkin·daily-login은
   선점 컬럼(last_*_date)을 되돌려 재시도 가능하게. 동시요청 코인·아이템 중복 차단.
2. **SW 개인정보 캐시 차단** — public/sw.js v8. 동적 HTML/RSC는 SW 미관여(캐시 안 함),
   공개 정적 allowlist(/protection·/tips·정적자원)만 캐시. no-store/private 응답 저장 금지.
   push data.url은 same-origin + 허용 pathname allowlist만(외부·스킴 URL은 "/"로 강등),
   notificationclick에서 재검증. v8 범프로 기존 개인화 캐시도 청소.
3. **결제 로그 마스킹** — lib/log-sanitize.ts. confirm/webhook/cancel에서 paymentKey는
   HMAC digest(pk#…12자)로, 토스 오류는 code/status/message allowlist만, 예외는 메시지+시크릿
   치환만 기록. 응답 본문 원문 로깅 제거.
4. **저장형 XSS** — map 병원명 innerHTML에 escapeHtml 적용(수동등록 병원 라벨).
   shop/[id] JSON-LD는 이미 `<` 치환돼 있어 유지. 유사 JSON-LD 다수 이미 안전(확인만).
5. **server-only 경계** — lib/supabase/service.ts 최상단 `import "server-only"`.
   (server-only 패키지 설치 — package.json 변경은 이 목적)
6. **Sentry/서버 로그 PII redaction** — lib/sentry-redact.ts. server/edge/client 3 config에
   sendDefaultPii:false 명시 + beforeSend redactSentryEvent 연결. reportError extra는
   allowlist(식별자·수치)만 통과. request 헤더·쿠키·query·body 제거, user는 id만.

## 완료 (설계·초안, 운영 미적용)
7. **서클 사진 비공개화** — private 버킷 circle-photos + 멤버 업로드 RLS + signed URL(5분).
   image_url에 "private:{경로}" 참조 저장, /api/circle/image-url이 멤버십 검증 후 발급.
   레거시 공개 URL은 렌더러가 값 분기해 그대로 표시(호환). fail-closed(SQL 전엔 첨부만 실패).
   파일: box/supabase_circle_photos_private_migration.sql, box/서클사진_비공개전환_전략.md,
   lib/circle-chat-repo.ts, app/api/circle/image-url/route.ts, chat/page.tsx(ChatImage).
8. **위치 프라이버시** — 조사+최소안 제안서만(코드 변경 없음).
   핵심 발견: 일반 로그인만으로 ±444m 정밀도 획득 = 검증 관계자와 동일. anon 잠금 STEP B 미실행.
   제안: 관계자(등록자/돌봄이력/서클) 등급 분리 + STEP B 마무리. box/위치프라이버시_권한모델_제안_20260726.md

## 완료 (운영 검증 자료, box/)
9. **RPC 권한 검증** — box/보안검증_경제성RPC_권한_20260726.sql(읽기전용 grantee 점검 +
   SET ROLE 안전 probe + curl JWT 절차). **발견: buy_shop_item_atomic 등이 authenticated에
   execute 부여됨 → p_user_id 인자를 받는 security definer라 타인 계정 조작 가능(수평 상승).**
   하드닝 SQL 초안: box/supabase_economy_rpc_lockdown_migration.sql (운영 미적용).

## 검증 결과
- lib/experiment-metrics·보안 순수함수: scripts/test-security-patch.ts 통과
- **tests/ 회귀 스위트(다른 세션이 추가, 보존): node --test 29/29 통과**
  (vm으로 sw.js 실제 핸들러 검증 + fail-closed 소스 불변식 + 로그/Sentry redaction)
- npx tsc --noEmit: 통과
- 신규/변경 파일 eslint: 에러 0 (map/care의 기존 any·미사용 경고는 변경 전부터 존재 — git stash로 확인)
- npm run build: Compiled successfully, /api/circle/image-url 등록 확인
- **npm audit: high 5건(next/postcss/sharp/fast-uri) — 그대로 기록. `npm audit fix` 미실행
  (Next 버전 상승 회귀 위험 — 운영 판단 필요)**

## 운영 확인·수동 실행 대기 (사장님)
- box/supabase_economy_rpc_lockdown_migration.sql — 경제성 RPC execute 회수 (검증 SQL 먼저)
- box/supabase_circle_photos_private_migration.sql — 서클 사진 private 버킷 (코드 배포 후)
- box/supabase_cats_anon_coord_lockdown_migration.sql STEP B — 이미 대기 중이던 anon 좌표 잠금
- npm audit high 5건 처리 방침 결정

## 커밋 안 함
작업트리에 변경만 존재. 다른 세션이 추가한 tests/*.mjs·box/supabase_rpc_*.sql은 미변경 보존.
