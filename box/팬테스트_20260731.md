# 도시공존 팬테스트 리포트 — 2026-07-31

> 수행: 제로투(AI) + 4개 침투 서브에이전트(ag_ops_security ×2, ag_ops_crypto-env, 인가전수) + 프로덕션 블랙박스 실측
> 대상: `C:/Users/grow2/city` (Next.js 16 + Supabase), 프로덕션 dosigongzon.com
> 이전 감사: 2026-07-30(보안감사_20260730.md) — Critical/High 0건 이후

## 총평
- **anon 경계·시크릿·인증·IDOR·결제 코어는 견고.** 취약점 대부분이 "다른 곳엔 건 방어를 여기만 빠뜨린" 일관성 결손.
- **지금 실동작하는 심각 경로 2건**(게시글 사칭·QR XSS)은 이 세션에서 수정·배포/작성 완료.
- 결제 관련 High 2건은 `PAYMENT_ENABLED=false`라 잠복 — 결제 오픈 전 필수 수정.

---

## 실측으로 튼튼했던 것 (재보고 불필요)
- **anon**: profiles 락다운 프로덕션 적용 확정(invite_code/coins/email → 42501), cats 좌표 컬럼 차단(lat/lng 42501), orders/DM/refunds/admins/push_subscriptions/cat_location_history 0행, 익명 쓰기·products 가격조작 거부, 스토리지 버킷 열거 차단.
- **시크릿**: 프로덕션 JS 17청크 전량 분석 — service_role/Toss시크릿/Gemini/VAPID-private/Turnstile-secret 유출 0건. `server-only` 경계.
- **인가**: 74라우트 인증누락 0, cron 22종 fail-closed, admin 7종 이중검증, mass-assignment 0.
- **결제 코어**: IDOR(user.id 대조)·금액조작(서버 재계산+CHECK)·레이스(부분 유니크 인덱스+조건부 전이)·웹훅위조(토스 재조회)·관리자인가 전부 방어.
- profiles 게임컬럼(coins/admin_title/suspended)은 `profiles_guard_protected_fields` 트리거로 직접 PATCH 차단(코드+실측), DM UPDATE는 컬럼 grant 하드닝됨.
- search `.or()` 필터 인젝션: `escapeForIlike`가 `,()`제거+`%_\`이스케이프로 차단.

---

## 🔴 지금 악용 가능 (LIVE)

### #1 [HIGH] 게시글·댓글 작성자 스냅샷 위조 → ✅ 조치(SQL 실행 대기)
posts/post_comments/cat_comments INSERT 정책이 `auth.uid()=author_id`만 검사. PostgREST 직접 호출로 `author_title='official_volunteer'`(💛공식봉사자 배지 사칭)·`is_pinned=true`(최상단 고정)·`hidden=false`(자동숨김 무력화)·counts 조작. 독립 2개 에이전트가 프로덕션 201 write로 재현.
- **조치**: `box/supabase_author_snapshot_guard_migration.sql` — 관리자 인지 BEFORE INSERT/UPDATE 트리거. **⚠ Supabase SQL Editor 실행 필요.**

### #2 [HIGH] 고양이 이름 Stored XSS (QR 인쇄 팝업) → ✅ 수정·배포 완료
`CatQRModal.tsx`가 `selectedCat.name`을 `window.open("")` 팝업 `document.write()`에 무이스케이프 삽입. about:blank가 opener origin 상속 → same-origin 세션 탈취. region은 `/[<>]/` 막았지만 name은 누락.
- **조치(커밋 0c9f106)**: escapeHtml 3곳 적용 + cats-repo `assertSafeName` 배선. 배포됨.

### #3 [MEDIUM] `/api/check-nickname` 무인증 + 클라 지정 currentUserId
route.ts:19,36-38 — body의 `currentUserId`로 중복검사 우회. profiles에 nickname unique 없어 닉네임 도용 가능. **미조치.**
- 수정: currentUserId 제거→세션 판정 + `unique index profiles(lower(nickname))`.

### #4 [MEDIUM] 푸시 구독 endpoint 무검증 Stored SSRF
`push/subscribe/route.ts:14-23` 스킴/호스트 미검증 → webpush 서버 POST가 내부 포트스캔 오라클. **미조치.**
- 수정: https 강제 + 푸시 서비스 호스트 allowlist.

### #5 [MEDIUM] 케어 파밍(카드 등급·타이틀 즉시 최대)
ⓐ `add_cat_card_exp(p_amount)` 클라 EXP 무클램프 ⓑ `checkin/complete` tasks 배열 상한 없음 → care_logs 대량. rarity 임계(20/60/150) 즉시 legendary. **미조치.**
- 수정: p_amount 하드코딩/클램프 + tasks Set·상한 + care_logs 도배방지 트리거.

### #6 [MEDIUM] `/api/chat` 입력 무제한 → Gemini 비용 증폭
route.ts:222-243 요청당 토큰 상한 없음(레이트리밋만 존재). **미조치.**
- 수정: `message.slice(0,2000)`, `history.slice(-10)`.

### #7 [MEDIUM] OG 이미지 라우트 Blind SSRF
`cats/[id]/opengraph-image.tsx`가 photo_url 서버 fetch, `sanitizeImageUrl`은 스킴만 검사(http·사설IP 통과). community OG 동일. **미조치.**
- 수정: 서버 전용 URL 가드(https+사설/링크로컬 차단+remotePatterns allowlist).

---

## 🟠 결제 활성화 시 발화 (PAYMENT_ENABLED=false, 잠복)
환불 마이그레이션은 프로덕션 적용·RLS 잠금 확인됨. 결함은 정책 로직.
- **[HIGH·잠복] H-1** `refund-policy.ts:125` — 판매자귀책 사유(delayed/wrong_delivery 유저 선택가능)가 가상상품 7일기한 무제한 우회 + 무인 즉시환불. (가상상품 0건이라 미발화)
- **[HIGH·잠복] H-2** `refund-policy.ts:137-142` — paid/preparing 기한·심사 없이 즉시 전액환불+재고복원, shipped_at/tracking 무시 → 발송 후 상품 편취.
- **[MED·잠복] M-1** `refund-executor.ts:216-229` — 주문원장 update 0행이어도 성공 간주(.select() 없음), 재고/포인트 후처리 진행 → 이중복원.
- **[MED·잠복] M-2** `refund/route.ts:105` — 셀프 환불 재시도가 멱등키 새로 발급. 부분환불 열리면 이중환불.
- **[MED] M-3** 웹훅 무인증·무레이트리밋 → 토스 재조회 남용으로 결제대사 마비 가능.

---

## 🔵 Low / 정리
chat `role:"ai"` 위조로 의료 가드레일 우회 · avatar_url 무검증 CSS url() 삽입 · 익명 펀드투표 스터핑(cast_fund_vote_anon p_anon_id 클라 제공) · weather 라우트 위조가능 cf-connecting-ip 신뢰 · news/page 링크 sanitize 누락 · 토스 원문 에러 클라 노출 · orders INSERT 가드 refund 컬럼 미제한 · 인메모리 레이트리밋 서버리스 무력화 · author_level INSERT 위조(코스메틱, 배지 잠금으로 권위 신호는 제거됨).

---

## 진행 상태 (2026-07-31)
| # | 심각도 | 상태 |
|---|---|---|
| #2 QR XSS | HIGH·LIVE | ✅ 수정·배포(0c9f106) |
| #1 스냅샷 위조 | HIGH·LIVE | ✅ SQL 작성, **실행 대기** |
| #3 check-nickname | MED·LIVE | ⬜ 미조치 |
| #4 push SSRF | MED·LIVE | ⬜ 미조치 |
| #5 케어 파밍 | MED·LIVE | ⬜ 미조치 |
| #6 chat 비용 | MED·LIVE | ⬜ 미조치 |
| #7 OG SSRF | MED·LIVE | ⬜ 미조치 |
| H-1·H-2 환불 | HIGH·잠복 | ⬜ 결제 오픈 전 필수 |
