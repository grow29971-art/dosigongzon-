# 개발일지 2026-07-26 (야간) — 보안 재감사 High·Medium 수정 (로컬, 커밋·배포 안 함)

지시: 재감사에서 나온 미해결 High·Medium 수정. C:\Users\1\city만. 냥줍 미접촉.
SQL 실행·커밋·푸시·배포 금지(사용자 검토 후 별도 수행).

## 수정 완료

### [H-4] 의존성 취약점 → runtime 0건
- **next 16.2.10 → 16.2.12** (`^16.2.12`): SSRF·DoS·캐시혼동 등 9개 권고 해소.
  (취약 범위 16.0.0-16.2.10, 16.2.11+가 패치판이며 `^16.2.4` 호환 — 처음엔 forward fix
  없다고 오판했으나 audit range 재확인으로 정정)
- **overrides 추가**(package.json): postcss `^8.5.23`, sharp `^0.35.3`, fast-uri `^3.1.4`,
  `brace-expansion@5.0.7` → `5.0.8`. brace-expansion은 취약한 5.0.7만 정밀 교체 —
  전역 `^5.0.8` 강제 시 minimatch@3(eslint 체인) CJS가 깨져 lint 폭발했기 때문.
- 결과: **runtime 취약점 0**. 남은 9 high는 전부 eslint devDependency 체인
  (brace-expansion DoS via minimatch) — 빌드/lint 시점만 사용, 배포 runtime 무관.
  전역 강제 시 툴체인 파손이라 clean fix 없음(eslint 생태계 갱신 대기). eslint-config-next도
  16.2.12로 정렬(무해).

### [M-1] CSP unsafe-eval 제거 (next.config.ts)
- 앱·lib 소스에 eval/new Function/WebAssembly **전무** 확인 → 프로덕션 CSP에서 제거.
- Next dev(Turbopack HMR)는 eval 필요 → `NODE_ENV !== production`일 때만 유지.
- ⚠ 배포 전 확인: 프리뷰/프로덕션에서 Kakao 지도·Toss 결제 플로우 CSP 위반 없는지.

### [M-2] 채팅 Rate Limit 분산화 (app/api/chat/route.ts)
- 인메모리 Map → Postgres RPC `check_ai_chat_rate`(고정창 원자 카운터) 우선 사용.
- RPC 미배포(42883)면 인메모리 폴백 → 채팅 계속 동작(분산 보장만 빠짐).
- 마이그레이션: `box/supabase_ai_chat_rate_migration.sql` (멱등, service_role 전용, 실행 대기).

### [M-3] tips XSS DOMPurify (lib/html-sanitize-server.ts, tips/[slug]/page.tsx)
- 정규식 sanitizer(write-time)에 더해 **렌더 sink에 DOMPurify 재정화** 이중 방어.
- isomorphic-dompurify 설치. jsdom 기반이라 **서버 전용 파일 분리**(html-sanitize.ts는
  클라이언트 번들에도 포함되므로 손대지 않음) + `serverExternalPackages`에 등록.
- extractToc로 헤딩 id 주입 후 sanitizeTipHtmlServer 적용.

## 재검증 (중복 적용 안 함)
- 경제성 API 7종: 503 fail-closed ✓, 비원자 폴백 없음 ✓ (이전 세션 조치 유지 확인)
- RPC 권한 잠금: buy/equip/consume/increment_coins/award_care_bonus **5종 전부 기존
  마이그레이션에 존재**(security_audit_20260716_2, equip_item_authz, consume_item, coins_tx).
  중복 신규 생성 안 함. 운영 적용 여부는 `box/보안검증_경제성RPC_권한_20260726.sql`(읽기전용)로 확인.
- 서클 사진 private: 코드(circle-photos 버킷 + signed URL + 멤버십 검증) 정상 ✓.

## 검증 결과
- npx tsc --noEmit ✓
- node --test tests/ 29/29 ✓, scripts/test-security-patch·experiment-metrics ✓
- 변경 파일 eslint: 에러 0 (repo 전반 기존 89 에러는 무관 — map·news-crawler 등 미변경 파일)
- npm run build ✓ (next 16.2.12 + sharp 0.35.3 override + DOMPurify 통합 정상)
- npm audit: **high 9 (전부 eslint dev 체인, runtime 0)** — 그대로 기록

## 신규/변경 파일 (이 세션)
- package.json, package-lock.json (deps + overrides)
- next.config.ts (CSP unsafe-eval, serverExternalPackages)
- app/api/chat/route.ts (분산 rate limit)
- app/(main)/tips/[slug]/page.tsx (DOMPurify sink)
- lib/html-sanitize-server.ts (신규, DOMPurify 서버 유틸)
- box/supabase_ai_chat_rate_migration.sql (신규, 실행 대기)

## 운영 실행 대기 (사용자 검토 후)
- box/supabase_ai_chat_rate_migration.sql (채팅 분산 rate limit)
- (기존 대기분) 경제성 RPC 검증/잠금, 서클 사진 private, cats anon 좌표 STEP B, 실험 MVP

## 남은 위험
- eslint dev 체인 9 high (build/lint 시점, runtime 무관 — 생태계 갱신 대기)
- next: 16.2.12로 현재 알려진 권고 해소, 향후 신규 권고는 상위 패치 추적 필요
- CSP unsafe-eval 제거의 서드파티 SDK 영향은 배포 전 실동작 확인 필요
