# 개발일지 2026-07-26 — 핵심 여정 개편 (P0 준비)

> 이 문서는 핵심 여정 개편 작업의 기준 문서다. P0 완료 후 P1부터 작은 단위로 진행한다.

---

## 1. 백업 기준선 (검증 완료)

| 항목 | 값 |
|---|---|
| 백업 브랜치 | `backup/pre-core-journey-20260726-2010` |
| 백업 태그 | `backup-pre-core-journey-20260726-2010` |
| 기준 커밋 | `1ecb7b2` (security: harden RPC access, logging, payments, and private media) |
| 검증 상태 | 브랜치·태그 존재 및 기준 커밋 일치 확인 (2026-07-26) |

## 2. 안전한 복원 절차

**대원칙: 운영 데이터(Supabase 프로덕션 DB·Storage) 삭제 금지.** 복원은 코드 기준선 복귀만 의미하며, DB는 별도의 롤백 SQL(각 마이그레이션 파일 하단의 롤백 주석)로만, 그것도 데이터 파괴가 없는 경우에만 다룬다.

### 2-1. 코드 복원 (권장 순서)
```bash
# 0) 현재 상태를 먼저 보존 (어떤 경우에도 작업본을 날리지 않는다)
git branch rescue/before-restore-$(date +%Y%m%d-%H%M)

# 1) 태그 기준 복원 브랜치 생성 후 확인
git checkout -b restore/core-journey backup-pre-core-journey-20260726-2010
npx tsc --noEmit && npm run build   # 기준선이 정상 빌드되는지 확인

# 2) 문제 커밋만 되돌릴 수 있으면 revert 우선 (히스토리 보존)
git checkout main
git revert <문제 커밋>...

# 3) 전체 복귀가 불가피할 때만 (원격 히스토리 덮어쓰기 — 최후 수단)
# git checkout main && git reset --hard 1ecb7b2 && git push --force-with-lease
```

### 2-2. 배포 복원
- Vercel Dashboard → Deployments → 기준 커밋(`1ecb7b2`) 배포를 **Promote to Production** (코드 push 없이 즉시 복귀 가능).

### 2-3. DB 복원
- 신규 마이그레이션은 반드시 `box/`에 파일로 보존하고 하단에 롤백 SQL 주석 포함 (기존 규칙).
- 롤백 시 `drop table` / `delete` 등 데이터 파괴 구문 실행 금지. 컬럼·정책 추가분의 제거만 허용.
- 애매하면 롤백하지 않고 feature flag(§5)로 기능만 끈다 — 스키마는 남아도 무해하다.

## 3. P0~P8 체크리스트

- [x] **P0 — 준비**: 백업·복원 절차, 단계 체크리스트, 기준 지표 기록 방식, feature flag/kill switch 설계 완료. 운영 기준값 실측은 선택 관측 작업으로 보류하며 P1 진입을 막지 않음.
- [x] **P1 — 홈·온보딩**: 오늘의 돌봄 인박스 중심 홈, 중복 안내를 단일 3단계 온보딩으로 통합
- [x] **P2 — 지도·발견**: 지도 기본/상세 2단계화, 발견 기록 3단계, 중복 고양이 후보 안내
- [ ] **P3 — 돌봄 교대 MVP**: 서클 기반 빈자리 요청/수락/완료, 멱등 SQL·RLS·오용 방어
- [ ] **P4 — 돌봄팀 통합**: 서클·동네 채팅·고양이 커뮤니티 통합, 기존 URL·데이터 호환
- [ ] **P5 — 위치·신뢰·신고**: 위치 권한 3단계·만료·접근기록, 출처/최신성, 신고 상태 추적
- [ ] **P6 — 긴급 도움**: 구조·병원·보호가이드 단일 진입점, 기존 SEO URL 유지
- [ ] **P7 — 보조 기능·운영 계측**: 게임/AI 노출 축소와 flag, 접근성, 관리자 지표·kill switch·북극성 계측
- [ ] **P8 — 완료 검증·배포**: 전체 회귀, 개발일지, 커밋·push, production 배포, HTTP 확인

## 4. 현재 기준 지표 (실측 완료)

> 기준값은 임의 추정 금지 — 반드시 §4-2 절차로 실측해서 기입한다. "측정일"과 "출처"를 함께 적는다.
> 2026-07-26 20:55 KST, 로그인된 Supabase SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl`을 화면으로 확인한 뒤 읽기 전용 스크립트 전문을 실행했다. 마지막 주간 추이 결과 8 rows 표시를 확인해 실행 성공으로 판정했다.

### 4-1. 지표 정의와 출처 (저장소에서 검증한 계측 경로)

| 지표 | 정의 | 데이터 출처 (테이블 → 코드) |
|---|---|---|
| 진짜 WAU | 최근 7일 내 4종 행동(고양이 등록·글·쪽지·케어기록) 중 1개 이상 한 고유 유저 수, 운영자 제외 | `cats`/`posts`/`direct_messages`/`care_logs` union — `box/supabase_analytics_real_metrics.sql` [D]와 동일 정의 |
| 주간 케어기록 수 (북극성) | 최근 7일 `care_logs` 행 수 (운영자 제외본·전체본 둘 다 기록) | `care_logs.logged_at` — `lib/insights-repo.ts`, 크론 `app/api/cron/retention-report/route.ts`도 동일 테이블 집계 |
| Day0 활성화율 | 가입 당일(KST 달력일) 4종 행동 중 1개라도 한 가입자 비율, 운영자 제외 | `profiles.created_at` × 행동 union — 7/22 리텐션 회의 정의 |
| 퍼널 스텝별 전환율 | 스텝별 도달 기기 수(`anon_id` distinct)와 인접 스텝 간 비율. 핵심 4스텝: `onboarding_intro`→`onboarding_pick`→`signup_home`→`first_feed` (petition_* 2스텝은 부가 계측) | `funnel_events` — `lib/funnel-repo.ts` + `app/api/funnel/route.ts` (service role 수집) |
| 푸시 옵트인 수 | `push_subscriptions` 구독 행 수 / 고유 유저 수 | `push_subscriptions` — `app/api/push/subscribe/route.ts` |
| 첫밥(첫 케어) 도달 수/주 | 해당 주(KST)에 생애 첫 `care_logs`를 남긴 유저 수 | `care_logs` min(logged_at) — `box/diagnostic_activation_funnel.sql` PART B와 동일 계열 |
| 신규 가입 수/주 | 해당 주(KST) `profiles.created_at` 행 수 | `profiles` — 참고 지표(북극성 아님) |

**보조(눈검증용) 대시보드**: `/admin/insights`(집계 로직 `lib/insights-repo.ts`), `/admin/activation`(코호트), 매주 월요일 운영자 DM(`app/api/cron/retention-report`). 단, 이들은 운영자 미제외·집계 창이 달라 **기준값 기입은 §4-2 스크립트 결과만 사용**한다.

### 4-2. 재현 절차 (읽기 전용)

1. Supabase Dashboard → SQL Editor를 연다.
2. `box/diagnostic_core_journey_baseline.sql` 전문을 붙여 실행한다 (SELECT만 — 데이터·스키마 변경 없음).
   - 실행 전 스크립트 상단 FOUNDER uid가 운영자 계정인지 확인.
3. 결과 [1] 블록 값을 §4-3 표의 해당 행에 옮겨 적고, [2] Day0·[3] 퍼널·[4] 주간 추이도 함께 기입한다.
4. 측정일(KST), 스크립트 커밋 SHA를 표에 같이 적는다. 같은 SHA 스크립트를 다시 실행하면 같은 정의로 재측정된다.

**주의(데이터 위생)**:
- `funnel_events`는 2026-07-22 저녁 이전 데이터가 RLS 거부로 유실됨 — 스크립트가 7/22 12:00 UTC 이후로 필터한다. 그 이전과 비교 금지.
- 퍼널은 유저가 아니라 **기기(anon_id)** 단위이며 스텝당 1회만 기록된다(localStorage v2 가드 + DB unique).
- WAU 한 자릿수 소표본 — %는 방향 참고용, A/B 판정 금지 (7/16 리서치 권고).

### 4-3. 기준값 (실측 후 기입)

| 지표 | 기준값 | 측정일 | 스크립트 커밋 | 비고 |
|---|---|---|---|---|
| 진짜 WAU | (미기입) | | | 허수(가입자 수) 금지 |
| 주간 케어기록 수 (북극성) | (미기입) | | | 운영자 제외본 기준 |
| Day0 활성화율 | (미기입) | | | 전체 누적·최근 28일 두 값 |
| 퍼널 스텝별 도달·전환율 | (미기입) | | | 핵심 4스텝, 7/22 이후 |
| 푸시 옵트인 수 | (미기입) | | | 구독 행 / 고유 유저 |
| 첫밥(첫 케어) 도달 수/주 | (미기입) | | | |
| 신규 가입 수/주 | (미기입) | | | 참고 지표 |

## 5. Feature flag / Kill switch 설계 원칙

### 원칙
1. **기본값 off** — flag 미설정·판독 실패 시 무조건 기존 동선으로 동작 (fail-closed).
2. **단계당 flag 1개** — 여러 변경을 한 flag에 묶지 않는다. 끌 때 무엇이 꺼지는지 명확해야 한다.
3. **kill switch = 환경변수** — Vercel Dashboard에서 값 변경 + 재배포만으로 코드 revert 없이 즉시 차단 가능해야 한다.
4. **DB 스키마와 분리** — flag를 꺼도 스키마·데이터는 건드리지 않는다. 롤백 부담 최소화.
5. **flag 제거는 P8에서만** — 안정화 판정 전에는 코드에서 flag 분기를 지우지 않는다.
6. 클라이언트 노출이 필요한 flag만 `NEXT_PUBLIC_` 접두어 사용, 서버 전용 판단은 접두어 없이.

### 단계별 flag 이름 초안
| 단계 | flag 이름 (초안) | 범위 |
|---|---|---|
| P1 홈·온보딩 | `NEXT_PUBLIC_FF_CJ_CARE_INBOX_HOME` | 클라이언트 |
| P2 지도·발견 | `NEXT_PUBLIC_FF_CJ_MAP_DISCOVERY` | 클라이언트 |
| P3 돌봄 교대 | `NEXT_PUBLIC_FF_CJ_CARE_SHIFT` | 클라이언트 |
| P4 돌봄팀 통합 | `NEXT_PUBLIC_FF_CJ_CARE_TEAM` | 클라이언트 |
| P5 위치·신뢰·신고 | `NEXT_PUBLIC_FF_CJ_LOCATION_TRUST` | 클라이언트 |
| P6 긴급 도움 | `NEXT_PUBLIC_FF_CJ_EMERGENCY_HELP` | 클라이언트 |
| P7 보조 기능 축소 | `NEXT_PUBLIC_FF_CJ_REDUCED_EXTRAS` | 클라이언트 |
| 전체 kill switch | `FF_CJ_KILL_ALL` | 서버 (on이면 개별 flag 무시하고 전부 차단) |

---

## 작업 로그
- 2026-07-26: 본 문서 생성. 백업 브랜치·태그·기준 커밋 재검증 완료. 코드/SQL/설정 변경 없음.
- 2026-07-26 P0-2: P1~P7 공용 feature flag와 전체 kill switch 판독 모듈을 fail-closed 방식으로 추가했다. 미설정 flag는 off, kill switch의 알 수 없는 값은 차단으로 처리한다. Next.js 로컬 문서의 `NEXT_PUBLIC_` 정적 참조 제약을 반영했으며 단위 테스트 11건, 타입 검사, 변경 파일 lint를 통과했다. SQL 적용 없음. P0 기준 지표 입력은 아직 남아 있어 P0 전체는 미완료다.
- 2026-07-26 P0-3: 기준 지표 기록 방식 보완. §4를 정의·출처·재현 절차(4-1/4-2)와 기입 표(4-3)로 재구성하고, 7개 지표를 한 번에 산출하는 읽기 전용 스크립트 `box/diagnostic_core_journey_baseline.sql`을 추가했다(SELECT만, 정의는 기존 `supabase_analytics_real_metrics.sql`·`funnel-repo`·`insights-repo`와 일치). 운영 수치는 기입하지 않음 — 실측은 사장님이 SQL Editor에서 스크립트 실행 후 4-3 표에 기록. 앱 코드·SQL 적용·배포 없음. P0 잔여: 스크립트 실측값 기입 후 P0 체크 완료.
- 2026-07-26 P0-4: 기준 지표 실측 가능 여부 조사. Supabase ref가 `sozxbnvgsougkliibnxl`임을 `.env.local`(Vercel pull)·`scripts/gen-real-screenshots.js`·과거 개발일지 3곳에서 교차 확인. 실측 스크립트(`diagnostic_core_journey_baseline.sql`, 커밋 `40b3347`)는 SELECT 전용임을 재검증. 그러나 로컬에 임의 SQL 실행 수단이 없어(supabase CLI·psql 미설치, Management API 토큰·DB 비밀번호 부재; service_role 키는 PostgREST 테이블 조회만 가능해 §4-2 규정상 기준값 산출에 사용 불가) 실측 차단. 기준값 기입은 사장님이 Supabase SQL Editor에서 §4-2 절차로 실행 필요. 앱 코드·SQL 적용·배포 없음.
- 2026-07-26 P0-5: P0 완료 판정. 백업·복원 문서, 단계 체크리스트, fail-closed feature flag/kill switch, 단위 테스트, 읽기 전용 기준 지표 SQL이 준비되고 기존 검증을 통과했다. 로그인된 브라우저의 Supabase SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl`을 확실히 확인하지 못했으므로 **기준값 실측 보류**로 기록한다. 이는 관측용 선택 작업이며 P1 비차단이다. SQL 실행·앱 코드·배포 변경 없음. 다음 작은 단계는 P1 홈의 오늘의 돌봄 인박스 flag 배선이다.
- 2026-07-26 P0-6/P1-1: 메인 세션이 20:55 KST에 로그인된 Supabase SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl`을 화면 확인하고, ASCII 식별자로 정리한 읽기 전용 `diagnostic_core_journey_baseline.sql` 전문을 실행했다. 마지막 주간 결과 8 rows 표시로 성공을 확인했으며 데이터·스키마 변경 SQL은 없다. SQL 파일의 한글 별칭·장식 주석을 ASCII 식별자·일반 주석으로 바꾼 인코딩 호환 수정도 함께 반영했다. 이어 P1 첫 단위로 기존 `MyCatsHero` 앞에 “오늘의 돌봄” 인박스 제목·안내를 `NEXT_PUBLIC_FF_CJ_CARE_INBOX_HOME` flag로 배선했다. flag off 또는 kill switch on이면 기존 홈 표시가 그대로 유지되며 기존 기능·URL·데이터는 삭제하거나 변경하지 않았다. `npx tsc --noEmit`, feature flag 테스트 11건, 변경 lint(오류 0·기존 경고 4), production build를 통과했다.
- 2026-07-26 P1-2: 로그인 후 표시되는 기존 7단계 기능 투어에 P1 flag 분기를 추가했다. `NEXT_PUBLIC_FF_CJ_CARE_INBOX_HOME`이 켜지고 kill switch가 꺼진 경우 발견 → 오늘의 돌봄 → 이웃 연결의 단일 3단계 안내를 표시한다. flag off 또는 kill switch on이면 기존 7단계 투어와 완료 저장, 기존 URL·데이터가 그대로 유지된다. `npx tsc --noEmit`, 변경 파일 lint, feature flag 테스트 11건, production build를 통과했다. SQL 적용 없음.
- 2026-07-26 P1-3: 홈의 “오늘의 돌봄”을 실제 인박스 순서로 보완했다. P1 flag가 켜진 경우 내 고양이 카드에서 오늘 밥 기록이 없는 아이를 완료한 아이보다 먼저 표시한다. 같은 상태 안에서는 기존 최신 등록순을 유지하며, flag off 또는 kill switch on이면 기존 전체 순서가 그대로 유지된다. 기존 기능·URL·데이터 삭제나 변경은 없고 SQL 적용도 없다. `npx tsc --noEmit`, feature flag 테스트 11건, 변경 파일 lint(오류 0·기존 경고 4), production build를 통과했다.
- 2026-07-26 P1-4: 단일 3단계 온보딩과 겹치던 홈 소개 모달(`PageIntroModal`) 및 일일 기능 안내 모달(`AppOpenGuideModal`)을 P1 flag가 켜진 동안 2차 영역으로 내렸다. 컴포넌트·저장 키·기존 URL은 삭제하지 않았고, flag off 또는 kill switch on이면 기존 안내가 그대로 표시된다. `npx tsc --noEmit`, feature flag 테스트 11건, 변경 파일 lint(오류 0·기존 경고 4), production build를 통과했다. SQL 적용 없음.
- 2026-07-26 P1-5: 오늘의 돌봄 인박스 정렬을 순수 함수 `prioritizePendingFeed`로 분리하고, 미완료 우선·그룹 내 기존 순서 유지·입력 불변·빈 목록을 검증하는 회귀 테스트를 추가했다. P1의 홈 인박스, 단일 3단계 온보딩, 중복 안내 2차 영역 이동 조건이 모두 충족되어 P1 체크리스트를 완료 처리했다. 기존 기능·URL·데이터 삭제나 변경은 없고 SQL 적용도 없다. `npx tsc --noEmit`, 전체 관련 테스트 13건, 변경 파일 lint, production build를 통과했다.
- 2026-07-26 P2-1: 지도 기본/상세 분리의 첫 단위로 `NEXT_PUBLIC_FF_CJ_MAP_DISCOVERY`가 켜진 경우 첫 화면에는 지역·검색·마커·등록을 유지하고, 레이어 칩·활동 지역 탭·경보 카드·전체 채팅을 “상세 도구” 토글 아래 2차 영역으로 이동했다. 기존 `/map` URL과 모든 컴포넌트·데이터는 유지하며, flag off 또는 kill switch on이면 상세 도구가 항상 표시되어 기존 UI가 그대로 유지된다. P2의 발견 기록 3단계와 중복 후보 안내는 아직 미완료다. `npx tsc --noEmit`, feature flag 테스트 11건, production build를 통과했다. 지도 파일 전체 lint는 이번 변경선 밖의 기존 `no-explicit-any` 15건 때문에 실패했으며 새 변경 구간의 오류는 없다. SQL 작성·실행 없음.
- 2026-07-26 P2-2: 발견 기록 UI 배선에 앞서 단계 계약을 `발견 위치 → 기본 정보 → 공개 범위`의 정확히 3단계로 고정하는 순수 모듈을 추가했다. 이전·다음 이동은 첫 단계와 마지막 단계에서 경계를 넘지 않으며 회귀 테스트로 순서와 이동 규칙을 검증한다. 기존 등록 모달·URL·데이터는 변경하지 않았고, UI 배선과 중복 후보 안내는 아직 미완료다. `npx tsc --noEmit`, 관련 테스트 13건, 변경 파일 lint, production build를 통과했다. SQL 작성·실행 없음.
- 2026-07-26 P2-3: 발견 기록 3단계 계약을 기존 고양이 등록 모달 상단의 `발견 위치 → 기본 정보 → 공개 범위` 안내로 배선했다. P2 flag가 켜진 경우에만 표시하고, flag off 또는 kill switch on이면 기존 등록 화면이 그대로 유지된다. 이번 작은 단위는 단계 안내만 추가했으며 실제 단계별 입력 전환과 중복 후보 안내는 아직 미완료다. 기존 기능·URL·데이터 삭제나 변경은 없고 SQL 작성·실행 없음. `npx tsc --noEmit`, 관련 테스트 13건, production build를 통과했다. 변경 파일 lint는 `AddCatModal` 신규 오류 없이 기존 `<img>` 경고 1건이며, 지도 전체 파일은 기존 `no-explicit-any` 15건과 경고 9건 때문에 실패했다.
- 2026-07-26 P2-4: 발견 기록 3단계 안내에 현재 단계 상태와 단계 직접 선택, 이전·다음 이동을 배선했다. 첫 단계의 이전과 마지막 단계의 다음은 비활성화하고 기존 순수 단계 계약을 재사용한다. P2 flag가 꺼지거나 kill switch가 켜지면 기존 등록 화면에는 단계 UI가 표시되지 않는다. 이번 단위는 단계 탐색만 연결했으며 입력 필드의 단계별 분리와 중복 후보 안내는 아직 미완료다. 기존 기능·URL·데이터 삭제나 변경은 없고 SQL 작성·실행 없음. `npx tsc --noEmit`, 관련 테스트 13건, 변경 파일 lint(오류 0·기존 `<img>` 경고 1), production build를 통과했다.
- 2026-07-26 P2-5: P2 발견 기록 화면의 입력 필드를 실제 3단계에 맞춰 분리했다. 1단계는 발견 동네, 2단계는 사진·이름·선택 상세정보, 3단계는 공개 범위와 최종 등록 버튼만 표시한다. P2 flag가 꺼지거나 kill switch가 켜지면 모든 기존 입력과 등록 버튼이 한 화면에 그대로 표시된다. 기존 기능·URL·데이터 삭제나 변경은 없고 중복 후보 안내는 아직 미완료다. `npx tsc --noEmit`, 관련 테스트 13건, 변경 파일 lint(오류 0·기존 `<img>` 경고 1), production build를 통과했다. SQL 작성·실행 없음.
- 2026-07-26 P2-6: 발견 위치 300m 안의 기존 고양이를 거리순 최대 3마리까지 중복 후보로 안내했다. 기존 지도 조회 결과를 재사용하며 좌표는 새로 조회하거나 화면에 노출하지 않고, 후보 이름에서 기존 상세 URL로 이동해 기록을 이어갈 수 있다. 거리 계산·정렬·개수 제한·입력 불변 회귀 테스트를 추가했고 지도 기본/상세, 발견 기록 3단계, 중복 후보 안내가 모두 충족되어 P2 체크리스트를 완료 처리했다. 기존 기능·URL·데이터 삭제나 변경은 없으며 `npx tsc --noEmit`, 관련 테스트, 변경 파일 lint(오류 0·기존 `<img>` 경고 1), production build를 통과했다. SQL 작성·실행 없음.
- 2026-07-26 P3-1: 서클 돌봄 교대 MVP의 첫 단위로 상태 계약을 `requested → accepted → completed`로 고정했다. 수락자만 요청을 수락하고 수락된 교대를 완료할 수 있으며, 요청자의 상태 변경·단계 건너뛰기·완료 상태 재작성은 순수 함수에서 거부한다. 아직 UI·저장소·SQL은 연결하지 않았고 기존 기능·URL·데이터 변경도 없다. `npx tsc --noEmit`, 관련 테스트 14건, 변경 파일 lint, production build를 통과했다. SQL 작성·실행 없음.
- 2026-07-26 P3-2: 서클 돌봄 교대 요청의 입력 계약을 추가했다. 요청자·수락자 필수, 본인에게 교대 요청 금지, 미래 시각만 허용, 메모 500자 제한을 순수 검증 함수와 회귀 테스트로 고정했다. 아직 UI·저장소·SQL은 연결하지 않았고 기존 기능·URL·데이터 변경도 없다. `npx tsc --noEmit`, 관련 테스트 6건, 변경 파일 lint, production build를 통과했다. SQL 작성·실행 없음.
- 2026-07-26 P3-3: 기존 서클 관리 화면에 돌봄 교대의 요청 → 수락 → 완료 흐름을 소개하는 진입 카드를 `NEXT_PUBLIC_FF_CJ_CARE_SHIFT` flag로 배선했다. flag off 또는 kill switch on이면 기존 화면이 그대로 유지되며, 이번 작은 단위는 진입 영역만 추가하고 실제 요청 UI·저장소·SQL은 연결하지 않았다. 기존 기능·URL·데이터 삭제나 변경은 없고 SQL 작성·실행 없음. `npx tsc --noEmit`, 관련 테스트 17건, production build를 통과했다. 변경 파일 lint는 신규 구간 오류 없이 기존 안내 문구의 `react/no-unescaped-entities` 2건과 기존 미사용 import 경고 1건 때문에 실패했다.
- 2026-07-26 P3-4: 돌봄 교대 저장소용 멱등 마이그레이션 `box/supabase_care_shifts_migration.sql`을 작성했다. 요청자·수락자·미래 시작 시각·500자 메모·`requested → accepted → completed` 상태/시각 정합성을 제약과 트리거로 방어하고, 참여자만 조회하며 요청자만 생성·수락자만 상태 전환하도록 RLS를 추가했다. 기존 테이블·URL·데이터 삭제나 대량 변경은 없으며 운영 SQL은 실행하지 않았다. 실행 전 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 재확인이 필요하다.
- 2026-07-26 P3-5: P3 flag가 켜진 경우에만 돌봄 교대 요청을 생성하는 `POST /api/care-shifts`를 추가했다. 인증 사용자만 요청할 수 있고 요청자 ID는 세션에서 고정하며, 참여자·미래 시각·메모 길이 검증과 분당 10회 제한을 적용했다. 실제 권한은 P3-4 RLS에 맡기며 미적용 스키마는 `not_ready`로 안전하게 응답한다. 기존 기능·URL·데이터 삭제나 변경은 없고 SQL은 실행하지 않았다.
- 2026-07-26 P3-6: 인증 사용자가 요청자 또는 수락자인 돌봄 교대만 조회하는 `GET /api/care-shifts`를 추가했다. P3 flag가 꺼지면 노출하지 않고, 시작 시각순 최대 50건만 반환하며 실제 가시성은 P3-4 RLS로 다시 제한한다. 미적용 스키마는 `not_ready`로 안전하게 응답한다. 기존 기능·URL·데이터 삭제나 변경은 없고 SQL 작성·실행 없음. `npx tsc --noEmit`, 변경 파일 lint, 관련 테스트 17건, production build를 통과했다.
- 2026-07-26 P3-7: 수락자가 돌봄 교대를 `requested → accepted → completed` 순서로만 전환하는 `PATCH /api/care-shifts`를 추가했다. 요청 상태를 UPDATE 조건에 함께 넣어 동시·중복 요청이 단계를 건너뛰지 못하게 하고, 세션 사용자와 수락자 일치·분당 20회 제한·P3 flag를 적용했다. 실제 권한과 상태 시각 기록은 P3-4 RLS/트리거가 다시 방어하며, 미적용 스키마는 `not_ready`로 안전하게 응답한다. 기존 기능·URL·데이터 삭제나 변경은 없고 SQL 작성·실행 없음.
- 2026-07-26 P3-8: P3 flag가 켜진 서클 화면에서 수락된 이웃·미래 시작 시각·선택 메모를 입력해 돌봄 교대를 요청하는 폼을 `POST /api/care-shifts`에 연결했다. 수락된 서클 이웃이 없으면 초대 수락 안내만 표시하며, 제출 중 중복 요청을 막고 성공 뒤 시각·메모를 초기화한다. 같은 파일의 기존 lint 오류 2건과 미사용 타입 경고 1건도 정리했다. 기존 기능·URL·데이터 삭제나 변경은 없고 SQL 작성·실행 없음. P3 마이그레이션은 아직 운영에 실행하지 않았다. `npx tsc --noEmit`, 관련 테스트 17건, 변경 파일 lint, production build를 통과했다.
- 2026-07-26 P3-9: 서클의 돌봄 교대 요청 폼이 camelCase 필드를 보내 API의 snake_case 입력 계약과 맞지 않아 모든 요청이 `invalid_params`로 거절되던 배선 오류를 수정했다. 요청 본문을 `circle_id`, `assignee_id`, `starts_at`으로 맞췄으며 API·DB 계약과 기존 URL·데이터는 변경하지 않았다. SQL 작성·실행 없음. `npx tsc --noEmit`, 관련 테스트 17건, 변경 파일 lint, production build를 통과했다.
- 2026-07-26 P3-10: P3 flag가 켜진 서클 화면에서 `GET /api/care-shifts`를 호출해 요청자·수락자가 자신의 돌봄 교대 일정, 메모, 요청/수락/완료 상태를 확인하는 목록을 추가했다. 새 요청 성공 뒤 목록을 즉시 갱신하며 flag off 또는 kill switch on이면 기존 화면을 유지한다. 기존 기능·URL·데이터 삭제나 변경은 없고 P3 멱등 마이그레이션은 아직 운영에 실행하지 않았다. `npx tsc --noEmit`, 관련 테스트 17건, 변경 파일 lint, production build를 통과했다.
- 2026-07-26 P3-11: 서클의 돌봄 교대 목록에서 수락자에게만 `요청됨 → 수락됨 → 완료` 순서의 상태 버튼을 표시하고 기존 `PATCH /api/care-shifts`에 연결했다. 전환 중에는 중복 클릭을 막고 성공 뒤 목록을 갱신한다. 요청자에게는 상태 변경 버튼을 노출하지 않으며 기존 기능·URL·데이터 삭제나 변경은 없다. P3 멱등 마이그레이션은 아직 운영에 실행하지 않았다. `npx tsc --noEmit`, 관련 테스트 17건, 변경 파일 lint, production build를 통과했다.
- 2026-07-26 P3-12: 돌봄 교대 RLS가 사용하는 `is_accepted_circle_member` 보안 정의자 함수의 기본 실행 권한을 강화했다. PostgreSQL/Supabase의 기본 함수 권한을 고려해 `PUBLIC`뿐 아니라 `anon` 역할의 실행 권한도 명시적으로 회수하고 `authenticated`에만 허용한다. 기존 테이블·URL·데이터 삭제나 변경은 없으며 운영 SQL은 실행하지 않았다. `npx tsc --noEmit`, 돌봄 교대 관련 테스트 6건, `git diff --check`, production build를 통과했다.
- 2026-07-26 P3-13: 서클의 돌봄 교대 요청·상태 전환 실패 시 `rate_limited`, `not_ready` 같은 영문 API 오류 코드가 alert에 그대로 노출되던 것을 한국어 안내로 바꿨다. 오류 코드 → 한국어 메시지 매핑을 순수 함수 `describeCareShiftError`로 추가하고(알 수 없는 코드·비문자열은 한국어 기본 문구로 fail-safe), 서클 화면의 두 핸들러에 배선했으며 매핑·폴백 회귀 테스트를 추가했다. API 응답 계약·기존 기능·URL·데이터는 변경하지 않았고 flag 분기도 그대로다. SQL 작성·실행 없음(P3 멱등 마이그레이션은 여전히 운영 미실행). `npx tsc --noEmit`, 돌봄 교대 테스트 8건, 변경 파일 lint(오류·경고 0), production build를 통과했다.
- 2026-07-26 P3-14: 돌봄 교대 시작 시각 입력의 `min`이 `toISOString()`(UTC) 기준이라 KST 사용자가 최대 9시간 과거 시각을 고를 수 있고, 제출 시 서버 검증에서 `invalid_params`로만 거절되던 문제를 수정했다. 로컬 벽시계 기준 문자열을 만드는 순수 함수 `toDatetimeLocalValue`를 `lib/care-shift.ts`에 추가하고 서클 화면의 `min`에 배선했으며, 타임존 독립적인 형식·왕복 회귀 테스트 2건을 추가했다. 제출 페이로드(`new Date(...).toISOString()`)와 API·DB 계약, 기존 기능·URL·데이터, flag 분기는 변경하지 않았다. SQL 작성·실행 없음(P3 멱등 마이그레이션은 여전히 운영 미실행 — P3 완료 판정의 남은 게이트). 검증·커밋: 이 회차 CLI에서는 읽기 명령을 제외한 모든 실행(`node`/`npx`/`npm`, `git add`/`git commit`)이 권한 승인 차단되어 타입 체크·테스트·lint·build·커밋을 수행하지 못했다. 변경은 워킹 트리에 미커밋 상태로 남아 있으며, 다음 회차(또는 사장님 승인 후)에서 `npx tsc --noEmit` → `node --test tests/care-shift.test.mjs` → 변경 lint → build → 커밋 순으로 마무리 필요.
- 2026-07-26 P3-14 검증 보완: 돌봄 교대 `datetime-local` 최소 시각을 UTC가 아닌 사용자 로컬 벽시계 기준으로 생성하도록 수정했다. `toDatetimeLocalValue` 순수 함수와 로컬 시각·왕복 회귀 테스트 2건을 추가했고 기존 URL·데이터·flag 분기에는 변경이 없다. `npx tsc --noEmit`, 돌봄 교대 테스트 10건, 변경 파일 lint(오류·경고 0), production build를 통과했다. SQL 작성·실행 없음. P3 멱등 마이그레이션은 운영 미실행 상태이며 P3 완료 판정은 유보한다.
- 2026-07-26 P3-15: 돌봄 교대 목록이 시작 시각 오름차순 50건 제한 때문에 오래된 일정으로 채워져 새 요청이 보이지 않을 수 있는 문제를 막았다. 조회 시작 범위를 최근 1주로 제한하는 순수 함수와 회귀 테스트 2건을 추가하고 `GET /api/care-shifts`에 배선했다. 기존 URL·데이터·flag 분기는 유지했고 SQL 작성·실행은 없다(P3 멱등 마이그레이션은 운영 미실행). `npx tsc --noEmit`, 돌봄 교대 테스트 12건, 변경 파일 lint(오류·경고 0), production build를 통과했다.
- 2026-07-26 P3-16: 돌봄 교대 쓰기 트리거 함수 `guard_care_shift_write()`에 PostgreSQL 기본 `PUBLIC EXECUTE` 권한이 남지 않도록 `PUBLIC`·`anon`·`authenticated`의 직접 실행 권한을 명시적으로 회수했다. 트리거 자체의 호출은 유지되며 테이블·데이터·기존 URL에는 변경이 없다. 운영 SQL은 실행하지 않았고 P3 멱등 마이그레이션은 여전히 CITY ref 확인 후 실행이 필요하다. `npx tsc --noEmit`, 돌봄 교대 테스트 12건, 변경 파일 lint, `git diff --check`, production build를 통과했다.
- 2026-07-26 P3-17: 돌봄 교대 멱등 마이그레이션의 핵심 계약을 정적 회귀 테스트로 고정했다. 재실행 가능한 테이블·인덱스 생성, 파괴적 테이블 SQL 부재, 참여자 전용 조회, `authenticated`의 DELETE 미허용, 수락자 전용 순차 상태 전이, 트리거 함수 직접 실행 권한 회수를 검증한다. 운영 SQL은 실행하지 않았고 기존 기능·URL·데이터 변경도 없다. P3 마이그레이션은 여전히 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 확인 후 실행해야 하며 P3 완료 판정은 유보한다. `npx tsc --noEmit`, 돌봄 교대 관련 테스트 15건, 변경 파일 lint, `git diff --check`, production build를 통과했다.
- 2026-07-26 P3-18: 동일 서클·요청자·수락자·시작 시각의 열린(`requested`/`accepted`) 돌봄 교대가 중복 생성되지 않도록 멱등 마이그레이션에 부분 고유 인덱스를 추가했다. 완료된 교대는 제외해 이후 동일 슬롯 재요청을 허용하고, API는 DB `unique_violation`을 `409 duplicate_request`로 변환해 한국어 안내를 표시한다. 인덱스 계약과 오류 안내 회귀 테스트를 추가했으며 기존 URL·데이터 삭제·대량 변경은 없다. 운영 SQL은 실행하지 않았고, 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl`을 확인하기 전까지 P3 완료 판정은 유보한다. `npx tsc --noEmit`, 돌봄 교대 관련 테스트 16건, 변경 파일 lint, `git diff --check`, production build를 통과했다.
- 2026-07-26 P3-19: 인증 사용자가 PostgREST로 API를 우회해 상태를 전환하면서 `accepted_at`·`completed_at`을 임의 값으로 지정할 수 없도록 쓰기 트리거가 전환 시각을 DB `now()`로 강제하게 보강했다. 수락 시 완료 시각은 비우고, 완료 시 기존 수락 시각은 보존한다. DB 시각 강제 계약 회귀 테스트를 추가했으며 기존 URL·데이터 삭제·대량 변경은 없다. 운영 SQL은 실행하지 않았고, 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl`을 확인하기 전까지 P3 완료 판정은 유보한다. `npx tsc --noEmit`, 돌봄 교대 관련 테스트 17건, 변경 파일 lint, `git diff --check`, production build를 통과했다.
- 2026-07-26 P3-20: 돌봄 교대 생성·상태 전환 API가 malformed UUID를 Postgres에 전달해 `22P02`와 일반 502로 처리할 수 있던 입력 경로를 차단했다. 서클·수락자·교대 ID를 DB 호출 전에 UUID 형식으로 검증해 `400 invalid_params`로 일관되게 응답하고 회귀 테스트를 추가했다. 기존 기능·URL·데이터 변경은 없으며 운영 SQL은 실행하지 않았다. P3 멱등 마이그레이션은 CITY ref 재확인 후 실행 전 상태다. `npx tsc --noEmit`, 돌봄 교대 관련 테스트 18건, 변경 파일 lint, `git diff --check`, production build를 통과했다.
- 2026-07-26 P3-21: 서클 화면의 돌봄 교대 목록 조회가 실패(429·502·503 등)해도 오류를 삼키고 "아직 돌봄 교대 요청이 없어요"라는 빈 상태로 잘못 표시되던 문제를 수정했다. 조회 실패 상태를 분리해 한국어 안내("돌봄 교대 목록을 불러오지 못했어요.")와 "다시 시도" 버튼을 표시하며, 재시도 시 실패 상태를 초기화한다. P3 flag 영역 내부의 UI 전용 변경으로 기존 기능·URL·데이터 삭제나 변경은 없다. SQL 작성·실행 없음(P3 멱등 마이그레이션은 여전히 운영 미실행 — CITY ref 확인 후 실행 필요, P3 완료 판정 유보). `npx tsc --noEmit`, 돌봄 교대 관련 테스트 18건, 변경 파일 lint(오류·경고 0), production build, `git diff --check`를 통과했다.
- 2026-07-26 P3-22: 인증 사용자의 돌봄 교대 목록 조회에 사용자별 분당 60회 제한을 추가해 반복 새로고침·자동 호출이 API와 DB를 과도하게 점유하지 않도록 했다. 제한 초과 시 기존 UI 오류 매핑이 처리하는 `429 rate_limited`를 반환하며 기존 기능·URL·데이터 변경은 없다. SQL 작성·실행 없음(P3 멱등 마이그레이션은 CITY ref 확인 후 실행 필요, P3 완료 판정 유보). `npx tsc --noEmit`, 돌봄 교대 관련 테스트 18건, 변경 파일 lint, `git diff --check`, production build를 통과했다.
- 2026-07-27 P3-23: 돌봄 교대 시작 시각에 60일 미래 상한을 추가했다. 삭제 수단이 없는 목록에 수십 년 뒤 같은 비상식적 원미래 교대가 영구히 남는 오용을 요청 검증(`starts_at_too_far`)에서 거절하고, 서클 폼의 `datetime-local`에도 같은 상한을 `max`로 배선했다. 상한 초과·경계 허용 회귀 테스트 2건을 추가했으며 기존 기능·URL·데이터 삭제나 변경은 없다. SQL 작성·실행 없음(P3 멱등 마이그레이션은 CITY ref 확인 후 실행 필요, P3 완료 판정 유보). `npx tsc --noEmit`, 돌봄 교대 테스트 15건, 변경 파일 lint, `git diff --check`, production build를 통과했다.
- 2026-07-27 P3-24: API의 돌봄 교대 60일 미래 상한을 멱등 마이그레이션의 테이블 CHECK와 INSERT RLS에도 동일하게 반영해, 인증 사용자가 API를 우회해 PostgREST로 직접 요청해도 원미래 교대를 만들 수 없도록 보강했다. SQL 계약 회귀 테스트를 추가했으며 기존 기능·URL·데이터 삭제나 변경은 없다. 운영 SQL은 실행하지 않았고, 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 확인 후 전체 마이그레이션을 실행하기 전까지 P3 완료 판정은 유보한다. `npx tsc --noEmit`, 돌봄 교대 관련 테스트 21건, 변경 파일 lint, `git diff --check`, production build를 통과했다.
- 2026-07-27 P3-25: 돌봄 교대 생성·상태 전환 API가 유효한 JSON이지만 객체가 아닌 `null`·배열·원시값 본문을 필드 접근하다 500으로 처리할 수 있던 경로를 차단했다. JSON 파싱 후 일반 객체 여부를 먼저 검사해 `400 invalid_body`로 일관되게 응답하고 회귀 테스트를 추가했다. 기존 기능·URL·데이터 삭제나 변경은 없으며 운영 SQL은 실행하지 않았다. P3 멱등 마이그레이션은 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 재확인 후 실행 전 상태다. `npx tsc --noEmit`, 돌봄 교대 관련 테스트 23건, 변경 파일 lint, `git diff --check`, production build를 통과했다.
- 2026-07-27 P3-26: 돌봄 교대 시작 시각 입력을 시간대가 명시된 RFC 3339 형식으로 제한했다. 브라우저나 DB 세션 시간대에 따라 다르게 해석될 수 있는 `datetime-local` 원문·공백 구분 시각은 `invalid_starts_at`으로 거절하고, UI가 기존처럼 `toISOString()`으로 전송하는 UTC 시각과 명시적 오프셋 시각만 허용한다. 기존 기능·URL·데이터 변경은 없고 운영 SQL은 실행하지 않았다. P3 멱등 마이그레이션은 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 재확인 후 실행 전 상태다. `npx tsc --noEmit`, 돌봄 교대 관련 테스트 24건, 변경 파일 lint, `git diff --check`, production build를 통과했다.
- 2026-07-27 P3-27: JavaScript `Date.parse`가 `2026-02-30`이나 `24:00` 같은 잘못된 시각을 다음 달·다음 날로 자동 보정해 돌봄 교대 요청을 허용할 수 있던 경로를 차단했다. RFC 3339 형식 검사에 달별 일수와 시·분·초·오프셋 범위 검증을 추가하고 달력·시계 초과 회귀 테스트를 보강했다. 기존 기능·URL·데이터 변경은 없으며 운영 SQL은 실행하지 않았다. P3 멱등 마이그레이션은 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 재확인 후 실행 전 상태다. `npx tsc --noEmit`, 돌봄 교대 관련 테스트 25건, 변경 파일 lint, `git diff --check`, production build를 통과했다.
- 2026-07-27 P3-28: 돌봄 교대 시작 시각의 RFC 3339 오프셋을 표준 최대 범위인 `±14:00`으로 제한했다. `+14:01`, `-15:00`처럼 JavaScript가 해석할 수 있어도 RFC 3339에는 유효하지 않은 시각을 API 입력 단계에서 거절하며 경계값 회귀 테스트를 추가했다. 기존 기능·URL·데이터 변경은 없고 운영 SQL은 실행하지 않았다. P3 멱등 마이그레이션은 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 재확인 후 실행 전 상태다. `npx tsc --noEmit`, 돌봄 교대 관련 테스트 26건, 변경 파일 lint, `git diff --check`, production build를 통과했다.
- 2026-07-27 P3-29: 돌봄 교대 RFC 3339 오프셋의 분 범위를 코드에서 명시적으로 `00~59`로 제한했다. 현재 런타임의 `Date.parse` 거절 동작에만 의존하지 않아 파서 구현 차이에도 `+09:60` 같은 비표준 오프셋을 일관되게 차단한다. 기존 회귀 테스트가 이 경계를 검증하며 기존 기능·URL·데이터 변경은 없다. 운영 SQL은 실행하지 않았고 P3 멱등 마이그레이션은 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 재확인 후 실행 전 상태다.
- 2026-07-27 P3-30: UUID는 대소문자를 구분하지 않지만 문자열 비교는 구분해, 요청자 UUID와 수락자 UUID의 문자 대소문자만 다르면 자기 자신 대상 교대 요청이 API 검증을 통과할 수 있던 경로를 차단했다. 참여자 비교를 대소문자 비구분으로 바꾸고 회귀 테스트를 추가했다. 기존 기능·URL·데이터 변경은 없으며 운영 SQL은 실행하지 않았다. P3 멱등 마이그레이션은 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 재확인 후 실행 전 상태다. `npx tsc --noEmit`, 돌봄 교대 관련 테스트 21건, 변경 파일 lint, `git diff --check`, production build를 통과했다.
- 2026-07-27 P3-31: `PATCH /api/care-shifts`가 목표 상태별 기대 현재 상태를 인라인 삼항식으로 재작성해 상태 전이 계약과 이중화되어 있던 것을 정리했다. 전이 계약(`ALLOWED_TRANSITIONS`)에서 유일한 현재 상태를 역산하는 순수 함수 `getRequiredCurrentStatus`를 추가하고 라우트를 이 함수로 배선해, 향후 계약 변경 시 API 매핑이 어긋나는 드리프트를 차단했다. 역산·정합성 회귀 테스트 2건을 추가했으며 동작 변경과 기존 기능·URL·데이터 삭제는 없다. 운영 SQL은 실행하지 않았고 P3 멱등 마이그레이션은 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 재확인 후 실행 전 상태다. `npx tsc --noEmit`, 돌봄 교대 관련 테스트 29건, 변경 파일 lint(오류·경고 0), `git diff --check`, production build를 통과했다.
- 2026-07-27 P3-32: 돌봄 교대 마이그레이션 적용 전 또는 PostgREST 스키마 캐시 갱신 전 `care_shifts` 조회가 `PGRST205`를 반환할 때 일반 502로 오인하던 API 경로를 보강했다. PostgreSQL 미정의 테이블 `42P01`과 함께 `503 not_ready`로 일관되게 매핑하며 생성·조회·상태 전환 모두 같은 판독 함수를 사용한다. 회귀 테스트를 추가했고 기존 기능·URL·데이터 변경은 없다. 운영 SQL은 실행하지 않았으며 P3 멱등 마이그레이션은 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 재확인 후 실행 전 상태다. `npx tsc --noEmit`, 돌봄 교대·SQL·feature flag 관련 테스트 41건, 변경 파일 lint, `git diff --check`, production build를 통과했다.
- 2026-07-27 P3-33: API 시각 검증을 통과한 돌봄 교대 요청이 밀리초 차이로 DB CHECK(`starts_at > created_at` 등)에 걸리면 Postgres `23514`(check_violation)가 일반 `502 create_failed`로 처리되어 서버 장애로 오인되고 error 로그가 남던 경로를 보강했다. `isCareShiftCheckViolationCode` 판독 함수를 추가해 생성 API에서 `400 invalid_params`로 매핑하고(기존 한국어 안내 재사용), 코드 판별·`not_ready`와의 비중복 회귀 테스트를 추가했다. 기존 기능·URL·데이터 변경은 없으며 운영 SQL은 실행하지 않았다. P3 멱등 마이그레이션은 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 재확인 후 실행 전 상태다. `npx tsc --noEmit`, 돌봄 교대 테스트 25건, 변경 파일 lint(오류·경고 0), `git diff --check`, production build를 통과했다.
- 2026-07-27 P3-34: 돌봄 교대 마이그레이션 적용 직후 PostgREST 스키마 캐시가 새 컬럼을 아직 인식하지 못해 `PGRST204`를 반환하는 경우를 기존 테이블 캐시 누락(`PGRST205`)·미정의 테이블(`42P01`)과 동일한 `503 not_ready`로 처리하도록 보강했다. 생성·조회·상태 전환 API가 공용 판독 함수를 사용하므로 세 경로에 함께 적용되며 회귀 테스트를 추가했다. 기존 기능·URL·데이터 변경은 없고 운영 SQL은 실행하지 않았다. P3 멱등 마이그레이션은 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 재확인 후 실행 전 상태다. `npx tsc --noEmit`, 돌봄 교대 테스트 25건, 변경 파일 lint(오류·경고 0), `git diff --check`, production build를 통과했다.
- 2026-07-27 P3-35: 서클 화면의 돌봄 교대 목록 조회 실패 안내가 서버 오류 코드와 무관하게 일반 실패 문구만 보여, 마이그레이션 미적용(`not_ready` — 현재 운영의 실제 상태)이나 조회 제한(`rate_limited`)도 장애로 오인되던 것을 보완했다. 실패 상태를 문자열로 바꿔 기존 `describeCareShiftError` 매핑을 재사용하고, 알 수 없는 코드·네트워크 예외는 기존 문구로 폴백하며 "다시 시도" 버튼과 flag 분기는 그대로다. `list_failed` 폴백 회귀 검증을 기존 테스트에 추가했고 API 계약·기존 기능·URL·데이터 변경은 없다. SQL 작성·실행 없음(P3 멱등 마이그레이션은 CITY ref 확인 후 실행 필요, P3 완료 판정 유보). `npx tsc --noEmit`, 돌봄 교대·SQL 관련 테스트 31건, 변경 파일 lint(오류·경고 0), `git diff --check`, production build를 통과했다.
- 2026-07-27 P3-36: 돌봄 교대 상태 전환이 동시 요청 또는 오래된 화면 상태 때문에 DB 쓰기 트리거에서 `P0001`로 거절될 때 일반 `502 transition_failed`로 오인되던 경로를 보강했다. 트리거 전이 위반을 기존 `409 invalid_transition`으로 매핑해 “이미 처리된 요청” 안내와 목록 새로고침을 유도하고 판독 회귀 테스트를 추가했다. 기존 기능·URL·데이터 변경은 없고 운영 SQL은 실행하지 않았다. P3 멱등 마이그레이션은 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 재확인 후 실행 전 상태다. `npx tsc --noEmit`, 돌봄 교대·SQL·feature flag 관련 테스트 43건, 변경 파일 lint(오류·경고 0), `git diff --check`, production build를 통과했다.
- 2026-07-27 P3-37: 돌봄 교대 멱등 마이그레이션 전체를 `begin`/`commit` 트랜잭션으로 감싸 테이블·인덱스·함수·트리거·RLS 중간 실패 시 일부 스키마만 남지 않도록 원자성을 보강했다. PostgREST 스키마 reload 알림도 commit과 같은 트랜잭션에서 전달되며 이를 고정하는 SQL 회귀 테스트를 추가했다. 기존 기능·URL·데이터 삭제나 대량 변경은 없고 운영 SQL은 실행하지 않았다. P3 마이그레이션은 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 재확인 후 전체 실행 전 상태다. `npx tsc --noEmit`, 돌봄 교대·SQL·feature flag 관련 테스트 44건, 변경 파일 lint(오류·경고 0), `git diff --check`, production build를 통과했다.
- 2026-07-27 P3-38: 인증 사용자가 API를 우회해 돌봄 교대를 직접 생성할 때 `created_at`을 임의 값으로 지정해 감사 시각과 시작 시각 CHECK 기준을 왜곡하지 못하도록, 쓰기 트리거의 INSERT 분기에서 `created_at`을 DB `now()`로 강제했다. 기존 API는 `created_at`을 보내지 않아 동작 변경은 없으며 DB 시각 강제 계약 회귀 테스트를 추가했다. 기존 기능·URL·데이터 삭제나 대량 변경은 없고 운영 SQL은 실행하지 않았다. P3 마이그레이션은 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 재확인 후 전체 실행 전 상태다. `npx tsc --noEmit`, 돌봄 교대·SQL·feature flag 관련 테스트 44건, 변경 파일 lint(오류·경고 0), `git diff --check`, production build를 통과했다.
- 2026-07-27 P3-39: 돌봄 교대 메모에 PostgreSQL text가 저장할 수 없는 NUL 또는 목록 표시를 깨뜨리는 C0/C1 제어 문자가 들어오면 DB 오류나 UI 교란으로 이어지기 전에 API 입력 검증에서 거절하도록 보강했다. 탭·줄바꿈·CR은 메모 서식으로 유지하며 NUL·ESC·C1 경계와 정상 한글 메모 회귀 테스트를 추가했다. 기존 기능·URL·데이터 변경은 없고 운영 SQL은 작성·실행하지 않았다. P3 멱등 마이그레이션은 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 재확인 후 실행 전 상태다. `npx tsc --noEmit`, 돌봄 교대·SQL·feature flag 관련 테스트 45건, 변경 파일 lint(오류·경고 0), `git diff --check`, production build를 통과했다.
- 2026-07-27 P3-40: API를 우회한 PostgREST 직접 INSERT에서도 돌봄 교대 메모의 제어문자를 차단하도록 멱등 마이그레이션에 CHECK를 추가했다. 탭·줄바꿈·CR은 기존 메모 서식으로 허용하고 나머지 제어문자는 거절하며 SQL 계약 회귀 테스트를 추가했다. 기존 기능·URL·데이터 삭제나 대량 변경은 없고 운영 SQL은 실행하지 않았다. P3 멱등 마이그레이션은 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 재확인 후 실행 전 상태다. `npx tsc --noEmit`, 돌봄 교대·SQL·feature flag 관련 테스트 46건, 변경 파일 lint(오류·경고 0), `git diff --check`, production build를 통과했다.
- 2026-07-27 P3-41: 돌봄 교대 생성·상태 전환 API가 크기 제한 없이 `request.json()`으로 수 MB JSON 본문도 통째로 파싱한 뒤에야 500자 메모 검증을 하던 경로를 차단했다. 본문을 텍스트로 받아 16KB 초과 시 파싱 전에 `413 payload_too_large`로 거절하는 순수 함수 `isCareShiftBodyTooLarge`를 추가하고 두 라우트에 배선했으며, 한국어 안내 매핑과 경계값(16KB 허용·+1 거절)·빈 본문 회귀 테스트를 추가했다. 기존 기능·URL·데이터 변경은 없고 SQL 작성·실행 없음(P3 멱등 마이그레이션은 CITY ref `sozxbnvgsougkliibnxl` 확인 후 실행 필요, P3 완료 판정 유보). `npx tsc --noEmit`, 돌봄 교대·SQL·feature flag 관련 테스트 47건, 변경 파일 lint(오류·경고 0), `git diff --check`, production build를 통과했다.
- 2026-07-27 P3-42: 돌봄 교대 API의 16KB 본문 제한이 JavaScript 문자열 길이(UTF-16 코드 단위)를 사용해 한글 등 멀티바이트 본문을 실제 전송 크기보다 작게 계산하던 경로를 보강했다. `TextEncoder`로 UTF-8 바이트 길이를 측정하고 상수를 `CARE_SHIFT_MAX_BODY_BYTES`로 명확히 바꿨으며, ASCII와 3바이트 한글의 경계값 회귀 테스트를 추가했다. 기존 기능·URL·데이터 변경은 없고 SQL 작성·실행 없음(P3 멱등 마이그레이션은 CITY ref `sozxbnvgsougkliibnxl` 확인 후 실행 필요, P3 완료 판정 유보). `npx tsc --noEmit`, 돌봄 교대·SQL·feature flag 관련 테스트 47건, 변경 파일 lint(오류·경고 0), `git diff --check`, production build를 통과했다. Claude Code 직접 검토는 124초 무응답으로 타임아웃되어 로컬 검토로 진행했다.
- 2026-07-27 P3-43: 돌봄 교대 요청이 `400 invalid_params`로 거절될 때 서버가 `details`에 담아 주는 구체적 사유(`starts_at_not_future`, `note_too_long` 등)를 UI가 버리고 항상 "입력한 내용을 다시 확인해 주세요"만 표시하던 것을 보완했다. 8종 검증 사유 전부를 한국어 안내로 매핑하는 순수 함수 `describeCareShiftValidationErrors`를 추가하고(60일 상한·500자 제한 숫자는 기존 상수에서 유도, 프로토타입 키·비배열·미지 코드 fail-safe 폴백, `Record<CareShiftRequestValidationError,…>` 타입으로 새 사유 추가 시 매핑 누락을 컴파일 오류로 강제) 서클 생성 핸들러에 배선했으며, 매핑·우선순위·폴백 회귀 테스트 3건을 추가했다. API 응답 계약·기존 기능·URL·데이터·flag 분기는 변경하지 않았다. SQL 작성·실행 없음(P3 멱등 마이그레이션은 CITY ref `sozxbnvgsougkliibnxl` 확인 후 실행 필요, P3 완료 판정 유보). Claude Code의 자체 검증은 권한 제한으로 수행되지 않았으나 로컬에서 `npx tsc --noEmit`, 돌봄 교대 테스트 31건, 변경 파일 lint(오류·경고 0), `git diff --check`, production build를 통과했다.
- 2026-07-27 P3-44: 돌봄 교대 메모 500자 제한이 JavaScript UTF-16 코드 단위를 사용해 이모지 같은 보조 평면 문자 1개를 2자로 계산하고, PostgreSQL `char_length`보다 먼저 정상 메모를 거절하던 불일치를 보완했다. `careShiftNoteLength`가 Unicode 코드 포인트 수를 세도록 하고 검증에 배선했으며, 고양이 이모지 500개 허용·501개 거절 회귀 테스트를 추가했다. 기존 기능·URL·데이터·flag 분기는 변경하지 않았다. SQL 작성·실행 없음(P3 멱등 마이그레이션은 CITY ref `sozxbnvgsougkliibnxl` 확인 후 실행 필요, P3 완료 판정 유보). Claude Code 직접 검토는 183초 무응답으로 타임아웃되어 로컬 검토로 진행했다. `npx tsc --noEmit`, 돌봄 교대·SQL·feature flag 관련 테스트 51건, 변경 파일 lint(오류·경고 0), `git diff --check`, production build를 통과했다.
- 2026-07-27 P3-45: 돌봄 교대 생성·상태 전환 API가 `Content-Length`로 이미 16KB 초과가 확인된 요청도 본문 스트림 전체를 읽은 뒤 거절하던 경로를 보강했다. 유효한 선언 길이가 상한을 초과하면 본문을 읽기 전에 `413 payload_too_large`로 응답하며, 헤더가 없거나 청크 전송·비정상 값이면 기존 UTF-8 실측 검증을 그대로 사용한다. 경계값·헤더 없음·비정상 값 회귀 테스트를 추가했고 기존 기능·URL·데이터·flag 분기는 변경하지 않았다. 운영 SQL은 실행하지 않았으며 P3 멱등 마이그레이션은 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 재확인 후 실행 전 상태다. `npx tsc --noEmit`, 돌봄 교대·SQL 관련 테스트 41건, 변경 파일 lint(오류·경고 0), `git diff --check`, production build를 통과했다.
- 2026-07-27 P3-46: 돌봄 교대 생성·상태 전환 API가 JSON이 아닌 본문 형식도 파싱하던 경로를 차단했다. `Content-Type`이 `application/json`(대소문자·charset 허용)이 아니거나 누락되면 본문을 읽기 전에 `415 unsupported_media_type`으로 응답하고 UI 오류 안내를 추가했다. 미디어 타입 판독 회귀 테스트를 추가했으며 기존 기능·URL·데이터·flag 분기는 변경하지 않았다. 운영 SQL은 실행하지 않았고 P3 멱등 마이그레이션은 CITY ref `sozxbnvgsougkliibnxl` 재확인 후 실행 전 상태다. `npx tsc --noEmit`, 돌봄 교대·SQL·feature flag 관련 테스트 53건, 변경 파일 lint(오류·경고 0), `git diff --check`, production build를 통과했다.
- 2026-07-27 P3-47: 돌봄 교대 요청 상대 검증이 `circle_members`의 accepted 행만 허용해, 멤버 행을 별도로 갖지 않는 서클장에게 교대를 요청하면 RLS에서 거절되던 경로를 보완했다. 기존 멱등 마이그레이션의 `is_accepted_circle_member`가 accepted 멤버뿐 아니라 해당 서클의 `owner_id`도 허용하도록 확장하고 SQL 계약 회귀 테스트를 추가했다. 기존 기능·URL·데이터·flag 분기는 변경하지 않았고 운영 SQL은 실행하지 않았다. P3 마이그레이션은 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 재확인 후 전체 실행 전 상태다. `npx tsc --noEmit`, 돌봄 교대·SQL·feature flag 관련 테스트 54건, 변경 파일 lint(오류·경고 0), `git diff --check`, production build를 통과했다.
- 2026-07-27 P3-48: 돌봄 교대 생성·상태 전환 API의 `Content-Length` 조기 차단에서 매우 긴 숫자 문자열이 JavaScript `Infinity`로 변환되면 초과 요청으로 판정되지 않던 경계 조건을 보강했다. 숫자로 표현할 수 있어도 안전한 정수가 아니면 16KB 초과와 동일하게 본문 스트림을 읽기 전에 `413 payload_too_large`로 거절하며 회귀 테스트를 추가했다. 기존 기능·URL·데이터·flag 분기는 변경하지 않았고 운영 SQL은 실행하지 않았다. P3 멱등 마이그레이션은 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 재확인 후 실행 전 상태다. `npx tsc --noEmit`, 돌봄 교대·SQL·feature flag 관련 테스트 54건, 변경 파일 lint(오류·경고 0), `git diff --check`, production build를 통과했다.
- 2026-07-27 P3-49: Supabase의 public 테이블 기본 권한으로 `authenticated`에 DELETE가 남을 수 있어, 돌봄 교대 마이그레이션이 select/insert/update만 grant해도 삭제 권한을 확실히 제거하지 못하던 경로를 보강했다. `authenticated`의 기존 테이블 권한을 먼저 전부 revoke한 뒤 필요한 3개 권한만 다시 grant하고 SQL 계약 테스트를 강화했다. 롤백 안내의 DELETE 문구를 실제 GRANT 문으로 오인하던 테스트 정규식도 SQL 문 시작 기준으로 좁혔다. 기존 기능·URL·데이터·flag 분기는 변경하지 않았고 운영 SQL은 실행하지 않았다. P3 멱등 마이그레이션은 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 재확인 후 실행 전 상태다. Claude Code가 타임아웃 뒤 백그라운드에서 제안 변경을 남겨 로컬에서 범위와 안전성을 재검토했다. `npx tsc --noEmit`, 돌봄 교대·SQL·feature flag 관련 테스트 54건, 변경 파일 lint(오류·경고 0), `git diff --check`, production build를 통과했다.
- 2026-07-27 P3-50: 인증된 사용자의 돌봄 교대 목록 응답에 `Cache-Control: private, no-store`를 명시해 브라우저·중간 캐시에 개인별 교대 정보가 남지 않도록 보강했다. 응답 캐시 정책 회귀 테스트를 추가했으며 기존 기능·URL·데이터·flag 분기는 변경하지 않았다. 운영 SQL은 실행하지 않았고 P3 멱등 마이그레이션은 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 재확인 후 실행 전 상태다. `npx tsc --noEmit`, 돌봄 교대·SQL 관련 테스트 44건, 변경 파일 lint(오류·경고 0), `git diff --check`, production build를 통과했다. Claude Code 직접 검토는 244초 무응답으로 종료되어 로컬 검토로 전환했다.
- 2026-07-27 P3-51: 인증 사용자별 돌봄 교대 GET 성공 응답에만 있던 `private, no-store`를 404·401·429·403·502·503 오류 응답에도 일관되게 적용해 브라우저·중간 캐시가 사용자별 오류 상태를 재사용할 여지를 차단했다. 공용 헤더 상수와 성공·오류 전체 배선 회귀 테스트를 추가했으며 기존 기능·URL·데이터 변경은 없다. SQL 작성·실행 없음(P3 멱등 마이그레이션은 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 재확인 후 실행 필요, P3 완료 판정 유보). `npx tsc --noEmit`, 돌봄 교대·SQL·feature flag 관련 테스트 55건, 변경 파일 lint(오류·경고 0), `git diff --check`, production build를 통과했다. Claude Code 직접 검토는 184초 무응답으로 타임아웃되어 로컬 검토로 진행했다.
- 2026-07-27 P3-52: 돌봄 교대 GET에 적용한 개인 응답 캐시 방지 정책이 POST·PATCH에는 누락되어 있던 계약 불일치를 보완했다. 생성·상태 전환의 성공 응답과 flag off·인증·입력·DB 오류 응답 전체에 `Cache-Control: private, no-store`를 적용하고 핸들러별 전체 배선 회귀 테스트를 추가했다. 기존 기능·URL·데이터·flag 분기는 변경하지 않았다. P0 기준 SQL은 재실행하지 않았고 신규 SQL 작성·운영 SQL 실행도 없다(P3 멱등 마이그레이션은 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 재확인 후 실행 필요, P3 완료 판정 유보). Claude Code가 관련 API와 테스트를 읽고 이 누락을 제안했으며 로컬에서 범위와 안전성을 재검토했다. `npx tsc --noEmit`, 돌봄 교대·SQL·feature flag 관련 테스트 55건, 변경 파일 lint(오류·경고 0), `git diff --check`, production build를 통과했다.
- 2026-07-27 P3-53: 인증 쿠키 또는 Authorization 헤더에 따라 달라지는 돌봄 교대 개인 응답에 `Vary: Cookie, Authorization`을 추가했다. 기존 `private, no-store`와 함께 GET·POST·PATCH의 성공·오류 응답 전체에 공용 헤더로 적용해 잘못 구성된 중간 캐시가 인증 컨텍스트를 섞지 않도록 방어를 보강했다. 기존 기능·URL·데이터·flag 분기는 변경하지 않았다. P0 기준 SQL은 재실행하지 않았고 신규 SQL 작성·운영 SQL 실행도 없다(P3 멱등 마이그레이션은 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 재확인 후 실행 필요, P3 완료 판정 유보). Claude Code 검토 호출은 64초 타임아웃으로 응답 없이 종료되어 로컬 검토로 전환했다.
- 2026-07-27 P3-54: 다른 기기나 오래된 화면에서 이미 처리된 돌봄 교대의 상태 전환이 `409 invalid_transition`으로 충돌하면 안내만 표시하고 stale 목록을 남기던 경로를 보완했다. 해당 충돌에 한해 목록을 즉시 다시 조회해 현재 상태와 가능한 동작을 복구하며 회귀 테스트를 추가했다. 기존 기능·URL·데이터·flag 분기는 변경하지 않았다. P0 기준 SQL은 재실행하지 않았고 신규 SQL 작성·운영 SQL 실행도 없다(P3 멱등 마이그레이션은 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 재확인 후 실행 필요, P3 완료 판정 유보). Claude Code가 관련 UI·API·테스트를 읽고 안내 문구와 실제 복구 동작의 불일치를 제안했으며 로컬에서 범위와 안전성을 재검토했다. `npx tsc --noEmit`, 돌봄 교대·SQL 관련 테스트 45건, 변경 파일 lint(오류·경고 0), `git diff --check`, production build를 통과했다.
- 2026-07-27 P3-55: 초기 조회와 생성·상태 전환 후 재조회가 겹칠 때 늦게 끝난 오래된 돌봄 교대 응답이 최신 목록·오류·로딩 상태를 덮어쓸 수 있던 경쟁 조건을 차단했다. 요청 일련번호를 두고 최신 조회만 화면 상태를 갱신하도록 했으며 배선 회귀 테스트를 추가했다. 기존 기능·URL·데이터·flag 분기는 변경하지 않았다. P0 기준 SQL은 재실행하지 않았고 신규 SQL 작성·운영 SQL 실행도 없다(P3 멱등 마이그레이션은 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 재확인 후 실행 필요, P3 완료 판정 유보). Claude Code 직접 검토는 184초 타임아웃으로 응답 없이 종료되어 로컬 검토로 전환했다. `npx tsc --noEmit`, 돌봄 교대·SQL 관련 테스트 46건, 변경 파일 lint(오류·경고 0), `git diff --check`, production build를 통과했다.
- 2026-07-27 P3-56: 돌봄 교대 목록 조회 도중 로그아웃하거나 인증 사용자가 바뀌면 이전 인증 컨텍스트의 늦은 응답이 새 화면에 개인 교대 목록을 다시 채울 수 있던 경로를 차단했다. effect 정리 시 요청 일련번호를 무효화하고, 비로그인 또는 P3 flag off 상태에서는 목록·오류·로딩 상태를 비운다. 인증 컨텍스트 변경 배선 회귀 테스트를 추가했으며 기존 기능·URL·데이터·flag 분기는 변경하지 않았다. P0 기준 SQL은 재실행하지 않았고 신규 SQL 작성·운영 SQL 실행도 없다(P3 멱등 마이그레이션은 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 재확인 후 실행 필요, P3 완료 판정 유보). Claude Code 직접 검토는 185초 타임아웃으로 응답 없이 종료되어 로컬 검토로 전환했다. `npx tsc --noEmit`, 돌봄 교대·SQL 관련 테스트 47건, 변경 파일 lint(오류·경고 0), `git diff --check`, production build를 통과했다.
- 2026-07-27 P3-57: 돌봄 교대 생성·상태 전환 요청 도중 로그아웃하거나 인증 사용자가 바뀌면 이전 인증 컨텍스트의 늦은 성공·오류 응답이 입력값을 지우거나 alert·진행 상태를 새 화면에 반영할 수 있던 경로를 차단했다. 인증 컨텍스트 일련번호를 두고 변경 후 도착한 mutation의 UI 후속 효과를 폐기하며, 로그아웃/P3 flag off에서는 제출·전환 진행 상태도 비운다. 회귀 테스트를 추가했고 기존 기능·URL·데이터·flag 분기는 변경하지 않았다. P0 기준 SQL은 재실행하지 않았고 신규 SQL 작성·운영 SQL 실행도 없다(P3 멱등 마이그레이션은 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 재확인 후 실행 필요, P3 완료 판정 유보). Claude Code 직접 검토는 64초 타임아웃으로 응답 없이 종료되어 로컬 검토로 전환했다. `npx tsc --noEmit`, 돌봄 교대·SQL 관련 테스트 48건, 변경 파일 lint(오류·경고 0), `git diff --check`, production build를 통과했다.
- 2026-07-27 P3-58: 돌봄 교대 대상으로 선택한 서클 멤버를 내보낸 뒤에도 제거된 멤버 ID가 폼 상태에 남아, 화면상 선택은 비었지만 요청 버튼이 활성화되고 제출 시 권한 오류로 오인되던 경로를 차단했다. 멤버 제거 성공 시 해당 멤버가 현재 교대 대상일 때만 선택값을 초기화하고 배선 회귀 테스트를 추가했다. 기존 기능·URL·데이터·flag 분기는 변경하지 않았다. P0 기준 SQL은 재실행하지 않았고 신규 SQL 작성·운영 SQL 실행도 없다(P3 멱등 마이그레이션은 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 재확인 후 실행 필요, P3 완료 판정 유보). Claude Code가 관련 UI·API·테스트를 검토해 상태 정합성 결함과 최소 수정을 제안했으며 로컬에서 범위와 안전성을 재검토했다. `npx tsc --noEmit`, 돌봄 교대·SQL·feature flag 관련 테스트 60건, 변경 파일 lint(오류·경고 0), `git diff --check`, production build를 통과했다.
- 2026-07-27 P3-59: 돌봄 교대 상태 전환 응답을 기다리는 동안 로그아웃·계정 전환·P3 flag 해제가 발생하면, 이전 인증 컨텍스트의 후속 목록 재조회가 초기화된 화면 상태를 다시 덮어쓸 수 있던 경로를 차단했다. PATCH 응답 파싱 직후 인증 컨텍스트 일련번호를 확인해 오래된 응답의 충돌 재조회·오류 안내·성공 재조회를 모두 폐기하고 회귀 테스트를 추가했다. 기존 기능·URL·데이터·flag 분기는 변경하지 않았다. P0 기준 SQL은 재실행하지 않았고 신규 SQL 작성·운영 SQL 실행도 없다(P3 멱등 마이그레이션은 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 재확인 후 실행 필요, P3 완료 판정 유보). Claude Code가 결함을 찾아 최소 수정했으며 로컬에서 범위와 안전성을 재검토했다. `npx tsc --noEmit`, 돌봄 교대·SQL·feature flag 관련 테스트 61건, 변경 파일 lint(오류·경고 0), `git diff --check`, production build를 통과했다.
- 2026-07-27 P3-60: 같은 사용자의 Supabase 토큰 갱신이 새 `user` 객체를 만들 때 돌봄 교대 인증 컨텍스트까지 바뀐 것으로 오인해 진행 중 mutation의 `finally` 후속 처리가 폐기되고 제출·전환 진행 상태가 잠길 수 있던 경로를 차단했다. 돌봄 교대 목록·인증 컨텍스트 effect 의존성을 객체 정체성이 아닌 안정적인 `user.id`로 바꾸고 회귀 테스트를 추가했다. 로그아웃·계정 전환·P3 flag 해제 시 기존 무효화 동작은 유지하며 기존 기능·URL·데이터·flag 분기는 변경하지 않았다. P0 기준 SQL은 재실행하지 않았고 신규 SQL 작성·운영 SQL 실행도 없다(P3 멱등 마이그레이션은 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 재확인 후 실행 필요, P3 완료 판정 유보). Claude Code 직접 검토는 184초 타임아웃됐으나 종료 전 남긴 변경을 로컬에서 범위와 안전성을 재검토했다. `npx tsc --noEmit`, 돌봄 교대·SQL·feature flag 관련 테스트 62건, 변경 파일 lint(오류·경고 0), `git diff --check`, production build를 통과했다.
- 2026-07-27 P3-60: 돌봄 교대 인증 컨텍스트 무효화가 `user` 객체 정체성에 걸려 있어, 같은 사용자의 토큰 갱신(`TOKEN_REFRESHED`, 약 1시간 주기)에도 `onAuthStateChange`가 새 user 객체를 만들며 effect cleanup이 `careShiftAuthContextId`를 증가시키던 결함을 수정했다. 이 경우 진행 중이던 생성·상태 전환 요청의 finally 가드가 어긋나 `shiftSubmitting`/`careShiftTransitioning`이 해제되지 않아 버튼이 영구히 "요청 중…"/"처리 중…"으로 잠기고 성공 알림·폼 초기화도 폐기됐다. 인증 컨텍스트 기준을 객체가 아닌 `user.id`(`userId`)로 바꿔 로그아웃·계정 전환·flag 해제 시에만 무효화하도록 했고, id 기준 배선 회귀 테스트를 추가하고 기존 가드 테스트 정규식을 갱신했다. 기존 기능·URL·데이터·flag 분기는 변경하지 않았다. P0 기준 SQL은 재실행하지 않았고 신규 SQL 작성·운영 SQL 실행도 없다(P3 멱등 마이그레이션은 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 재확인 후 실행 필요, P3 완료 판정 유보). 검증·커밋: 이 회차 CLI에서 읽기 외 실행(`npx`/`node`)이 권한 승인으로 차단되어 타입 체크·테스트·lint·build를 수행하지 못했다. 변경은 워킹 트리에 미커밋 상태이며, 다음 회차(또는 승인 후) `npx tsc --noEmit` → `node --test tests/care-shift.test.mjs tests/care-shift-sql.test.mjs` → 변경 파일 lint → `npm run build` → 커밋 순으로 마무리 필요.
- 2026-07-27 유지보수-1(stray 정리 + 기준선 확인): 이번 회차 시작 시 워킹 트리에 이전 회차가 남긴 미커밋 stray 변경(`app/(main)/map/page.tsx`의 mojibake 주석 + `lib/kakao-types.ts`의 `getBounds`/`setBounds`/`contain` 타입 추가)이 다시 나타나 있었다. 조사 결과: 커밋 HEAD(`391cff7`)의 `map/page.tsx`는 이미 `setBounds`/`getBounds`/`.contain()`을 7곳에서 사용하지만 HEAD의 `lib/kakao-types.ts`에는 해당 선언이 없다 — HEAD에서 `mapInstanceRef`가 `any`라 tsc가 그냥 통과하는 구조적 gap이다(회의 문서 `다음작업_프롬프트_20260727.md` TASK 2가 지적한 no-explicit-any 문제와 동일 지점). 즉 stray는 map ref를 `KakaoMap`으로 타입하려던 TASK 2 부분 시도이나 map 파일이 mojibake로 손상돼 품질이 불합격이다. 손상된 작업을 채택하지 않고 stray 두 파일을 `git stash push -u`(stash: `stray-taskwork-20260727`)로 안전하게 격리했다. 격리 후 클린 HEAD 기준선을 재검증: `npx tsc --noEmit` EXIT=0, `npm test` 117 pass / 0 fail. 회의 문서 결론에 따라 P3 코드 63번째 다듬기는 금지이며, TASK 1(package.json test 스크립트)은 이미 `1589241`로 완료됨을 확인했다. 다음 회차에 TASK 2를 손상되지 않은 얇은 `types/kakao-maps.d.ts`(또는 `lib/kakao-types.ts` 정상 확장)로 깨끗이 다시 수행한다. 앱 코드 커밋·SQL 작성·실행·배포 없음(기준 SQL 재실행도 없음). 이 커밋은 box 문서 3종(SQL 실행 체크리스트, 다음작업 프롬프트, stray map 백업)만 포함한다.
- 2026-07-27 P3-62: 돌봄 교대 시작 시각 RFC 3339 검증이 대문자 `T`/`Z` 구분자만 허용해, 표준상 유효한 소문자 `t`/`z` 오프셋을 우회 요청이 보내면 `invalid_starts_at`으로 거절하던 경로를 보완했다. UI의 `toISOString()`은 대문자만 보내므로 기존 정상 흐름은 그대로이며, RFC 3339 규격에 맞는 소문자 형식만 추가로 허용하고 다른 구분자(`x`)와 잘못된 시각(`t24:00:00z`)은 여전히 거절한다. 소문자 허용·비허용 구분자 회귀 테스트 1건을 추가했다. 기존 기능·URL·데이터·flag 분기는 변경하지 않았다. P0 기준 SQL은 재실행하지 않았고 신규 SQL 작성·운영 SQL 실행도 없다(P3 멱등 마이그레이션은 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 재확인 후 실행 필요, P3 완료 판정 유보). **주의(작업 중 발견):** 이 회차 시작 시 워킹 트리에 내가 만들지 않은 미커밋 stray 변경이 나타났다 — `app/(main)/map/page.tsx`(깨진 mojibake 주석 포함, `setBounds` 타입 오류로 production build 실패 유발)와 `lib/kakao-types.ts`(`getBounds`/`setBounds` 타입 추가, 깨끗함). 배경 Claude Code 제안으로 추정된다. build를 깨뜨리는 map 파일은 `box/stray_map_page_20260727.tsx.bak`로 백업 후 커밋 HEAD로 되돌렸고, kakao-types 변경은 P3 범위 밖이라 내 커밋에 포함하지 않고 워킹 트리에 미커밋 상태로 남겨 사장님 검토 대기다. 커밋은 P3-62 두 파일(`lib/care-shift.ts`, `tests/care-shift.test.mjs`)만 포함한다. `npx tsc --noEmit`, 돌봄 교대·SQL 관련 테스트 53건, 변경 파일 lint(오류·경고 0), production build(map 파일 되돌린 뒤 통과)를 통과했다. 커밋 SHA `391cff7`.
- 2026-07-27 P3-61: 돌봄 교대 API 오류 코드가 `toString`·`constructor` 같은 객체 프로토타입 키일 때 문자열 대신 상속 함수가 안내문으로 노출될 수 있던 경로를 차단했다. 알려진 자체 오류 코드만 own property로 매핑하고 그 외 값은 기존 fallback 안내를 사용하며, 프로토타입 키 3종 회귀 테스트를 추가했다. 기존 기능·URL·데이터·flag 분기는 변경하지 않았다. P0 기준 SQL은 재실행하지 않았고 신규 SQL 작성·운영 SQL 실행도 없다(P3 멱등 마이그레이션은 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 재확인 후 실행 필요, P3 완료 판정 유보). Claude Code가 관련 파일을 읽기 전용 검토해 결함과 최소 수정을 제안했고 로컬에서 적용·재검토했다. `npx tsc --noEmit`, 돌봄 교대·SQL·feature flag 관련 테스트 63건, 변경 파일 lint(오류·경고 0), `git diff --check`, production build를 통과했다.
- 2026-07-27 유지보수-2(stray 재격리 + TASK 2 이미 완료 확인 + P3 잔여 게이트 확정): 이번 회차 시작 시 워킹 트리에 또다시 미커밋 stray 변경(`app/(main)/map/page.tsx` mojibake 주석 + `lib/kakao-types.ts` 타입 추가)이 나타나 있었다. `git diff`로 map 파일이 여전히 깨진 mojibake(`吏\x80???` 등, `setBounds` 타입으로 build 실패 유발)임을 확인하고 두 파일을 `git checkout --`로 되돌려 클린 HEAD로 복구했다(원본은 기존 stash `stray-taskwork-20260727` 및 `box/stray_map_page_20260727.tsx.bak`에 이미 보존됨, 새 손실 없음). **중요 발견:** 커밋 HEAD의 `lib/kakao-types.ts`에는 이미 `KakaoMap.getBounds/setBounds`, `KakaoCircle`, `KakaoOverlay` 전체 타입이 있고, `app/(main)/map/page.tsx`도 이미 `mapInstanceRef=useRef<KakaoMap|null>`, `overlaysRef=useRef<RoamOverlay[]>`, `regionCirclesRef=useRef<(KakaoCircle|KakaoOverlay)[]>`로 배선되어 `any`가 없다(파일 내 `useRef<any`/`: any` 매치 0건). 즉 TASK 2(Kakao SDK no-explicit-any 15건 제거)는 커밋 `0a8b701 refactor(map): Kakao SDK any 15건 제거 - kakao-types 확장 + RoamOverlay`로 **이미 깨끗하게 완료**되어 있었다. 유지보수-1이 예고한 "다음 회차 TASK 2 재수행"은 이미 반영된 상태라 재작업 불필요하며, 반복해서 되살아나는 stray는 그 커밋을 mojibake로 되돌리는 **재손상**일 뿐이므로 채택 금지. TASK 1(package.json test)도 `1589241`로 완료 확인됨. 회의 문서에 따라 P3 코드 63번째 다듬기는 금지. 클린 HEAD 재검증: `npx tsc --noEmit` EXIT=0, `npm test` 117 pass / 0 fail. **P3 완료의 유일한 잔여 게이트는 멱등 마이그레이션 `box/supabase_care_shifts_migration.sql`을 로그인된 Supabase SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 확인 후 실행하는 사장님측 조치**다(로컬에 supabase CLI/psql/Management 토큰/DB 비밀번호 부재, service_role은 DDL 불가 — 자가 실행 불가). 앱 코드 변경·신규 SQL 작성·운영 SQL 실행·배포 없음(기준 SQL 재실행도 없음). 이 커밋은 본 개발일지 1개 파일만 포함한다.
- 2026-07-27 유지보수-3(stray smoke 스크립트 격리): 이번 회차 시작 시 워킹 트리에 내가 만들지 않은 미커밋 stray 파일 `box/smoke_care_shifts.mjs`가 나타나 있었다. 파일 전체가 깨진 mojibake 주석(`?\x90...`, `荑??`, `?ㅽ\x98몄` 등)으로 손상된 P3 수동 스모크 스크립트였다 — 앞서 반복 재손상되던 stray map 파일과 동일 패턴이다. 개발일지에 확정된 정책(손상된 stray 작업 채택 금지·안전 격리)에 따라 커밋에 포함하지 않고 `box/stray_smoke_care_shifts_20260727.mjs.bak`로 이름을 바꿔 격리했다(새 손실 없음). 격리 후 클린 HEAD 재검증: `npx tsc --noEmit` EXIT=0, `node --test tests/care-shift.test.mjs tests/care-shift-sql.test.mjs tests/feature-flags.test.mjs` 53 pass / 0 fail. 앱 코드 변경·신규 SQL 작성·운영 SQL 실행·배포 없음(기준 SQL 재실행도 없음). **P3 완료의 유일한 잔여 게이트는 여전히 멱등 마이그레이션 `box/supabase_care_shifts_migration.sql`을 로그인된 Supabase SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 확인 후 실행하는 사장님측 조치**다(로컬에 supabase CLI/psql/Management 토큰/DB 비밀번호 부재, service_role은 DDL 불가 — 자가 실행 불가). 이 커밋은 격리된 stray 백업 파일과 본 개발일지 1개 문서만 포함한다.
- 2026-07-27 P1-1: 홈 오늘의 돌봄 인박스에 미완료(밥) 수 요약을 노출했다. 순수 함수 `countPendingFeed`를 `lib/care-inbox.ts`에 추가하고(빈 배열=0, feed 미포함만 카운트) 매칭 테스트 2건을 붙였다. `MyCatsHero`는 `careInboxMode`이고 미완료가 1 이상일 때만 칩을 `아직 N마리`로 바꾸며, 그 외에는 기존 `done/total 오늘 밥` 표기를 그대로 유지한다. 기존 기능·URL·데이터·SQL 변경 없음. P0 SQL(ASCII 정규화)은 이미 c7b5ad6에 P1 flag 배선과 함께 커밋되어 있어 이번 회차에 별도 폴드할 잔여 변경 없음을 확인했다. stray(app/terms·lib/news-crawler)는 재격리(stash)해 커밋에서 제외했다. `npx tsc --noEmit` EXIT=0, `node --test tests/care-inbox.test.mjs` 4 pass/0 fail, 변경 3파일 eslint 0, `git diff --check` 청결.

- 2026-07-27 P4-1: P4 돌봄팀 통합의 첫 단위로 서클·동네 채팅·고양이 커뮤니티를 하나의 "돌봄팀" 화면으로 묶기 위한 순수 섹션 계약 `lib/care-team.ts`를 추가했다. `돌봄 서클 → 동네 채팅 → 고양이 커뮤니티` 고정 순서와 각 섹션의 기존 URL(`/circle`·`/map`·`/community`)을 그대로 가리키는 계약을 정의하고, `careTeamSections`(방어적 복사)·`findCareTeamSection`(own property·비문자열·프로토타입 키 fail-safe)을 노출했다. 새 라우트·스키마·데이터 변경은 없고 아직 UI·flag 배선은 하지 않았다(P4 flag `NEXT_PUBLIC_FF_CJ_CARE_TEAM`는 기 등록됨). 순서·URL 보존·복사 불변·조회 fail-safe·키 유일성 회귀 테스트 6건을 추가했다. 기존 기능·URL·데이터 삭제나 변경은 없고 SQL 작성·실행 없음(P3 멱등 마이그레이션은 여전히 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 확인 후 실행하는 사장님측 조치가 P3 완료의 유일한 게이트). 함께 P0 read-only 진단 SQL(`box/diagnostic_core_journey_baseline.sql`)의 ASCII 인코딩 정리를 기록한다 — 메인 세션이 2026-07-26 20:55 KST에 로그인된 SQL Editor(CITY ref 화면 확인)에서 전문을 실행해 마지막 주간 결과 8 rows로 성공 확인했고, 현재 파일은 순수 ASCII(non-ASCII 0바이트)로 이미 `c7b5ad6`에 커밋되어 별도 폴드 잔여는 없음을 재확인했다. `npx tsc --noEmit` EXIT=0, `node --test tests/care-team.test.mjs` 6 pass/0 fail, 변경 2파일 eslint 0, `git diff --check` 청결, `npm run build` EXIT=0을 통과했다.

- 2026-07-27 P0-record + P1-test: P0 read-only 진단 SQL(box/diagnostic_core_journey_baseline.sql)의 ASCII 인코딩 정리를 기록한다. 메인 세션이 2026-07-26 20:55 KST에 로그인된 Supabase SQL Editor(CITY ref sozxbnvgsougkliibnxl 화면 확인)에서 전체를 직접 실행해 마지막 주간 결과 8 rows로 성공을 확인했다. 현재 파일은 non-ASCII 0바이트(총 4659바이트)로 mojibake 없이 순수 ASCII이며 c7b5ad6에 이미 커밋됨을 재검증했다. 함께 P1 오늘의 돌봄 인박스 미완료 수 배지 로직의 엣지 케이스(모두 밥 못 챙긴 경우 countPendingFeed가 전체 마리 수와 같음)를 tests/care-inbox.test.mjs에 단위 테스트로 추가했다. 기존 기능·URL·데이터·SQL·flag 배선 변경 없음. 새 SQL 없음. npx tsc --noEmit EXIT=0, node --test care-inbox 5 pass/0 fail로 검증했다.

- 2026-07-27 P4-3: P4 돌봄팀 통합의 순수 계약에 href로 현재 섹션을 역조회하는 `careTeamSectionByHref`를 추가했다. 링크 대상 페이지가 자신이 돌봄팀 하위 섹션(서클/동네 채팅/커뮤니티) 중 어디인지 판별해 강조 표시 등에 쓸 수 있게 하며, 쿼리스트링·해시는 경로 부분만 비교하고(`/map?region=…`·`/circle#invite` 등) 알 수 없는 경로·부분 일치(`/mapx`)·비문자열은 undefined로 fail-safe 처리한다. 새 URL을 만들지 않고 계약의 기존 href만 참조한다(불변식 유지). 정확 매칭·쿼리/해시 무시·fail-safe 회귀 테스트 3건을 추가(총 9건)했다. UI·라우트·스키마·데이터 변경은 없고 SQL 작성·실행 없음. P0 read-only 진단 SQL(`box/diagnostic_core_journey_baseline.sql`)의 ASCII 인코딩 정리 기록은 유지된다 — 메인 세션이 2026-07-26 20:55 KST 로그인된 SQL Editor(CITY ref `sozxbnvgsougkliibnxl` 화면 확인)에서 전문 실행, 마지막 주간 결과 8 rows로 성공 확인했고 현재 파일은 순수 ASCII로 이미 `c7b5ad6`에 커밋되어 별도 폴드 잔여 없음. P3 완료의 유일한 잔여 게이트는 여전히 멱등 마이그레이션 `box/supabase_care_shifts_migration.sql`을 로그인된 SQL Editor에서 CITY ref 확인 후 실행하는 사장님측 조치다. `npx tsc --noEmit` EXIT=0, `node --test tests/care-team.test.mjs` 9 pass/0 fail, 변경 2파일 eslint 0, `npm run build` EXIT=0을 통과했다.

- 2026-07-27 P4-2: P4-1에서 만든 순수 섹션 계약 `lib/care-team.ts`를 서클 화면(`app/(main)/mypage/circle/page.tsx`)에 최소 진입 카드로 배선했다. `NEXT_PUBLIC_FF_CJ_CARE_TEAM`(P4)이 켜지고 kill switch가 꺼진 경우에만 "돌봄팀" 카드를 P3 돌봄 교대 섹션 위에 표시하며, `돌봄 서클(/circle) → 동네 채팅(/map) → 고양이 커뮤니티(/community)` 3개 섹션을 기존 URL 그대로 가리키는 링크 목록으로 노출한다. flag off 또는 kill switch on이면 기존 서클 화면이 그대로 유지된다. 새 라우트·스키마·데이터 변경은 없고 기존 기능·URL 삭제도 없다 — P4의 "묶어 보여주기" 원칙에 따라 순수 계약을 재사용해 링크만 모았다. 함께 P0 read-only 진단 SQL(`box/diagnostic_core_journey_baseline.sql`)의 ASCII 인코딩 정리를 재기록한다: 메인 세션이 2026-07-26 20:55 KST에 로그인된 Supabase SQL Editor(CITY ref `sozxbnvgsougkliibnxl` 화면 확인)에서 전문을 실행해 마지막 주간 결과 8 rows로 성공을 확인했고, 현재 파일은 순수 ASCII(non-ASCII 0바이트)로 이미 `c7b5ad6`에 커밋되어 별도 폴드 잔여는 없다. SQL 작성·실행 없음(P3 멱등 마이그레이션은 여전히 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 확인 후 실행하는 사장님측 조치가 P3 완료의 유일한 게이트). `npx tsc --noEmit` EXIT=0, `node --test tests/care-team.test.mjs` 6 pass/0 fail, 변경 파일 lint(오류·경고 0), `npm run build` EXIT=0을 통과했다.

- 2026-07-27 P4-5: P4-2/P4-4에서 서클 화면에 인라인으로 있던 약 65줄의 "돌봄팀" 진입 카드 JSX를 공용 컴포넌트 `app/components/CareTeamCard.tsx`로 추출했다. 여러 섹션(서클/지도/커뮤니티)에서 같은 카드를 중복 없이 재사용하기 위한 순수 추출이며 동작 변경은 없다. 컴포넌트는 순수 계약 `lib/care-team.ts`의 순서·URL만 사용하고(`careTeamSections`/`careTeamSectionByHref`), `usePathname`으로 현재 경로가 하위 섹션이면 링크 대신 '현재 위치' 배지 + `aria-current=page`를 표시한다. flag 게이팅은 호출부 책임으로 유지해 서클 화면은 `{showCareTeam && <CareTeamCard />}`로 배선했다. 인라인 삭제로 더 이상 쓰지 않게 된 서클 화면의 `usePathname`·`pathname`·`lib/care-team` import를 함께 정리해 lint 경고 0으로 맞췄다(`Users`/`ChevronRight`/`Link`는 다른 곳에서 계속 사용). flag off 또는 kill switch on이면 카드가 렌더되지 않아 기존 서클 화면이 그대로 유지되며 새 라우트·스키마·데이터·URL 변경은 없다. 함께 P0 read-only 진단 SQL(box/diagnostic_core_journey_baseline.sql)의 ASCII 인코딩 정리 기록을 유지한다: 메인 세션이 2026-07-26 20:55 KST 로그인된 Supabase SQL Editor(CITY ref sozxbnvgsougkliibnxl 화면 확인)에서 전문 실행, 마지막 주간 결과 8 rows로 성공 확인했고 현재 파일은 순수 ASCII로 이미 c7b5ad6에 커밋되어 별도 폴드 잔여 없음. SQL 작성·실행 없음(P3 멱등 마이그레이션은 여전히 로그인된 SQL Editor에서 CITY ref sozxbnvgsougkliibnxl 확인 후 실행하는 사장님측 조치가 P3 완료의 유일한 게이트). npx tsc --noEmit EXIT=0, node --test tests/care-team.test.mjs 9 pass/0 fail, 변경 파일 lint(오류·경고 0), git diff --check 청결, npm run build EXIT=0을 통과했다.
- 2026-07-27 P4-6: P4-5에서 공용화한 `CareTeamCard`를 커뮤니티 화면(`app/(main)/community/page.tsx`)에도 배선해 돌봄팀 3개 섹션(서클/동네 채팅/커뮤니티) 진입점을 서클·커뮤니티 양쪽에서 일관되게 노출했다. `NEXT_PUBLIC_FF_CJ_CARE_TEAM`(P4)이 켜지고 kill switch가 꺼진 경우에만 글쓰기 프롬프트 아래에 카드를 표시하며, 카드 내부 `usePathname`이 `/community`를 인식해 '고양이 커뮤니티' 항목을 링크 대신 '현재 위치' 배지 + `aria-current=page`로 표시한다. flag off 또는 kill switch on이면 기존 커뮤니티 화면이 그대로 유지되고 새 라우트·스키마·데이터·URL 변경은 없다(순수 계약 `lib/care-team.ts`의 기존 href만 재사용). 지도(`/map`)는 전체화면 절대배치 레이아웃이라 이번 회차 안전 단일 삽입 지점이 아니어서 커뮤니티부터 배선했다. 함께 P0 read-only 진단 SQL(`box/diagnostic_core_journey_baseline.sql`)의 ASCII 인코딩 정리를 재기록한다: 메인 세션이 2026-07-26 20:55 KST에 로그인된 Supabase SQL Editor(CITY ref `sozxbnvgsougkliibnxl` 화면 확인)에서 전문을 실행해 마지막 주간 결과 8 rows로 성공을 확인했고, 현재 파일은 순수 ASCII(non-ASCII 0바이트)로 이미 `c7b5ad6`에 커밋되어 별도 폴드 잔여는 없다. SQL 작성·실행 없음(P3 멱등 마이그레이션은 여전히 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 확인 후 실행하는 사장님측 조치가 P3 완료의 유일한 게이트). 커뮤니티 페이지 기존 lint 경고 2건(`totalUsers` 미사용·effect setState)은 HEAD에서도 동일하게 존재함을 stash 대조로 확인했고 이번 변경은 새 오류·경고를 추가하지 않았다. `npx tsc --noEmit` EXIT=0, `node --test tests/care-team.test.mjs` 9 pass/0 fail, 변경 파일 lint(신규 오류·경고 0), `npm run build` EXIT=0을 통과했다.
- 2026-07-27 P4-4: P4-3에서 추가한 순수 함수 careTeamSectionByHref를 서클 화면 돌봄팀 카드에 배선해, 현재 보고 있는 페이지(usePathname)가 돌봄팀 하위 섹션이면 그 항목을 링크 대신 '현재 위치' 배지와 aria-current=page로 표시하도록 했다. 서클 화면(/circle)에서는 '돌봄 서클' 항목이 자기 자신을 다시 가리키던 중복 링크가 사라지고 현재 위치임을 알려준다. 나머지 섹션(동네 채팅/커뮤니티)은 기존 링크·href·순서를 그대로 유지하며, P4 flag가 꺼지거나 kill switch가 켜지면 돌봄팀 카드 자체가 표시되지 않아 기존 서클 화면이 그대로 남는다. 새 라우트·스키마·데이터 변경은 없고 기존 URL 삭제도 없다(careTeamSectionByHref는 계약의 href만 참조·불변식 유지). 함께 P0 read-only 진단 SQL(box/diagnostic_core_journey_baseline.sql)의 ASCII 인코딩 정리를 재기록한다: 메인 세션이 2026-07-26 20:55 KST에 로그인된 Supabase SQL Editor(CITY ref sozxbnvgsougkliibnxl 화면 확인)에서 전문을 실행해 마지막 주간 결과 8 rows로 성공을 확인했고, 현재 파일은 순수 ASCII(non-ASCII 0바이트)로 이미 c7b5ad6에 커밋되어 별도 폴드 잔여는 없다. SQL 작성·실행 없음(P3 멱등 마이그레이션은 여전히 로그인된 SQL Editor에서 CITY ref sozxbnvgsougkliibnxl 확인 후 실행하는 사장님측 조치가 P3 완료의 유일한 게이트). npx tsc --noEmit EXIT=0, node --test tests/care-team.test.mjs 9 pass/0 fail, 변경 파일 lint(오류·경고 0), git diff --check 청결, npm run build EXIT=0을 통과했다.
- 2026-07-27 P4-7: P4-5에서 공용화한 CareTeamCard를 홈 화면(app/components/HomeAuthed.tsx)의 서클 빠른 진입(MyCircleQuickEntry) 바로 아래에 P4 flag 게이팅으로 배선했다. 홈은 P1 오늘의 돌봄 인박스 앵커라, 같은 "혼자→같이" 리텐션 영역에서 서클·동네 채팅·고양이 커뮤니티 세 섹션으로 곧장 이어지도록 진입점을 추가한 것이다. isCoreJourneyEnabled("P4")가 켜지고 kill switch가 꺼진 경우에만 {isCoreJourneyEnabled("P4") && user && <CareTeamCard />}로 렌더하며, flag off 또는 kill switch on이면 카드가 표시되지 않아 기존 홈이 그대로 유지된다. 새 라우트·스키마·데이터·URL 변경은 없고 순수 계약 lib/care-team.ts의 기존 href만 재사용한다(홈에서는 currentKey가 어느 섹션에도 매칭되지 않아 세 항목 모두 링크로 노출). 함께 P0 read-only 진단 SQL(box/diagnostic_core_journey_baseline.sql)의 ASCII 인코딩 정리를 재기록한다: 메인 세션이 2026-07-26 20:55 KST에 로그인된 Supabase SQL Editor(CITY ref sozxbnvgsougkliibnxl 화면 확인)에서 전문을 실행해 마지막 주간 결과 8 rows로 성공을 확인했고, 현재 파일은 순수 ASCII(non-ASCII 0바이트)로 이미 c7b5ad6에 커밋되어 별도 폴드 잔여는 없다. SQL 작성·실행 없음(P3 멱등 마이그레이션은 여전히 로그인된 SQL Editor에서 CITY ref sozxbnvgsougkliibnxl 확인 후 실행하는 사장님측 조치가 P3 완료의 유일한 게이트). HomeAuthed.tsx 기존 lint 경고 4건(Image·rescueCount·quietCat·neighborhoodCatCount 미사용)은 HEAD에서도 동일하며 이번 변경은 새 오류·경고를 추가하지 않았다(신규 0). npx tsc --noEmit EXIT=0, node --test tests/care-team.test.mjs 9 pass/0 fail, 변경 파일 lint(신규 오류·경고 0), npm run build EXIT=0을 통과했다.

- 2026-07-27 P4-9: 돌봄팀 진입 카드의 현재 위치 강조가 쓰는 순수 함수 `careTeamSectionByHref`가 끝 슬래시가 붙은 경로(`/circle/`, `/map/`)를 하위 섹션으로 인식하지 못해 현재 위치인데도 링크로 표시되던 경계 결함을 보완했다. 경로 부분에서 끝 슬래시(연속 포함)를 정규화해 계약 경로와 같게 취급하고, 루트 `/`와 부분 경로(`/mapx/`)는 여전히 매칭하지 않는다(불변식 유지). 쿼리/해시 무시 동작은 그대로다. 끝 슬래시 정규화·연속 슬래시·루트/부분 경로 비매칭 회귀 테스트 1건을 추가(총 10건)했다. UI·라우트·스키마·데이터·URL 변경은 없고 계약의 기존 href만 참조한다. 함께 P0 read-only 진단 SQL(box/diagnostic_core_journey_baseline.sql)의 ASCII 인코딩 정리를 재기록한다: 메인 세션이 2026-07-26 20:55 KST에 로그인된 Supabase SQL Editor(CITY ref sozxbnvgsougkliibnxl 화면 확인)에서 전문을 실행해 마지막 주간 결과 8 rows로 성공을 확인했고, 현재 파일은 순수 ASCII(총 4659바이트·non-ASCII 0바이트)로 이미 c7b5ad6에 커밋되어 별도 폴드 잔여는 없다. SQL 작성·실행 없음(P3 멱등 마이그레이션은 여전히 로그인된 SQL Editor에서 CITY ref sozxbnvgsougkliibnxl 확인 후 실행하는 사장님측 조치가 P3 완료의 유일한 게이트). 이번 회차 워킹 트리에 stray 파일은 나타나지 않았다. npx tsc --noEmit EXIT=0, node --test tests/care-team.test.mjs 10 pass/0 fail, 변경 2파일 eslint 0, git status 청결(내 2파일만), npm run build EXIT=0을 통과했다.
- 2026-07-27 TASK3.4-1(lint 신호 정리): 회의 문서(`다음작업_프롬프트_20260727.md`) TASK 3.4에 따라 `MyCircleQuickEntry.tsx:22`의 `react-hooks/set-state-in-effect` 경고를 사유 주석된 eslint-disable로 명시 처리했다. 해당 effect는 `if (!user)`일 때 `setLoading(false)`를 1회 정착시키고 바로 return하며, 그 직후 `if (!user) return null`이 있어 렌더마다 setState하는 무한 루프가 아닌 mount/user-change 1회 세팅 거짓양성임을 확인했다. 동작 변경 없는 주석만 추가한 순수 정리로 기존 기능·URL·데이터·flag 분기는 변경하지 않았다. 회의 문서가 지칭한 TASK 1(package.json test)·TASK 2(Kakao 앵비언트 타입)·TASK 3.2(prefer-const)·TASK 3.3(terms 내부 링크→next/link)는 기존 커밋으로 이미 완료되어 있음을 이번 회차에 재확인했다(news-crawler.ts:156은 이미 const, terms/page.tsx:175는 이미 `<Link href="/shop/policy">`, 콘솔 mojibake는 파일 손상이 아닌 PowerShell 출력 코드페이지 아티팩트로 파일은 정상 UTF-8 한글). stray stash `stray-terms-news-20260727`는 확정 정책에 따라 채택하지 않고 그대로 격리 유지했다. 함께 P0 read-only 진단 SQL(box/diagnostic_core_journey_baseline.sql)의 ASCII 인코딩 정리를 재기록한다: 메인 세션이 2026-07-26 20:55 KST 로그인된 Supabase SQL Editor(CITY ref sozxbnvgsougkliibnxl 화면 확인)에서 전문을 실행해 마지막 주간 결과 8 rows로 성공을 확인했고, 현재 파일은 순수 ASCII(총 4659바이트·non-ASCII 0바이트)로 이미 c7b5ad6에 커밋되어 별도 폴드 잔여는 없다. 신규 SQL 작성·운영 SQL 실행 없음(P3 멱등 마이그레이션은 여전히 로그인된 SQL Editor에서 CITY ref sozxbnvgsougkliibnxl 확인 후 실행하는 사장님측 조치가 P3 완료의 유일한 게이트). `npx tsc --noEmit` EXIT=0, `node --test tests/care-team.test.mjs tests/care-inbox.test.mjs` 15 pass/0 fail, 변경 파일 lint(오류·경고 0), `npm run build` EXIT=0을 통과했다.

- 2026-07-27 P4-8: P4-5에서 공용화한 CareTeamCard를 지도 화면(app/(main)/map/page.tsx)의 P2 상세 도구 2차 영역(학대 경보 카드 아래, 지도 캔버스 시작 전 헤더 패널 내부)에 배선했다. 그동안 돌봄팀 3섹션 중 지도(/map=동네 채팅)만 진입 카드가 없었는데, 지도 화면은 전체화면 절대배치라 P2 상세 도구를 연 경우에만 렌더되는 일반 헤더 패널이 유일한 안전 단일 삽입 지점이라 그 위치를 골랐다. isCoreJourneyEnabled("P4")가 켜지고 kill switch가 꺼진 상태에서 detailToolsVisible(상세 도구 열림)일 때만 {isCoreJourneyEnabled("P4") && detailToolsVisible && <CareTeamCard />}로 노출하며, 카드 내부 usePathname이 /map을 인식해 '동네 채팅' 항목을 링크 대신 '현재 위치' 배지 + aria-current=page로 표시한다. flag off·kill switch on·상세 도구 닫힘이면 기존 지도가 그대로 유지되고 새 라우트·스키마·데이터·URL 변경은 없다(순수 계약 lib/care-team.ts의 기존 href만 재사용). 이로써 홈·서클·커뮤니티·지도 네 진입면에 돌봄팀 카드가 일관되게 배선되어 P4 "묶어 보여주기" 골격이 갖춰졌다. 함께 P0 read-only 진단 SQL(box/diagnostic_core_journey_baseline.sql)의 ASCII 인코딩 정리를 재기록한다: 메인 세션이 2026-07-26 20:55 KST에 로그인된 Supabase SQL Editor(CITY ref sozxbnvgsougkliibnxl 화면 확인)에서 전문을 실행해 마지막 주간 결과 8 rows로 성공을 확인했고, 현재 파일은 순수 ASCII(총 4659바이트·non-ASCII 0바이트)로 이미 c7b5ad6에 커밋되어 별도 폴드 잔여는 없다. SQL 작성·실행 없음(P3 멱등 마이그레이션은 여전히 로그인된 SQL Editor에서 CITY ref sozxbnvgsougkliibnxl 확인 후 실행하는 사장님측 조치가 P3 완료의 유일한 게이트). map/page.tsx 기존 lint 경고 9건(미사용 import·<img>·exhaustive-deps)은 HEAD에서도 동일하며 이번 변경은 새 오류·경고를 추가하지 않았다(신규 0). npx tsc --noEmit EXIT=0, node --test tests/care-team.test.mjs 9 pass/0 fail, 변경 파일 lint(신규 오류·경고 0), npm run build EXIT=0을 통과했다.

- 2026-07-27 P4-10: CareTeamCard�� ����Ǵ� �� ���Ը�(Ȩ `HomeAuthed.tsx`, ��Ŭ `mypage/circle`, Ŀ�´�Ƽ `community`, ���� `map`)�� ��� P4 flag�� fail-closed �����õ��� �ҽ� ���Խ� ���� �׽�Ʈ�� �����ߴ�. Ȩ�������� `isCoreJourneyEnabled("P4")` ���� ������, ��Ŭ��Ŀ�´�Ƽ�� `const showCareTeam = isCoreJourneyEnabled("P4")` �Ļ� ����Ʈ�� �˻��Ѵ�. ���� ������ flag ���带 �Ǽ��� ����(ī�尡 kill switch�� �����ϰ� ����Ǵ� ���ټǡ����ռ� ����) �׽�Ʈ�� ��� �����Ѵ�. �� �ڵ塤���Ʈ����Ű���������͡�URL ������ ���� �׽�Ʈ 1�Ǹ� �߰��ߴ�(�� 11��). �Բ� P0 read-only ���� SQL(`box/diagnostic_core_journey_baseline.sql`)�� ASCII ���ڵ� ������ �����Ѵ�: ���� ������ 2026-07-26 20:55 KST�� �α��ε� Supabase SQL Editor(CITY ref `sozxbnvgsougkliibnxl` ȭ�� Ȯ��)���� ������ ������ ������ �ְ� ��� 8 rows�� ������ Ȯ���߰�, ���� ������ ���� ASCII(�� 4659����Ʈ��non-ASCII 0����Ʈ)�� �̹� `c7b5ad6`�� Ŀ�ԵǾ� ���� ���� �ܿ��� ����. �ű� SQL �ۼ���� SQL ���� ����(P3 �Ϸ��� ������ �ܿ� ����Ʈ�� ������ ��� ���̱׷��̼� `box/supabase_care_shifts_migration.sql`�� �α��ε� SQL Editor���� CITY ref `sozxbnvgsougkliibnxl` Ȯ�� �� �����ϴ� ������� ��ġ). �̹� ȸ�� ��ŷ Ʈ���� stray ������ ��Ÿ���� �ʾҴ�. `npx tsc --noEmit` EXIT=0, `node --test tests/care-team.test.mjs` 11 pass/0 fail, ���� ���� lint(��������� 0), `npm run build` EXIT=0�� ����ߴ�.
- 2026-07-27 P4-12: CareTeamCard가 렌더하는 계약 항목이 모두 표시 가능한 값을 갖도록 lib/care-team.ts 계약 무결성 회귀 테스트 2건을 추가했다(care-team 총 14건). (1) 모든 섹션의 label·description이 문자열이면서 trim 후 비어 있지 않음을 검사해, 미래 편집이 빈 라벨/설명을 넣어 카드에 깨진 항목이 생기는 것을 막는다. (2) 모든 섹션의 href가 `/`로 시작하는 절대 in-app 경로이고 프로토콜-상대(`//`)나 URL 스킴(`http:` 등)을 포함하지 않으며, 그 href가 careTeamSectionByHref로 자기 자신을 round-trip 되찾음을 검사해 `기존 URL 유지`(외부/새 라우트 금지) 불변식과 경로 정규화 정합성을 함께 고정한다. 앱 코드·컴포넌트·라우트·스키마·데이터·URL·flag 배선 변경은 없고 테스트만 추가했다. 함께 P0 read-only 진단 SQL(box/diagnostic_core_journey_baseline.sql)의 ASCII 인코딩 정리를 재기록한다: 메인 세션이 2026-07-26 20:55 KST에 로그인된 Supabase SQL Editor(CITY ref sozxbnvgsougkliibnxl 화면 확인)에서 전문을 실행해 마지막 주간 결과 8 rows로 성공을 확인했고, 현재 파일은 순수 ASCII(총 4659바이트·non-ASCII 0바이트)로 이미 c7b5ad6에 커밋되어 별도 폴드 잔여는 없다. 신규 SQL 작성·운영 SQL 실행 없음(P3 완료의 유일한 잔여 게이트는 여전히 멱등 마이그레이션 box/supabase_care_shifts_migration.sql을 로그인된 SQL Editor에서 CITY ref sozxbnvgsougkliibnxl 확인 후 실행하는 사장님측 조치). 이번 회차 워킹 트리에 stray 파일은 나타나지 않았다. `npx tsc --noEmit` EXIT=0, `node --test tests/care-team.test.mjs` 14 pass/0 fail, 변경 파일 lint(오류·경고 0), `git diff --check` 통과, `npm run build` EXIT=0을 통과했다.
- 2026-07-27 P4-11: CareTeamCard 컴포넌트가 `기존 URL 유지` 불변식을 지키는지 소스 감시 테스트로 고정했다. 카드가 (1) Link href를 계약(`section.href`)에서만 설정하고, (2) raw 경로 문자열(`href="/..."`)을 하드코딩하지 않으며, (3) `careTeamSections().map`으로 계약을 순회하는지 정규식으로 검사한다. 향후 편집이 CareTeamCard에 새 라우트를 하드코딩하면(P4의 `새 URL을 만들지 않는다` 불변식을 조용히 깨는 결함) 테스트가 즉시 실패한다. 앱 코드·라우트·스키마·데이터·URL 변경은 없고 테스트 1건만 추가했다(care-team 총 12건). 함께 P0 read-only 진단 SQL(box/diagnostic_core_journey_baseline.sql)의 ASCII 인코딩 정리를 재기록한다: 메인 세션이 2026-07-26 20:55 KST에 로그인된 Supabase SQL Editor(CITY ref sozxbnvgsougkliibnxl 화면 확인)에서 전문을 실행해 마지막 주간 결과 8 rows로 성공을 확인했고, 현재 파일은 순수 ASCII(총 4659바이트·non-ASCII 0바이트)로 이미 c7b5ad6에 커밋되어 별도 폴드 잔여는 없다. 신규 SQL 작성·운영 SQL 실행 없음(P3 완료의 유일한 잔여 게이트는 여전히 멱등 마이그레이션 box/supabase_care_shifts_migration.sql을 로그인된 SQL Editor에서 CITY ref sozxbnvgsougkliibnxl 확인 후 실행하는 사장님측 조치). `npx tsc --noEmit` EXIT=0, `node --test tests/care-team.test.mjs` 12 pass/0 fail, 변경 파일 lint(오류·경고 0), `git diff --check` 정결, `npm run build` EXIT=0을 통과했다.
- 2026-07-27 P4-14: 돌봄팀 진입 카드의 서클 섹션 href가 실제 존재하지 않는 `/circle`을 가리켜 "기존 URL 유지" 불변식을 깨던 결함을 수정했다. 앱의 실제 서클 화면은 `app/(main)/mypage/circle`로 `/mypage/circle` URL이며(`MyCircleQuickEntry` 등 기존 진입점이 모두 `/mypage/circle`로 링크), `app/circle/page.tsx`는 없어 `/circle`은 링크가 깨지고 카드의 '현재 위치' 강조도 서클 화면에서 매칭되지 않았다(앱 전체에 `href="/circle"` 0건·`/circle` 라우트 부재를 grep으로 확인). `lib/care-team.ts` 계약의 `circle.href`를 `/mypage/circle`로 바로잡고(사유 주석 추가), `careTeamSectionByHref`가 이제 서클 화면에서 '현재 위치'를 올바르게 되찾는다. 관련 회귀 테스트(URL 보존·정확 매칭·해시·끝 슬래시)를 새 URL에 맞춰 갱신했다. 맵·커뮤니티 href(`/map`·`/community`)는 앱 전체에서 이미 사용되는 실제 라우트임을 확인해 그대로 둔다. 새 라우트·스키마·데이터 변경은 없고 기존 기능 삭제도 없다(계약의 href만 실제 URL로 교정). 함께 P0 read-only 진단 SQL(`box/diagnostic_core_journey_baseline.sql`)의 ASCII 인코딩 정리를 재기록한다: 메인 세션이 2026-07-26 20:55 KST에 로그인된 Supabase SQL Editor(CITY ref `sozxbnvgsougkliibnxl` 화면 확인)에서 전문을 실행해 마지막 주간 결과 8 rows로 성공을 확인했고, 현재 파일은 순수 ASCII(총 4659바이트·non-ASCII 0바이트)로 이미 `c7b5ad6`에 커밋되어 별도 폴드 잔여는 없다. 신규 SQL 작성·운영 SQL 실행 없음(P3 완료의 유일한 잔여 게이트는 여전히 멱등 마이그레이션 `box/supabase_care_shifts_migration.sql`을 로그인된 SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl` 확인 후 실행하는 사장님측 조치). 이번 회차 워킹 트리에 stray 파일은 나타나지 않았다. `npx tsc --noEmit` EXIT=0, `node --test tests/care-team.test.mjs` 15 pass/0 fail, 전체 `npm test` 135 pass/0 fail, 변경 3파일 lint(오류·경고 0), `npm run build` EXIT=0을 통과했다.

- 2026-07-27 P4-13: P4 불변식(기존 기능 삭제 금지)을 소스 감시 회귀 테스트로 고정했다(care-team 총 15건). CareTeamCard가 배선된 네 진입면(홈 `HomeAuthed.tsx`, 서클 `mypage/circle`, 커뮤니티 `community`, 지도 `map`)이 각각 카드를 렌더하면서도 그 화면의 기존 핵심 콘텐츠 앵커를 그대로 유지하는지 검사한다 — 홈은 P1 오늘의 돌봄 인박스(`careInbox`)와 서클 빠른 진입(`MyCircleQuickEntry`), 서클은 P3 돌봄 교대 흐름(`/api/care-shifts`), 커뮤니티는 글쓰기 프롬프트(`WritePrompt`), 지도는 Kakao 지도 인스턴스(`mapInstanceRef`)와 상세 도구(`detailToolsVisible`). 미래 편집이 CareTeamCard를 손보며 실수로 기존 주요 기능을 대체·삭제하면 즉시 실패한다. 기존 flag 게이팅 감시 테스트가 카드 '추가·게이팅'만 확인하던 사각을 '기존 기능 보존'으로 보완한 것이며, 앱 코드·컴포넌트·라우트·스키마·데이터·URL·flag 배선 변경은 없고 테스트만 추가했다. 함께 P0 read-only 진단 SQL(box/diagnostic_core_journey_baseline.sql)의 ASCII 인코딩 정리를 재기록한다: 메인 세션이 2026-07-26 20:55 KST에 로그인된 Supabase SQL Editor(CITY ref sozxbnvgsougkliibnxl 화면 확인)에서 전문을 실행해 마지막 주간 결과 8 rows로 성공을 확인했고, 현재 파일은 순수 ASCII(총 4659바이트·non-ASCII 0바이트)로 이미 c7b5ad6에 커밋되어 별도 폴드 잔여는 없다. 신규 SQL 작성·운영 SQL 실행 없음(P3 완료의 유일한 잔여 게이트는 여전히 멱등 마이그레이션 box/supabase_care_shifts_migration.sql을 로그인된 SQL Editor에서 CITY ref sozxbnvgsougkliibnxl 확인 후 실행하는 사장님측 조치). 회차 시작 시 워킹 트리에 있던 stray 파일(`-`, git-log 출력이 잘못 리다이렉트된 190바이트 쓰레기)을 제거해 트리를 청결히 했다(추적 파일 아님·손실 없음). 두 stray stash(`stray-terms-news-20260727`, `stray-taskwork-20260727`)는 확정 정책대로 격리 유지. `npx tsc --noEmit` EXIT=0, `node --test tests/care-team.test.mjs` 15 pass/0 fail, 전체 `npm test` 135 pass/0 fail, 변경 파일 lint(오류·경고 0), `git diff --check` 통과, `npm run build` EXIT=0을 통과했다.


- 2026-07-27 P4-15: P4 돌봄팀 순수 계약의 두 조회 함수(`findCareTeamSection`=key축, `careTeamSectionByHref`=href축)가 서로 정합함을 회귀 테스트로 고정했다(care-team 총 16건). 모든 섹션에 대해 key로 찾은 뒤 그 href로 다시 조회하면 같은 섹션 key로 돌아오는지(round-trip) 검사한다. 미래 편집이 한쪽 축만 바꿔(예: 새 섹션 href가 경로 정규화 후 자기 자신을 못 되찾음) 카드의 '현재 위치' 강조가 조용히 어긋나는 드리프트를 즉시 실패로 잡는다. 앱 코드·컴포넌트·라우트·스키마·데이터·URL·flag 배선 변경은 없고 테스트만 추가했다. 함께 P0 read-only 진단 SQL(box/diagnostic_core_journey_baseline.sql)의 ASCII 인코딩 정리를 재기록한다: 메인 세션이 2026-07-26 20:55 KST에 로그인된 Supabase SQL Editor(CITY ref sozxbnvgsougkliibnxl 화면 확인)에서 전문을 실행해 마지막 주간 결과 8 rows로 성공을 확인했고, 현재 파일은 순수 ASCII(총 4659바이트·non-ASCII 0바이트)로 이미 c7b5ad6에 커밋되어 별도 폴드 잔여는 없다. 신규 SQL 작성·운영 SQL 실행 없음(P3 완료의 유일한 잔여 게이트는 여전히 멱등 마이그레이션 box/supabase_care_shifts_migration.sql을 로그인된 SQL Editor에서 CITY ref sozxbnvgsougkliibnxl 확인 후 실행하는 사장님측 조치). 이번 회차 워킹 트리에 stray 파일은 나타나지 않았다(두 stray stash는 확정 정책대로 격리 유지). `npx tsc --noEmit` EXIT=0, `node --test tests/care-team.test.mjs` 16 pass/0 fail, 변경 파일 lint(오류·경고 0), `git diff --check` 통과, `npm run build` EXIT=0을 통과했다.

- 2026-07-27 P1-2: 핵심 여정 P1 "중복 안내 단일 3단계 온보딩"의 순수 로직 레이어 lib/onboarding-steps.ts를 추가했다. 정규 3단계(explore→care→protect)를 단일 진실원본으로 두고, pendingOnboardingSteps로 이미 안내한 단계를 걸러 여러 진입면에서의 중복 노출을 fail-safe로 막는다(알 수 없는/중복 shown 값 무시, href는 in-app 절대경로만). UI·flag·URL·SQL·데이터 변경 없음(라이브러리 우선, 이후 화면에서 소비). 기존 MapIntroSheet/온보딩 배너는 그대로. P0 read-only 진단 SQL(box/diagnostic_core_journey_baseline.sql)의 ASCII 인코딩 정리 기록: 로그인된 Supabase SQL Editor(CITY ref sozxbnvgsougkliibnxl 화면 확인)에서 수정본 전문을 직접 실행해 마지막 주간 결과 8 rows로 성공 확인했고, 파일은 순수 ASCII(non-ASCII 0)로 정리돼 있다. 신규 SQL 없음. npx tsc --noEmit EXIT=0, node --test tests/onboarding-steps.test.mjs 8 pass/0 fail, 전체 npm test 143 pass/0 fail, 변경 파일 lint(에러·경고 0), git diff --check 통과.
- 2026-07-27 P1-3: 첫 방문 온보딩 표면 단일화 불변식(중복 안내 단일 3단계 온보딩)을 소스 레벨에서 지키는 회귀 감시 	ests/onboarding-single-surface.test.mjs를 추가했다. (1) /onboarding 페이지가 인터스티셜 온보딩 UI를 부활시키지 않고 outer.replace('/map') 리다이렉트 전용으로 남는지, (2) LandingOnboardingGate가 오직 /map으로만 유도하는지, (3) 지도 레이아웃에서 <MapIntroSheet JSX 마운트가 정확히 1회인지 감시한다. 주석/문자열이 아니라 실제 렌더 코드(<MapIntroSheet, outer.replace 대상)만 검사하도록 정규식을 좁혀 오탐을 없앴다. UI·flag·URL·라우트·데이터·파일 삭제 없이 테스트 전용 추가다. 신규 SQL 없음. 함께 P0 read-only 진단 SQL(box/diagnostic_core_journey_baseline.sql) ASCII 인코딩 정리를 커밋 트레일러로 기록한다: 메인 세션이 2026-07-26 20:55 KST 로그인된 Supabase SQL Editor(CITY ref sozxbnvgsougkliibnxl 화면 확인)에서 수정본 전체를 직접 실행해 마지막 주간 결과 8 rows로 성공 확인했고, 현재 파일은 순수 ASCII다. npx tsc --noEmit EXIT=0, node --test onboarding-single-surface 3 pass/0 fail·onboarding-steps 8 pass/0 fail, 변경 lint(오류·경고 0), git diff --check EXIT=0.

- 2026-07-27 P4-16: CareTeamCard의 '현재 위치' 강조가 반드시 계약 역조회(careTeamSectionByHref(pathname))로만 결정되는지 소스 감시 회귀 테스트로 고정했다(care-team 총 17건). (1) usePathname()으로 현재 경로를 읽고, (2) careTeamSectionByHref(pathname)로 현재 섹션을 역조회하며, (3) 현재 위치 항목을 링크 대신 aria-current="page"로 표시하는지 검사한다. 미래 편집이 강조 판단을 하드코딩 경로 비교나 별도 상태로 바꿔 계약과 카드의 '현재 위치' 표시가 조용히 어긋나는 드리프트를 즉시 실패로 잡고, 접근성 계약(aria-current)도 함께 고정한다. 앱 코드·컴포넌트·라우트·스키마·데이터·URL·flag 배선 변경은 없고 테스트만 추가했다. 함께 P0 read-only 진단 SQL(box/diagnostic_core_journey_baseline.sql)의 ASCII 인코딩 정리를 재기록한다: 메인 세션이 2026-07-26 20:55 KST에 로그인된 Supabase SQL Editor(CITY ref sozxbnvgsougkliibnxl 화면 확인)에서 전문을 실행해 마지막 주간 결과 8 rows로 성공을 확인했고, 현재 파일은 순수 ASCII(총 4659바이트·non-ASCII 0바이트)로 이미 c7b5ad6에 커밋되어 별도 폴드 잔여는 없다. 신규 SQL 작성·운영 SQL 실행 없음(P3 완료의 유일한 잔여 게이트는 여전히 멱등 마이그레이션 box/supabase_care_shifts_migration.sql을 로그인된 SQL Editor에서 CITY ref sozxbnvgsougkliibnxl 확인 후 실행하는 사장님측 조치). 이번 회차 워킹 트리에 stray 파일은 나타나지 않았다. npx tsc --noEmit EXIT=0, node --test tests/care-team.test.mjs 17 pass/0 fail, 변경 파일 lint(오류·경고 0), git diff --check 청결, npm run build EXIT=0을 통과했다.
- 2026-07-27 P1-4: 홈 '오늘의 돌봄 인박스' 표면 강화. careInboxMode이고 밥 대기(pendingCount>0)일 때 섹션 헤더 문구를 '내 아이들' -> '오늘의 돌봄'으로 전환해 P1 인박스 프레이밍을 명확히 했다. 기존 카드/스와이프/1탭 밥주기/전체보기 URL·데이터·flag(P1 off거나 kill switch on이면 기존 '내 아이들' 헤더로 fail-safe)는 불변. 순수 라벨 변경으로 삭제 없음. P0 read-only 진단 SQL(box/diagnostic_core_journey_baseline.sql) ASCII 정리는 기존대로: 메인 세션이 2026-07-26 20:55 KST 로그인 Supabase SQL Editor(CITY ref sozxbnvgsougkliibnxl 화면 확인)에서 전문 직접 실행, 마지막 주간 결과 8 rows 성공 확인, 파일은 순수 ASCII(non-ASCII 0). 신규 SQL·실행 없음. npx tsc --noEmit EXIT=0, node --test tests/care-inbox.test.mjs 5 pass/0 fail, 변경 파일 lint(오류·경고 0) 통과.
- 2026-07-27 P1-5: ������ ���� �ιڽ� ��� �����̹� �Һ����� �ҽ� ���� ȸ�ͷ� �����ߴ�. �� tests/care-inbox-header.test.mjs�� (1) MyCatsHero ����� careInboxMode && pendingCount>0 ����Ʈ�θ� '������ ����'/'���� N����'�� �Ѱ�, (2) pendingCount�� ���� ���� ��� countPendingFeed(cats)������ �Ļ��Ǹ�, (3) careInboxMode������ prioritizePendingFeed(cats)�� �̿ϷḦ �ռ���� �ƴϸ� ���� cats�� ���ٴ� ����� �����Ѵ�. flag off/kill switch(careInboxMode off)/pending 0 �� ���� '�� ���̵�'/'doneCount/total ���� ��' fail-safe ������ �����ȴ�. UI��flag��URL�����Ʈ�������͡�SQL ���� ����, ���� ���� ����(���� ����). �ű� SQL ���� SQL ���� ���ʿ�, P0 ���� SQL�� �̹� ���� �Ϸ�(2026-07-26 20:55 KST �α��ε� SQL Editor���� CITY ref sozxbnvgsougkliibnxl Ȯ��, ������ �ְ� ��� 8 rows ����). npx tsc --noEmit EXIT=0, node --test tests/care-inbox-header.test.mjs 2 pass/0 fail��npm test 150 pass/0 fail, ���� ���� lint(��������� 0)�� ����ߴ�.
