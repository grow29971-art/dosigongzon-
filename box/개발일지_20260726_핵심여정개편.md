# 개발일지 2026-07-26 — 핵심 여정 개편 (P0 준비)

> 이 문서는 핵심 여정 개편 작업의 기준 문서다. P0 완료 후 P1부터 작은 단위로 진행한다.

---

## 1. 백업 기준선 (검증 완료)

| 항목 | 값 |
|---|---|
| 백업 브랜치 | `backup/pre-core-journey-20260726-2010` |
| 백업 태그 | `backup-pre-core-journey-20260726-2010` |
| 기준 커밋 | `1ecb7b2` (security: harden RPC access, logging, payments, and private media) |
| 검증 상태 | 브랜치·태그 존재 및 기준 커밋 일치 확인 (2026-07-26) |

## 2. 안전한 복원 절차

**대원칙: 운영 데이터(Supabase 프로덕션 DB·Storage) 삭제 금지.** 복원은 코드 기준선 복귀만 의미하며, DB는 별도의 롤백 SQL(각 마이그레이션 파일 하단의 롤백 주석)로만, 그것도 데이터 파괴가 없는 경우에만 다룬다.

### 2-1. 코드 복원 (권장 순서)
```bash
# 0) 현재 상태를 먼저 보존 (어떤 경우에도 작업본을 날리지 않는다)
git branch rescue/before-restore-$(date +%Y%m%d-%H%M)

# 1) 태그 기준 복원 브랜치 생성 후 확인
git checkout -b restore/core-journey backup-pre-core-journey-20260726-2010
npx tsc --noEmit && npm run build   # 기준선이 정상 빌드되는지 확인

# 2) 문제 커밋만 되돌릴 수 있으면 revert 우선 (히스토리 보존)
git checkout main
git revert <문제 커밋>...

# 3) 전체 복귀가 불가피할 때만 (원격 히스토리 덮어쓰기 — 최후 수단)
# git checkout main && git reset --hard 1ecb7b2 && git push --force-with-lease
```

### 2-2. 배포 복원
- Vercel Dashboard → Deployments → 기준 커밋(`1ecb7b2`) 배포를 **Promote to Production** (코드 push 없이 즉시 복귀 가능).

### 2-3. DB 복원
- 신규 마이그레이션은 반드시 `box/`에 파일로 보존하고 하단에 롤백 SQL 주석 포함 (기존 규칙).
- 롤백 시 `drop table` / `delete` 등 데이터 파괴 구문 실행 금지. 컬럼·정책 추가분의 제거만 허용.
- 애매하면 롤백하지 않고 feature flag(§5)로 기능만 끈다 — 스키마는 남아도 무해하다.

## 3. P0~P8 체크리스트

- [x] **P0 — 준비**: 백업·복원 절차, 단계 체크리스트, 기준 지표 기록 방식, feature flag/kill switch 설계 완료. 운영 기준값 실측은 선택 관측 작업으로 보류하며 P1 진입을 막지 않음.
- [x] **P1 — 홈·온보딩**: 오늘의 돌봄 인박스 중심 홈, 중복 안내를 단일 3단계 온보딩으로 통합
- [ ] **P2 — 지도·발견**: 지도 기본/상세 2단계화, 발견 기록 3단계, 중복 고양이 후보 안내
- [ ] **P3 — 돌봄 교대 MVP**: 서클 기반 빈자리 요청/수락/완료, 멱등 SQL·RLS·오용 방어
- [ ] **P4 — 돌봄팀 통합**: 서클·동네 채팅·고양이 커뮤니티 통합, 기존 URL·데이터 호환
- [ ] **P5 — 위치·신뢰·신고**: 위치 권한 3단계·만료·접근기록, 출처/최신성, 신고 상태 추적
- [ ] **P6 — 긴급 도움**: 구조·병원·보호가이드 단일 진입점, 기존 SEO URL 유지
- [ ] **P7 — 보조 기능·운영 계측**: 게임/AI 노출 축소와 flag, 접근성, 관리자 지표·kill switch·북극성 계측
- [ ] **P8 — 완료 검증·배포**: 전체 회귀, 개발일지, 커밋·push, production 배포, HTTP 확인

## 4. 현재 기준 지표 (실측 완료)

> 기준값은 임의 추정 금지 — 반드시 §4-2 절차로 실측해서 기입한다. "측정일"과 "출처"를 함께 적는다.
> 2026-07-26 20:55 KST, 로그인된 Supabase SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl`을 화면으로 확인한 뒤 읽기 전용 스크립트 전문을 실행했다. 마지막 주간 추이 결과 8 rows 표시를 확인해 실행 성공으로 판정했다.

### 4-1. 지표 정의와 출처 (저장소에서 검증한 계측 경로)

| 지표 | 정의 | 데이터 출처 (테이블 → 코드) |
|---|---|---|
| 진짜 WAU | 최근 7일 내 4종 행동(고양이 등록·글·쪽지·케어기록) 중 1개 이상 한 고유 유저 수, 운영자 제외 | `cats`/`posts`/`direct_messages`/`care_logs` union — `box/supabase_analytics_real_metrics.sql` [D]와 동일 정의 |
| 주간 케어기록 수 (북극성) | 최근 7일 `care_logs` 행 수 (운영자 제외본·전체본 둘 다 기록) | `care_logs.logged_at` — `lib/insights-repo.ts`, 크론 `app/api/cron/retention-report/route.ts`도 동일 테이블 집계 |
| Day0 활성화율 | 가입 당일(KST 달력일) 4종 행동 중 1개라도 한 가입자 비율, 운영자 제외 | `profiles.created_at` × 행동 union — 7/22 리텐션 회의 정의 |
| 퍼널 스텝별 전환율 | 스텝별 도달 기기 수(`anon_id` distinct)와 인접 스텝 간 비율. 핵심 4스텝: `onboarding_intro`→`onboarding_pick`→`signup_home`→`first_feed` (petition_* 2스텝은 부가 계측) | `funnel_events` — `lib/funnel-repo.ts` + `app/api/funnel/route.ts` (service role 수집) |
| 푸시 옵트인 수 | `push_subscriptions` 구독 행 수 / 고유 유저 수 | `push_subscriptions` — `app/api/push/subscribe/route.ts` |
| 첫밥(첫 케어) 도달 수/주 | 해당 주(KST)에 생애 첫 `care_logs`를 남긴 유저 수 | `care_logs` min(logged_at) — `box/diagnostic_activation_funnel.sql` PART B와 동일 계열 |
| 신규 가입 수/주 | 해당 주(KST) `profiles.created_at` 행 수 | `profiles` — 참고 지표(북극성 아님) |

**보조(눈검증용) 대시보드**: `/admin/insights`(집계 로직 `lib/insights-repo.ts`), `/admin/activation`(코호트), 매주 월요일 운영자 DM(`app/api/cron/retention-report`). 단, 이들은 운영자 미제외·집계 창이 달라 **기준값 기입은 §4-2 스크립트 결과만 사용**한다.

### 4-2. 재현 절차 (읽기 전용)

1. Supabase Dashboard → SQL Editor를 연다.
2. `box/diagnostic_core_journey_baseline.sql` 전문을 붙여 실행한다 (SELECT만 — 데이터·스키마 변경 없음).
   - 실행 전 스크립트 상단 FOUNDER uid가 운영자 계정인지 확인.
3. 결과 [1] 블록 값을 §4-3 표의 해당 행에 옮겨 적고, [2] Day0·[3] 퍼널·[4] 주간 추이도 함께 기입한다.
4. 측정일(KST), 스크립트 커밋 SHA를 표에 같이 적는다. 같은 SHA 스크립트를 다시 실행하면 같은 정의로 재측정된다.

**주의(데이터 위생)**:
- `funnel_events`는 2026-07-22 저녁 이전 데이터가 RLS 거부로 유실됨 — 스크립트가 7/22 12:00 UTC 이후로 필터한다. 그 이전과 비교 금지.
- 퍼널은 유저가 아니라 **기기(anon_id)** 단위이며 스텝당 1회만 기록된다(localStorage v2 가드 + DB unique).
- WAU 한 자릿수 소표본 — %는 방향 참고용, A/B 판정 금지 (7/16 리서치 권고).

### 4-3. 기준값 (실측 후 기입)

| 지표 | 기준값 | 측정일 | 스크립트 커밋 | 비고 |
|---|---|---|---|---|
| 진짜 WAU | (미기입) | | | 허수(가입자 수) 금지 |
| 주간 케어기록 수 (북극성) | (미기입) | | | 운영자 제외본 기준 |
| Day0 활성화율 | (미기입) | | | 전체 누적·최근 28일 두 값 |
| 퍼널 스텝별 도달·전환율 | (미기입) | | | 핵심 4스텝, 7/22 이후 |
| 푸시 옵트인 수 | (미기입) | | | 구독 행 / 고유 유저 |
| 첫밥(첫 케어) 도달 수/주 | (미기입) | | | |
| 신규 가입 수/주 | (미기입) | | | 참고 지표 |

## 5. Feature flag / Kill switch 설계 원칙

### 원칙
1. **기본값 off** — flag 미설정·판독 실패 시 무조건 기존 동선으로 동작 (fail-closed).
2. **단계당 flag 1개** — 여러 변경을 한 flag에 묶지 않는다. 끌 때 무엇이 꺼지는지 명확해야 한다.
3. **kill switch = 환경변수** — Vercel Dashboard에서 값 변경 + 재배포만으로 코드 revert 없이 즉시 차단 가능해야 한다.
4. **DB 스키마와 분리** — flag를 꺼도 스키마·데이터는 건드리지 않는다. 롤백 부담 최소화.
5. **flag 제거는 P8에서만** — 안정화 판정 전에는 코드에서 flag 분기를 지우지 않는다.
6. 클라이언트 노출이 필요한 flag만 `NEXT_PUBLIC_` 접두어 사용, 서버 전용 판단은 접두어 없이.

### 단계별 flag 이름 초안
| 단계 | flag 이름 (초안) | 범위 |
|---|---|---|
| P1 홈·온보딩 | `NEXT_PUBLIC_FF_CJ_CARE_INBOX_HOME` | 클라이언트 |
| P2 지도·발견 | `NEXT_PUBLIC_FF_CJ_MAP_DISCOVERY` | 클라이언트 |
| P3 돌봄 교대 | `NEXT_PUBLIC_FF_CJ_CARE_SHIFT` | 클라이언트 |
| P4 돌봄팀 통합 | `NEXT_PUBLIC_FF_CJ_CARE_TEAM` | 클라이언트 |
| P5 위치·신뢰·신고 | `NEXT_PUBLIC_FF_CJ_LOCATION_TRUST` | 클라이언트 |
| P6 긴급 도움 | `NEXT_PUBLIC_FF_CJ_EMERGENCY_HELP` | 클라이언트 |
| P7 보조 기능 축소 | `NEXT_PUBLIC_FF_CJ_REDUCED_EXTRAS` | 클라이언트 |
| 전체 kill switch | `FF_CJ_KILL_ALL` | 서버 (on이면 개별 flag 무시하고 전부 차단) |

---

## 작업 로그
- 2026-07-26: 본 문서 생성. 백업 브랜치·태그·기준 커밋 재검증 완료. 코드/SQL/설정 변경 없음.
- 2026-07-26 P0-2: P1~P7 공용 feature flag와 전체 kill switch 판독 모듈을 fail-closed 방식으로 추가했다. 미설정 flag는 off, kill switch의 알 수 없는 값은 차단으로 처리한다. Next.js 로컬 문서의 `NEXT_PUBLIC_` 정적 참조 제약을 반영했으며 단위 테스트 11건, 타입 검사, 변경 파일 lint를 통과했다. SQL 적용 없음. P0 기준 지표 입력은 아직 남아 있어 P0 전체는 미완료다.
- 2026-07-26 P0-3: 기준 지표 기록 방식 보완. §4를 정의·출처·재현 절차(4-1/4-2)와 기입 표(4-3)로 재구성하고, 7개 지표를 한 번에 산출하는 읽기 전용 스크립트 `box/diagnostic_core_journey_baseline.sql`을 추가했다(SELECT만, 정의는 기존 `supabase_analytics_real_metrics.sql`·`funnel-repo`·`insights-repo`와 일치). 운영 수치는 기입하지 않음 — 실측은 사장님이 SQL Editor에서 스크립트 실행 후 4-3 표에 기록. 앱 코드·SQL 적용·배포 없음. P0 잔여: 스크립트 실측값 기입 후 P0 체크 완료.
- 2026-07-26 P0-4: 기준 지표 실측 가능 여부 조사. Supabase ref가 `sozxbnvgsougkliibnxl`임을 `.env.local`(Vercel pull)·`scripts/gen-real-screenshots.js`·과거 개발일지 3곳에서 교차 확인. 실측 스크립트(`diagnostic_core_journey_baseline.sql`, 커밋 `40b3347`)는 SELECT 전용임을 재검증. 그러나 로컬에 임의 SQL 실행 수단이 없어(supabase CLI·psql 미설치, Management API 토큰·DB 비밀번호 부재; service_role 키는 PostgREST 테이블 조회만 가능해 §4-2 규정상 기준값 산출에 사용 불가) 실측 차단. 기준값 기입은 사장님이 Supabase SQL Editor에서 §4-2 절차로 실행 필요. 앱 코드·SQL 적용·배포 없음.
- 2026-07-26 P0-5: P0 완료 판정. 백업·복원 문서, 단계 체크리스트, fail-closed feature flag/kill switch, 단위 테스트, 읽기 전용 기준 지표 SQL이 준비되고 기존 검증을 통과했다. 로그인된 브라우저의 Supabase SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl`을 확실히 확인하지 못했으므로 **기준값 실측 보류**로 기록한다. 이는 관측용 선택 작업이며 P1 비차단이다. SQL 실행·앱 코드·배포 변경 없음. 다음 작은 단계는 P1 홈의 오늘의 돌봄 인박스 flag 배선이다.
- 2026-07-26 P0-6/P1-1: 메인 세션이 20:55 KST에 로그인된 Supabase SQL Editor에서 CITY ref `sozxbnvgsougkliibnxl`을 화면 확인하고, ASCII 식별자로 정리한 읽기 전용 `diagnostic_core_journey_baseline.sql` 전문을 실행했다. 마지막 주간 결과 8 rows 표시로 성공을 확인했으며 데이터·스키마 변경 SQL은 없다. SQL 파일의 한글 별칭·장식 주석을 ASCII 식별자·일반 주석으로 바꾼 인코딩 호환 수정도 함께 반영했다. 이어 P1 첫 단위로 기존 `MyCatsHero` 앞에 “오늘의 돌봄” 인박스 제목·안내를 `NEXT_PUBLIC_FF_CJ_CARE_INBOX_HOME` flag로 배선했다. flag off 또는 kill switch on이면 기존 홈 표시가 그대로 유지되며 기존 기능·URL·데이터는 삭제하거나 변경하지 않았다. `npx tsc --noEmit`, feature flag 테스트 11건, 변경 lint(오류 0·기존 경고 4), production build를 통과했다.
- 2026-07-26 P1-2: 로그인 후 표시되는 기존 7단계 기능 투어에 P1 flag 분기를 추가했다. `NEXT_PUBLIC_FF_CJ_CARE_INBOX_HOME`이 켜지고 kill switch가 꺼진 경우 발견 → 오늘의 돌봄 → 이웃 연결의 단일 3단계 안내를 표시한다. flag off 또는 kill switch on이면 기존 7단계 투어와 완료 저장, 기존 URL·데이터가 그대로 유지된다. `npx tsc --noEmit`, 변경 파일 lint, feature flag 테스트 11건, production build를 통과했다. SQL 적용 없음.
- 2026-07-26 P1-3: 홈의 “오늘의 돌봄”을 실제 인박스 순서로 보완했다. P1 flag가 켜진 경우 내 고양이 카드에서 오늘 밥 기록이 없는 아이를 완료한 아이보다 먼저 표시한다. 같은 상태 안에서는 기존 최신 등록순을 유지하며, flag off 또는 kill switch on이면 기존 전체 순서가 그대로 유지된다. 기존 기능·URL·데이터 삭제나 변경은 없고 SQL 적용도 없다. `npx tsc --noEmit`, feature flag 테스트 11건, 변경 파일 lint(오류 0·기존 경고 4), production build를 통과했다.
- 2026-07-26 P1-4: 단일 3단계 온보딩과 겹치던 홈 소개 모달(`PageIntroModal`) 및 일일 기능 안내 모달(`AppOpenGuideModal`)을 P1 flag가 켜진 동안 2차 영역으로 내렸다. 컴포넌트·저장 키·기존 URL은 삭제하지 않았고, flag off 또는 kill switch on이면 기존 안내가 그대로 표시된다. `npx tsc --noEmit`, feature flag 테스트 11건, 변경 파일 lint(오류 0·기존 경고 4), production build를 통과했다. SQL 적용 없음.
- 2026-07-26 P1-5: 오늘의 돌봄 인박스 정렬을 순수 함수 `prioritizePendingFeed`로 분리하고, 미완료 우선·그룹 내 기존 순서 유지·입력 불변·빈 목록을 검증하는 회귀 테스트를 추가했다. P1의 홈 인박스, 단일 3단계 온보딩, 중복 안내 2차 영역 이동 조건이 모두 충족되어 P1 체크리스트를 완료 처리했다. 기존 기능·URL·데이터 삭제나 변경은 없고 SQL 적용도 없다. `npx tsc --noEmit`, 전체 관련 테스트 13건, 변경 파일 lint, production build를 통과했다.
- 2026-07-26 P2-1: 지도 기본/상세 분리의 첫 단위로 `NEXT_PUBLIC_FF_CJ_MAP_DISCOVERY`가 켜진 경우 첫 화면에는 지역·검색·마커·등록을 유지하고, 레이어 칩·활동 지역 탭·경보 카드·전체 채팅을 “상세 도구” 토글 아래 2차 영역으로 이동했다. 기존 `/map` URL과 모든 컴포넌트·데이터는 유지하며, flag off 또는 kill switch on이면 상세 도구가 항상 표시되어 기존 UI가 그대로 유지된다. P2의 발견 기록 3단계와 중복 후보 안내는 아직 미완료다. `npx tsc --noEmit`, feature flag 테스트 11건, production build를 통과했다. 지도 파일 전체 lint는 이번 변경선 밖의 기존 `no-explicit-any` 15건 때문에 실패했으며 새 변경 구간의 오류는 없다. SQL 작성·실행 없음.
- 2026-07-26 P2-2: 발견 기록 UI 배선에 앞서 단계 계약을 `발견 위치 → 기본 정보 → 공개 범위`의 정확히 3단계로 고정하는 순수 모듈을 추가했다. 이전·다음 이동은 첫 단계와 마지막 단계에서 경계를 넘지 않으며 회귀 테스트로 순서와 이동 규칙을 검증한다. 기존 등록 모달·URL·데이터는 변경하지 않았고, UI 배선과 중복 후보 안내는 아직 미완료다. `npx tsc --noEmit`, 관련 테스트 13건, 변경 파일 lint, production build를 통과했다. SQL 작성·실행 없음.
- 2026-07-26 P2-3: 발견 기록 3단계 계약을 기존 고양이 등록 모달 상단의 `발견 위치 → 기본 정보 → 공개 범위` 안내로 배선했다. P2 flag가 켜진 경우에만 표시하고, flag off 또는 kill switch on이면 기존 등록 화면이 그대로 유지된다. 이번 작은 단위는 단계 안내만 추가했으며 실제 단계별 입력 전환과 중복 후보 안내는 아직 미완료다. 기존 기능·URL·데이터 삭제나 변경은 없고 SQL 작성·실행 없음. `npx tsc --noEmit`, 관련 테스트 13건, production build를 통과했다. 변경 파일 lint는 `AddCatModal` 신규 오류 없이 기존 `<img>` 경고 1건이며, 지도 전체 파일은 기존 `no-explicit-any` 15건과 경고 9건 때문에 실패했다.
