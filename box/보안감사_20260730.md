# 도시공존 보안감사 리포트 — 2026-07-30

> 수행: 제로투(AI) + 서브에이전트 회의 (ag_ops_security · ag_ops_crypto-env · ag_dev_nextjs · ag_qa_devils-advocate)
> 대상: `C:/Users/grow2/city` (Next.js 16 + Supabase)

## 총평
- **확정 Critical/High 취약점 0건.** 라우트 계층·시크릿·RLS 모두 대체로 견고 (2026-07-26 보안패치 이력 반영됨).
- 남은 것: Medium 코드 수정 2건, RLS 프로덕션 실측 2건.

---

## ✅ 조치 완료 (코드 수정 + 타입체크·ESLint 통과)

### #1 `app/api/cats/generate-card/route.ts` — AI 비용 폭탄 차단
- **원자적 선점**: `card_generated_at`을 `null→now()` 조건부 UPDATE로 잠금 → 동시 요청 N개여도 이긴 1개만 Gemini 호출. 나머지는 기존 카드 반환.
- `perfect_catch_count` 증가를 선점 이후로 이동 → 타이틀 파밍 차단.
- 유저당 분당 20회 레이트리밋 + 이미지 base64 8MB 상한 + mime 화이트리스트(jpeg/png/webp).

### #2 `app/api/payment/webhook/route.ts` — 취소 동기화 버그 2건
- **부분취소 방어**: `balanceAmount>0`(진짜 부분환불)이면 전액취소 처리 안 함 → reconcile/수동 위임. 전액취소(`balance=0`)만 자동 동기화.
- **조건부 전환**: `paid→cancelled` + `.eq("status","paid").select("id")` → 웹훅 중복 재전송 시 재고 이중 복원 차단 (cancel 라우트와 동일 패턴).

---

## 🟠 남은 조치

### RLS 프로덕션 실측 2건 (파일 ≠ 실제 적용) → **둘 다 실측 완료. 아래 🔬 절 참조**
1. ~~**[High·확인필요] `v_recent_cat_location_changes` 뷰**~~ → ✅ **`security_invoker=true` 적용 확정.** 조치 불필요.
2. ~~**[검증] anon 프로브 재실행**~~ → ✅ **8개 항목 중 7개 차단 확인.** 로그인 세션 프로브 1건만 미검증(테스트 계정 비번 부재).

### Medium — profiles 로그인 교차조회 → **코드 단계 완료, SQL 2건 대기**
- authenticated가 남의 `invite_code`(초대 파밍)·coins를 REST로 SELECT 가능. **실측으로 재확인됨**(344명분).
- 조치: 공개 안전컬럼 전용 VIEW(`profiles_public`, 이미 프로덕션 존재) + 앱 교차조회 **18곳 이관 완료** + base RLS를 self+admin으로 잠금(SQL 대기).
- 실행 순서·검증 절차: **`box/supabase_profiles_lockdown_RUN_ORDER_20260730.md`**
- ⚠️ **SQL 4번을 실행하기 전까지 이 항목은 미조치 상태다.** 코드 배포는 회귀를 막을 뿐 보안 이득이 0.

---

## 🟡 백로그 (급하지 않음)
- 인메모리 레이트리밋(`notify-inquiry`·`push/send`)을 Postgres RPC로 이전 (Vercel 다중 인스턴스에서 한도 ×N 무력화).
- admin 쓰기 repo 7종을 서버 API로 이전 or RLS 배포 게이트화 (현재 서버측 인가 백스톱 0, RLS 단일 의존).
- cron/admin 시크릿 비교 `crypto.timingSafeEqual`로.
- gitleaks pre-commit 훅 추가.

---

## 🔬 RLS 프로덕션 실측 결과 (2026-07-30, REST 프로브)

수단: `.env.local`의 anon/service_role 키로 PostgREST 읽기 전용 호출.
(레포에 raw SQL 수단이 **전무** — supabase CLI·psql·DATABASE_URL·Management API 토큰 모두 부재.
확인: `box/개발일지_20260726_핵심여정개편.md:129`가 동일 결론을 기록. 따라서 `pg_class` 직접 조회는 SQL Editor 외 경로 없음.)

### ① anon 프로브 판정표

| # | 프로브 | 결과 | 판정 |
|---|---|---|---|
| 1 | `GET /profiles?select=email` | `401 / 42501 permission denied` | ✅ **차단됨** |
| 2 | `GET /profiles?select=invite_code,coins` | `401 / 42501` | ✅ **차단됨** (anon 한정) |
| 3 | `GET /profiles?select=*` | `401 / 42501` | ✅ **차단됨** |
| 4 | `INSERT INTO admins` (FK 미존재 UUID로 안전 시도) | `401 / 42501 new row violates RLS` | ✅ **차단됨** — RLS 단계에서 거부(FK까지 못 감 = 정책 자체가 막음) |
| 5 | `GET /orders?select=id` | `200 rows=0` | ✅ **차단됨** |
| 6 | `GET /direct_messages?select=id` | `200 rows=0` | ✅ **차단됨** |
| 7 | `GET /cat_location_history?select=id` | `200 rows=0` | ✅ **차단됨** |
| 8 | `GET /profiles?select=id,nickname` | `200 rows>0` (344명 열람 가능) | 🟡 **의도된 노출** — 공개 작성자 정보. 다만 이게 SELECT 정책이 아직 `USING(true)`라는 증거 |

> ③의 "로그인 세션으로 타 유저 orders/direct_messages 조회"는 **실행 못 함**.
> 테스트 계정(`grow29971+claudetest@gmail.com`) 비밀번호가 레포에 없고
> (`box/개발일지_20260718.md:118-119`에 "비번은 리포 미기록"으로 명시),
> 프로덕션에 계정을 새로 만드는 건 쓰기라 금지 범위였다. → **미검증 1건으로 남김.**

### ② `v_recent_cat_location_changes` — `security_invoker=true` **적용 확정**

`pg_class.reloptions`를 직접 못 봤지만, 행동 증거 2개가 독립적으로 같은 결론을 가리킨다.

1. **뷰 자체**: anon 조회 → `200 rows=0`. 그런데 service_role은 같은 뷰에서 **23행**을 본다.
   `security_invoker`가 꺼져 있었다면 뷰는 소유자(postgres) 권한으로 실행돼 하부
   `cat_location_history`의 RLS를 우회하므로 **anon에게 23행이 전부 나왔어야** 한다. 0행이 나왔다 = 호출자 권한으로 RLS가 걸렸다.
2. **같은 마이그레이션의 다른 뷰**: `my_invite_stats`를 anon으로 조회하면
   `42501 permission denied for table **profiles**`가 뜬다. 이 에러는 뷰 본문이
   **호출자 권한으로** 실행될 때만 발생한다(definer였다면 소유자가 profiles 권한을 갖고 있어 에러가 없다).
   두 뷰는 `supabase_security_invoker_views_fix_migration.sql` **한 파일**에서 함께 수정되므로,
   한쪽이 invoker면 다른 쪽도 실행된 것이다.

**→ `box/supabase_security_invoker_views_fix_migration.sql`은 이미 실행됐다. 재실행 불필요.**
스토킹 벡터 없음. `box/_SQL_대장_20260717.md:192`의 "불명" 항목을 이걸로 닫을 수 있다
(대장 33행이 "뷰 존재"를 근거로 ✅ 처리했던 건 근거가 틀렸다 — 뷰는 fix와 무관하게 4월부터 존재했다.
결론은 같지만 이유가 달랐고, 이번엔 행동 증거로 확정했다).

### ③ profiles 락다운 상태 — **미적용 확정**

- anon이 남의 `id,nickname`을 읽는다 → SELECT 정책이 아직 `USING(true)`.
  (`profiles_select_self_or_admin`가 적용됐다면 anon은 `auth.uid()`가 null이라 0행이어야 함)
- `profiles_public` 뷰: **존재함**(7개 컬럼 전부 정상, count 344 = base와 동일)
- RPC `total_user_count`·`get_inviter_code`: **미존재**(`PGRST202`)
- 앱 코드 repoint: main에 참조 0건이었음 → **미배포였음**

즉 7/24 RUN_ORDER의 1-1만 실행됐고 1-2·1-3·1-4가 전부 멈춰 있었다.
**authenticated는 여전히 남의 `invite_code`·`coins`를 REST로 덤프 가능**(344명분) = Medium 확정.

---

## ✅ 안전 확인됨
- 시크릿/env 유출 0 (회전 필요 키 없음), `service_role`은 `server-only` 하드경계, git 클린.
- 결제 승인경로(`confirm`): 금액 이중검증 + 원자 선점 + 자동환불.
- DM·orders(전화/주소 PII)·points/coins·circle·event_keyring: 교차유저 격리.
- `admins` 테이블: insert 정책 없음 = 권한상승 차단.
- 무인증 admin/cron 호출·IDOR·권한상승: 전부 차단.

---

# 후속조치 이력 (2026-07-30)

> 서브에이전트 4종(`ag_ops_security`·`ag_dev_nextjs`·`ag_qa_devils-advocate` + 실측 프로브) 회의 결과.
> **프로덕션 DB 쓰기는 하지 않았다.** 읽기 프로브만 실행하고, 적용할 SQL은 파일로만 준비했다.

## 커밋 1 — `fix(보안)`: 감사 확정 Medium 2건

| 파일 | 왜 |
|---|---|
| `app/api/cats/generate-card/route.ts` | AI 비용 폭탄 차단. `card_generated_at` `null→now()` 원자적 선점으로 동시 요청 중 1개만 Gemini 호출. `perfect_catch_count` 증가를 선점 이후로 옮겨 타이틀 파밍 차단. 분당 20회 제한 + base64 8MB 상한 + mime 화이트리스트. |
| `app/api/payment/webhook/route.ts` | 부분취소(`balanceAmount>0`)를 전액취소로 오처리하던 버그 수정. `paid→cancelled` 조건부 전환으로 웹훅 재전송 시 재고 이중 복원 차단. |
| `box/보안감사_20260730.md` | 감사 리포트 자체. |

## 커밋 2 — `refactor(보안)`: profiles 교차조회 18곳 → `profiles_public`

7/24 브랜치 `security/profiles-lockdown`에 이미 준비돼 있던 작업이 **머지되지 않고 방치돼 있었다.**
브랜치 자체는 main 대비 145파일이 뒤처져 통째 머지 시 몇 주치 작업이 되돌아가므로,
**repoint 커밋 `6b17431`만 cherry-pick**하고 나머지는 새로 작업했다.

| 파일 | 왜 |
|---|---|
| `lib/blocks-repo.ts`, `lib/circle-chat-repo.ts`, `lib/circles-repo.ts`(3), `lib/notifications-repo.ts`, `app/circle/[circleId]/chat/page.tsx`, `app/circle/join/[ownerId]/page.tsx` | 남의 프로필(닉네임/아바타) 읽기 → `profiles_public` 뷰. base가 잠겨도 안 깨지게. (7/24 이식분) |
| `app/about/page.tsx`, `app/celebrate/page.tsx`, `app/opengraph-image.tsx`, `app/components/HomeLanding.tsx`, `app/components/Event1000Banner.tsx`, `app/(main)/event/keyring/page.tsx` | 가입자 수 카운트 → `profiles_public` count. **원안(`total_user_count()` RPC)에서 변경** — RPC 방식은 SQL 실행보다 배포가 먼저 가면 가입자 수가 0명으로 보이는 회귀가 났다. 뷰는 definer라 락다운 전후 모두 344를 반환(실측). |
| `lib/invites-repo.ts` | 남의 `invite_code` 직접조회 → `get_inviter_code()` RPC. RPC 미생성 구간을 위해 기존 직접조회 폴백 추가. |
| `lib/users-server.ts`(3), `app/api/search/route.ts`, `app/(main)/admin/event-keyring/page.tsx` | **7/24 이후 새로 생긴 미커버 지점.** 공개 프로필·공개 스탯·동네 이웃 목록·통합검색·응모자 목록. 이걸 빼먹고 SQL을 실행했으면 전부 조용히 빈 값이 됐다. |
| `box/supabase_profiles_lockdown_RUN_ORDER_20260730.md` | 실측 기반 실행 순서 문서(신규). |

## 커밋 3 — `fix(보안)`: 락다운 후 **조용히 틀어지는** 회귀 2건 선제 수정

악마의 변호인 검증에서 나온, 에러가 안 나서 배포 후에도 한동안 모를 유형.

| 파일 | 왜 |
|---|---|
| `lib/blocks-repo.ts` | 차단 목록이 `profiles!user_blocks_blocked_id_fkey(...)` **중첩 조인**으로 base를 읽었다. 락다운 후 PostgREST는 RLS로 걸러진 to-one 임베드를 **에러 없이 `null`**로 반환 → `if(error)` 폴백이 영영 발동 안 하고 닉네임·아바타만 빈칸. 중첩 조인 제거, `profiles_public` 2단 조회를 기본 경로로. |
| `app/api/auth/callback/route.ts`(2곳) | `count(profiles) <= 100`이면 `early_supporter` 부여. 유저 세션 클라이언트라 락다운 후 count가 **항상 1** → **신규 가입자 전원 타이틀 오지급**. `profiles_public` count로 교체. |
| `box/supabase_profiles_lockdown_RUN_ORDER_20260730.md` | 검증 절차 교체(아래). |

**검증 절차의 구멍도 같이 메웠다**: 원래 검증안이던 SQL Editor `select count(*) from profiles`는
`postgres` 롤로 실행돼 RLS를 우회하므로 **락다운 성공이든 실패든 344**가 나온다 — 실패를 감지 못 한다.
비관리자 JWT REST 프로브만 합격 기준으로 바꾸고, 실행 **전** `pg_policies` 정책 이름 대조 단계를 추가했다
(락다운 SQL은 정책 2개 이름만 `drop`하는데 프로덕션 실제 이름이 미확인 → 다른 이름의 permissive 정책이
남아 있으면 OR로 합쳐져 락다운이 무효가 된다).

---

## 🔴 성우가 Supabase SQL Editor에서 실행할 것

**이 세션에서 프로덕션 DB에 쓴 것은 없다.** 아래는 전부 대기 상태.

| 순서 | 파일 | 성격 | 안 하면 |
|---|---|---|---|
| 0 | (조회만) `select policyname, cmd, qual from pg_policies where tablename='profiles' and cmd='SELECT';` | 사전 확인 | 2번 락다운이 조용히 무효가 될 수 있음 |
| 1 | `box/supabase_profiles_lockdown_rpcs_migration.sql` | 추가만 — 무해 | 락다운 후 "나를 초대한 사람 코드"가 빈칸 |
| 2 | `box/supabase_profiles_authenticated_lockdown_migration.sql` | **실제 잠금** | **Medium 미조치 유지** — 로그인 유저가 344명분 `invite_code`·`coins` 계속 덤프 가능 |

> **2번은 코드 배포(위 커밋 3개)가 프로덕션에 올라간 것을 확인한 뒤에.** 순서를 어기면
> 서클 멤버·차단목록·알림·공개 프로필·통합검색·이웃 목록이 전부 빈 화면이 된다.
> 상세 절차·검증·롤백: `box/supabase_profiles_lockdown_RUN_ORDER_20260730.md`

**실행 불필요로 판명된 것**: `box/supabase_security_invoker_views_fix_migration.sql`
— 이미 실행돼 있음이 행동 증거로 확정됨(🔬 절 참조).

## ⛔ 이 세션에서 못 끝낸 것

- **git push·`vercel --prod` 둘 다 인증 미완료로 막힘.** 커밋 3개는 **로컬에만** 있다.
  - `credential.helper=manager`가 GUI 인증창을 띄우는데 비대화형 세션이라 응답 불가. `gh` CLI 미설치.
  - Vercel도 "No existing credentials found" — 7/28 머신 이사 후 로그인 미완료 항목 그대로.
  - → 성우가 `git push origin main` + `vercel login` 후 `vercel --prod --yes`를 직접 실행해야 한다.
- **로그인 세션 RLS 프로브 1건 미검증** — 타 유저 `orders`/`direct_messages` 교차조회.
  테스트 계정(`grow29971+claudetest@gmail.com`) 비밀번호가 레포에 없고, 프로덕션에 계정을
  새로 만드는 건 쓰기라 금지 범위였다. anon 레벨은 차단 확인됨.
- **로컬 `npm run build` 불가** — 이 PC의 Application Control 정책이 Next의 네이티브 SWC 바이너리를
  차단한다(`@next/swc-win32-x64-msvc`). 내 변경과 무관한 환경 문제. `npx tsc --noEmit`과 ESLint로 대체 검증했고
  둘 다 통과. 실제 빌드는 Vercel(리눅스)에서 정상 동작할 것.
