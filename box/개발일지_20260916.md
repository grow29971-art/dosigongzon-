# 개발일지 2026-09-16 — 리디자인 방향 결정·시빅 포스터 스킨 배포 → 당일 전체 되돌림

## 배경

사장님: "전체적으로 사이트맵들이 AI티가 너무 많이 나 촌스럽고, 디자인 레퍼런스 인터넷에서 찾아서 리디자인하고 싶어."
8/13 토큰 수렴, 8/26 D 아이보리 에디토리얼을 거쳤는데도 같은 불만이 세 번째로 나왔다.

## 진단 (비로그인 390px 실측 12화면)

토큰(색·폰트·라운드)은 정리돼 있었다. 촌스러움의 실체는 **화면 문법** 5종:
1. 모든 것이 카드(카드 안 카드, 틴트 박스 안 아이콘)
2. 모든 모서리가 둥긂(14~22px + 필 + 원형)
3. 한 화면에 색 5종 이상(보호지침은 카테고리마다 무지개 파스텔)
4. 설명문 길고 밀도 낮음
5. 진짜 자산(실사 고양이 사진)이 지도·목록·홈에 없음
+ 첫 진입 오버레이 중첩(지도 시트·쿠키·포인트 안내·AI집사 모달)

## 레퍼런스

같은 문제를 푼 곳만: 당근 SEED(동네 리스트) · iNaturalist(시민 관찰 기록 카드) · Merlin Bird ID(내가 본 목록) ·
Airbnb 지도 탐색(사진 마커→카드, 흑백 CTA) · AllTrails(활동 로그) · 29CM(무장식 커머스).
탐색 도구: wwit.design · oh-my-design.kr/collections/korean-apps · mobbin.

## 방향 결정

디자인 캔버스(artifact 9nq1goXXwK71FdaGtqB3Lw)에 3안 제시: 추천안 「동네 기록장」 / 대안 A 「익숙한 동네앱」 /
대안 C 「시빅 포스터」. **사장님 선택 = 대안 C, 단 "UI/UX는 기존 유지"** → 구조·동선 손대지 않고 스킨만 교체.

시빅 포스터 문법: 잉크 검정 CTA · 라임(#D4F542) 액센트는 검정 위에서만 · 라운드 0 · 그림자 대신 검정 룰 ·
Gothic A1 900 제목 · 순백 배경 · 뉴트럴 그레이.

## 배포 (8커밋, 전부 revert 가능 단위)

| # | 커밋 | 내용 |
|---|---|---|
| 1 | e7b74540 | globals.css 토큰 전면 교체 + Tailwind rounded-* 언레이어드 오버라이드(라운드 킬 스위치) |
| 2 | 4da4f0d2 | 제목 웹폰트 Noto Serif KR → Gothic A1 900 (fontsource, CSP 범위 내) |
| 3 | 627b88ca | scripts/civic-swap.mjs 1차 — 웜 뉴트럴·테라코타 hex 124파일 315곳 |
| 4 | 1749a635 | BottomNav 흰 바 + 2px 검정 룰 + 활성 탭 라임 블록 |
| 5 | eb6e1937 | ui/Button weak·text 변형, ui/Card 검정 룰 |
| 6 | bcadb78a | PageIntroBanner 검정 밴드 + 라임 아이콘 |
| 7 | a36d2438 | civic-swap 2차 — 토큰 밖 아이보리 배경·피치 틴트·웜 브라운 116파일 354곳 |
| 8 | 24629293 | civic-swap 3차 — 장식용 카테고리 무지개(블루·틸·퍼플·오렌지·골드·세이지·AI집사 초록) 108파일 446곳 |

의미색 유지: 위험 빨강(#D85555·#B84545), 성공 초록(#22B573·#2E7D32·#5BA876·--color-sage), 카카오 노랑,
네이버 초록, 관리자 인디고, 찜 핑크(--color-like). 지도 마커 slot 구분은 블루→그레이(#6B6B6B)로 대비 유지.

`npx tsc --noEmit` 매 커밋 통과. Vercel 배포 성공은 GitHub 커밋 상태 API로 확인. 계측 라인(HomeAuthed·map/layout·cats/[id])은
색 리터럴 외 무변경.

## 롤백

- 전체: 8커밋 역순 revert (`git revert 24629293 a36d2438 bcadb78a eb6e1937 1749a635 627b88ca 4da4f0d2 e7b74540`).
- 라운드만 되돌리기: globals.css "라운드 킬 스위치" 블록 삭제 + radius 토큰 값 복원.
- 기준선(D 아이보리): e7b74540 이전 = 070d5572.

## 남은 것 (문법 교체 — 다음 세션)

스킨만 바꿨으므로 문법 5종은 그대로다. 사장님이 UI/UX 유지를 지시했으니 구조 변경은 승인 후:
- 보호지침 틴트 박스 아이콘 행(8회 반복) → 헤어라인 리스트
- 고양이 상세 카드 안 카드·점선 빈상태 카드 → 사진 풀블리드 + 수치 스트립
- 첫 진입 오버레이 1개 제한(쿠키는 하단 한 줄)
- 지도 마커·목록에 실사 사진

## 제약 메모

8/29 PMF 회의의 "인터뷰 3명 전 3커밋 초과 변경 금지"는 사장님이 오늘 방향 선택·실행 지시로 사실상 해제.
스킨 커밋 8개는 그 결정에 따른 것이며, 인터뷰 과제 자체는 여전히 미실행(0/3).

## 되돌림 (같은 날 오후)

사장님: "디자인 이전으로 되돌리고싶어."

- 실행: `git revert --no-commit 070d5572..24629293` → 한 커밋 55c46c99 (8커밋 전체, 역순 개별 revert 대신 단일 커밋).
- 검증: 기준선 070d5572 대비 `git diff` 0줄, `npx tsc --noEmit` 통과, Vercel 배포 success(GitHub 커밋 상태 API),
  라이브 CSS에서 `--color-primary:#b05c36`(D 아이보리 테라코타) 서빙 확인.
- 결과: 코드는 8/26 D 아이보리 에디토리얼 상태와 동일. 스킨 커밋 8개는 히스토리에 남아 있어 개별 cherry-pick으로 재적용 가능
  (예: 폰트만 살리려면 `git cherry-pick 4da4f0d2`).
- 해석: 시빅 포스터(대안 C)는 실물 확인 후 기각. 리디자인 방향은 다시 미정. 위 "남은 것(문법 교체)" 항목은 스킨과 무관하게 유효.

## 커뮤니티 홈 피드형 전환 (되돌림 직후)

사장님 레퍼런스: 카페 앱 피드 스크린샷(KakaoTalk_20260916_131357121.jpg) — "커뮤니티 페이지를 이런식으로".

- 08de9ce5 feat: 상단 sticky(제목·검색 + 가로 필 칩: 전체·긴급·돌봄 부탁·자유·임보·입양·중고마켓·인기·내 동네)
  + 카드 없는 전폭 피드(`app/components/CommunityFeedItem.tsx`: 아바타·닉네임·카테고리 / 제목+본문 8줄 클램프 /
  첫 사진 / 하트·댓글·공유 / 좋아요 N개 / 댓글 N개 모두 보기 / 첫 댓글 / 상대시간) + "글쓰기" 확장 FAB.
  `listFirstCommentsForPosts`(post-comments-repo, 비밀 댓글 제외) 신설. 상세 페이지 댓글 제목에 `#comments` 앵커.
- 807d8a17 fix: 운영팀 공지(pinned 6건)가 피드 첫 화면을 다 차지 → 접힌 공지 블록(첫 제목 +N, 펼치면 목록).
- 이전 섹션(안내 모달·배너·글감 프롬프트·돌봄팀 카드·방문자 수·카테고리 그룹 리스트·인기 글)은
  `SHOW_COMMUNITY_LEGACY=false`로 숨김(코드 보존). `/community/category/*`·`/community/popular` 라우트는 딥링크용 유지.
- 좋아요는 기존 `post_vote_update` RPC + localStorage 투표 관례 그대로(관리자 누적·일반 토글·실패 롤백).
- 레퍼런스의 다크 테마는 적용하지 않음 — 현행 D 아이보리 토큰 위에 구조만 이식(테라코타 활성 칩·FAB).
- 검증: tsc 통과, Vercel success, 사장님 크롬 로그인 세션으로 프로덕션 /community 실측(좋아요 7·댓글 2·첫 댓글 렌더 확인).

## 커뮤니티 운영 페르소나 봇 (오후)

사장님 요청: "AI 집사 빼고 페르소나마다 닉네임 부여해서 글·댓글 자동 작성". 운영 계정임이 드러나지 않게 해달라는
요청은 거절(실사용자 기만)하고, **"운영" 배지가 붙는 조건**으로 구현. 4커밋(6382c586·e31439e5·09713810·c2c55a65).

- `lib/community-personas.ts`: 집사 나비(다정·질문형, 글+댓글) · 골목지기(실용 팁, 글+댓글) · 치즈네이모(응원, 댓글만).
  폴백 글·댓글 문구 내장, `ALL_BOT_NAMES`(구 "AI 집사 나비" 포함)로 봇 식별.
- `community-topic` 크론: 페르소나 로테이션(직전 글과 다른 페르소나), 최근 8건 제목 중복 회피, `author_title='staff'`.
  스케줄 화·금 → 월·수·금 09:30 KST, 중복 가드 72h → 40h. 유저 글 24h 내 3건 이상이면 스킵(기존 유지).
- `community-comment` 크론 신설(매일 20:30 KST): 자유게시판·숨김 아님·봇 글 아님·2시간~7일 사이 유저 글 중
  댓글 적은 순 1건에 첫 댓글, 글쓴이에게 푸시 1건. 20시간 쿨다운(하루 최대 1건). 긴급·돌봄 부탁·임보·입양·중고마켓 제외.
  proxy.ts KNOWN_CRONS 화이트리스트에 추가(빠뜨리면 미들웨어가 차단).
- 배지: `lib/titles.ts` ADMIN_TITLES에 `staff`("운영") 추가, `TitleBadge`가 관리자 타이틀도 렌더, 피드 아이템 작성자·첫 댓글 옆 표시.
- **실행 게이트**: `box/supabase_staff_title_guard_20260916.sql` — `is_admin_only_title`에 'staff' 추가. 실행 전에는 로그인 유저가
  PostgREST로 `author_title='staff'`를 넣어 운영 사칭 가능. 실행 후 `select public.is_admin_only_title('staff')` = true 확인.
- 검증: tsc 통과, Vercel success(c2c55a65). 크론 수동 트리거는 이 세션 권한 분류기가 차단 → 오늘 20:30 KST 자동 발화 후
  `cron_runs`·post_comments(author_title='staff')로 실측 필요. 수동 확인은 `curl -H "Authorization: Bearer $CRON_SECRET" https://dosigongzon.com/api/cron/community-comment`.
- 지표 주의: 대외 자료·watch-probe 집계에서 페르소나 글·댓글은 `ALL_BOT_NAMES`/`author_title='staff'`로 제외할 것.

## 커뮤니티 운영 페르소나 봇 — 크론 마무리 + exe (오후)

사장님: "커뮤니티 기능에 글이랑 덧글 자동으로 써주는 봇을 만들고싶어" → "exe 프로그램으로 만들고싶어".

- 앞선 세션의 미커밋 작업(페르소나 로테이션·댓글 크론·운영 배지 'staff')을 검증 후 4커밋으로 배포:
  6382c586 운영 배지 타이틀 + 피드 배지 / e31439e5 글 크론 페르소나 로테이션 / 09713810 댓글 크론 신설(매일 20:30 KST) + 글 크론 월·수·금 + proxy KNOWN_CRONS /
  c2c55a65 staff 사칭 차단 SQL(실행 대기) + business-rules 문서화.
- d0dd3ac2 exe 용 봇 API 4종 `/api/bot/community/{personas,candidates,post,comment}` + `lib/community-bot.ts`
  (비밀키 인증 = COMMUNITY_BOT_SECRET, 없으면 CRON_SECRET 겸용 / 운영 배지 강제 / 글 3·댓글 10 일일 상한 / 글당 운영 댓글 1개 / 글쓴이 푸시).
  계약은 `docs/contracts.md` 5절. 배포 success, 무인증 401·인증 200 실측.
- exe 는 별도 저장소 `C:\Users\grow2\community-bot` (스레드봇과 같은 뼈대, 저장소 분리). `release/도시공존커뮤니티봇.exe` → OneDrive 바탕화면.
- 실측: 최근 7일 유저 글 0건 → 댓글 크론·댓글 후보는 당분간 "대상 없음"으로 스킵. `is_admin_only_title('staff')` 는 아직 false(SQL 미실행).
- 기존 테스트 실패 5건(shop/coins/care 모듈 삭제 잔재)은 이번 작업과 무관 — 별도 정리 필요.

**사장님 할 일**: ① `box/supabase_staff_title_guard_20260916.sql` 실행 ② (선택) Vercel 에 `COMMUNITY_BOT_SECRET` 추가 ③ exe 설정에 비밀키 입력.
