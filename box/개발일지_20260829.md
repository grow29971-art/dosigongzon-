# 개발일지 2026-08-29

## 오늘 한 일 요약
사장님 번아웃 복귀 → 주간 방향 원탁회의(6관점) → **버그 사냥 3팀 병렬 감사 → 12건 수정·배포 → 카드·배틀 폐지 DB까지 3단 완결**. 커밋 4개 배포(success), 테스트 257/257 그린 복구, SQL 실행·8종 프로브 검증.

| 커밋 | 내용 |
|------|------|
| `bc99e2b` | 월동 인터뷰 키트 + 9/2 판정 기준 사전 박제 (회의 산출물) |
| `adcb186` | 버그 사냥 일괄 수정 (P0·회귀·보안·잔존물 12건) |
| `37ed642` | 카드 제거 SQL — cats_public_map 뷰 선드랍·재생성 (2차 2BP01) |
| `(SQL)` | supabase_card_system_removal_20260827.sql 실행 완료 |

---

## 1. 복귀 회의 (6관점: 그로스·도메인·심리·BM·B2G·악마)
- 주간 설계: 당근 글 1건+급식소 대면으로 월동 인터뷰, 행정 3건(성동구 전화·홈택스·나라장터).
- **악마의 진단이 핵심**: 최근 4일의 대형 변경 연발+번복 = "번아웃기의 통제감 회복 행동". 처방 = 인터뷰 10→3명 하향·오전 1블록만·번복 0회·**되돌릴 수 없는 결정 48시간 쿨다운**. 9/2 판정 기준을 사전 박제(당일 재해석 금지).
- 산출물: `box/월동인터뷰_키트_20260829.md` (접근 문구·Mom Test 질문 6·구매의사 판정선·면담록 양식=B2G 증빙 겸용·결과별 시나리오).
- 이후 사장님이 "소프트웨어 할 것"으로 방향 전환 → 발품 플랜은 키트로 보존.

## 2. 버그 사냥 — 3팀 병렬 감사 (카드 폐지 잔여 / 신설 표면 보안 / 돈 계산 정합)

### 🥇 P0: 돌봄 레벨 무증상 사망 지뢰 (SQL 실행 전 발견)
`add_cat_card_exp` RPC가 DROP 예정이던 `card_generated_at`을 조건으로 읽음 → 카드 제거 SQL을 그대로 실행했으면 **모든 돌봄 기록의 EXP 적립이 42703으로 조용히 영구 중단**(fire-and-forget이라 화면 무증상). SQL에 함수 재정의 블록 추가로 해체. **"존치 결정한 기능이 DB 함수 의존으로 같이 죽는" 유형 — 기능 폐지 시 RPC/트리거 의존성도 전수 조사할 것.**

### 🥈 진짜 회귀: 테스트 스위트 8/20부터 상시 실패
게이트 6(반품비 상수→함수)때 테스트 미동반 수정 → `RETURN_SHIPPING_FEE` import가 SyntaxError로 스위트 전체 exit 1, 9일간 CI 빨간불(진짜 회귀 탐지 불능 상태). `returnShippingFeeFor` 연동으로 수리, **257/257 그린 복구**. security-invariants의 삭제된 equip-item 검사도 제거.

### 🥉 보안 4건 (신설 표면 — 자금탈취급 0, 전부 Low~Low-Med)
- **주소 마스킹 우회**: 공백 토큰화 의존이라 무공백 주소는 통째로 텔레그램 통과 → 행정구역(동·읍·면) 경계 정규식 절단 + 20자 상한 이중화. 1글자 이름 전체 마스킹.
- CRON_SECRET `timingSafeEqual` 상수시간 비교(크론 2종).
- notify-shipping 주문당 10분 1회 rate limit(중복 발송 방지).
- signup_source DB 형식·길이 CHECK(잠복 저장형 XSS 봉인 — 현재 렌더 싱크 0 확인).

### 기타 수정
- 기능 투어의 폐지된 "카드 등급" 슬라이드 삭제(신규 유저에게 없는 기능 약속 중이었음), 잔존 카드 카피 8곳("카드 EXP"→"돌봄 EXP" 등), 소비자 0인 cat-card-levelup 이벤트 제거.
- **닉네임 중복 실존 확인**("학익2동 미미" 2명 — 사칭 가능 상태) → SQL로 중복 해소+lower(nickname) unique 인덱스.

### 클린 판정 (감사의 절반은 "이상 없음"의 확인)
돈 계산 4곳 계약(트리거·게스트RPC·confirm·webhook) **불일치 0** — floor·100%분기·null→0·sale_price 우선·포인트 위치 전부 동일식. 신설 RLS 3종(product_costs·fund_adjustments·fund_snapshot) 구멍 0. 로그인 실패 7일 0건. **크론 09:01 정시 발화 — Vercel Pro 효과 실증**(Hobby 땐 09:24~38). 수용 리스크로 기록: F-2(다이제스트 원가는 현재값 — 표시 전용 주석 명시), F-3(webhook is_active 미확인 — 정책 비대칭).

## 3. 카드 제거 SQL 실행 — 뷰 의존성 2연전 끝에 완결
- 1차 2BP01: profiles_public(어제 해소) → **2차 2BP01: cats_public_map** — cats 전 컬럼을 동적 복사하는 좌표 퍼징 뷰라 배틀 컬럼을 물고 있었음. CASCADE 금지(지도 공개 통로), **drop→컬럼정리→동적 블록 재생성→grant+쓰기 revoke 재적용** 순서로 해소.
- 실행 후 8종 프로브 전부 통과: 카드 컬럼 42703·돌봄 레벨 생존·지도 뷰 정상(퍼징 유지)·테두리 0행·card_battles 404·닉네임 중복 0·profiles_public 정상·EXP 함수 생존.
- **8/27 시작한 카드 폐지가 코드(-2,450줄)→데이터→안전망까지 3단 완결.**

## 교훈
1. **기능 폐지 = 코드 grep만으론 부족** — DB 함수(RPC)·트리거가 지워질 컬럼을 읽고 있으면 무증상 사망. 이번 P0가 정확히 그 유형.
2. **동적 생성 뷰(select 전 컬럼)는 컬럼 drop의 숨은 의존자** — 2BP01 힌트의 CASCADE는 두 번 다 함정이었다. 뷰 재정의가 정답.
3. 상수→함수 API 변경 시 테스트 동반 수정 — 9일 빨간불의 교훈. CI 실패 메일은 읽자.
4. PowerShell로 소스 파일 텍스트 치환 금지(인코딩 파괴 실사고) — 텍스트 수정은 Edit 도구·git checkout 복구.

## 실행 대기 / 다음
- **9/2(화) 판정**: 첫밥 fallback·찜·signup_source — 기준은 키트에 박제됨, 당일 재해석 금지
- 토스 회신(~9/4-9/11) → 그날 세트: 토큰 회전·웹훅 실사격·오픈 게이트
- 사장님 선택: 월동 인터뷰(키트 준비됨)·행정 3건
- 수용 리스크 재방문 조건: F-3은 주문량 생기면, 다이제스트 원가는 매입가 변동 시
