// ══════════════════════════════════════════
// 실화폐 결제(토스페이먼츠) 활성화 플래그
// ⚠ 통신판매업 신고는 완료(제2026-인천검단-0207호). 지금 false인 이유는 토스페이먼츠
//    계약심사(라이브 키) 미완 — 테스트 키로 켜는 것은 금지. 라이브 키 전환 + D-day 체크리스트
//    (box/쇼핑오픈_체크리스트.md) 통과 후 true로 바꾸면 주문생성·결제창·서버 승인이 한 번에 열린다.
// 이 플래그는 실화폐(KRW) 결제 경로만 제어한다(포인트 적립·찜·장바구니는 무관).
// ══════════════════════════════════════════

// 타입을 boolean으로 넓혀 둠 — 리터럴 false로 좁혀지면 소비처에서 "항상 참" 린트가
// 빌드를 막을 수 있어서. 오픈 시 이 값만 true로 바꾸면 결제가 열린다.
export const PAYMENT_ENABLED: boolean = false;

export const PAYMENT_DISABLED_MESSAGE =
  "결제 시스템을 연결하는 중이에요. 정식 오픈 때 알려드릴게요.";

// 토스페이먼츠 결제 수수료율 (VAT 포함) — 발주 다이제스트의 "남는 돈" 계산용.
// ⚠ 현재는 계약 확정 전이라 일반 요율 3.3%로 보수적으로 잡음(남는 돈이 실제보다
//   적게 보이는 쪽이 안전). 심사 통과 후 계약서의 실제 요율(영세 우대 시 더 낮음)로
//   이 값만 교체하면 된다. 정산 리포트와 대조해 검증할 것.
export const TOSS_FEE_RATE = 0.033;
